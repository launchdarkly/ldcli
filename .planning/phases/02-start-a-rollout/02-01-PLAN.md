---
phase: 02-start-a-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/rollouts/instructions.go
  - internal/rollouts/idempotency.go
  - cmd/cliflags/flags.go
  - .planning/STATE.md
autonomous: true
requirements: [START-01, START-02, START-03]
requirements_addressed: [START-01, START-02, START-03]
tags: [rollouts, cobra, semantic-patch, prerequisites]

must_haves:
  truths:
    - "internal/rollouts/instructions.go declares SemanticPatch.EnvironmentKey (`json:\"environmentKey\"`) so Plan 02's Start can route to the right env (RESEARCH Pitfall 2)."
    - "internal/rollouts/instructions.go declares the full Phase 2 StartInstruction shape (releaseKind, originalVariationId, targetVariationId, randomizationUnit, stages, metrics, metricMonitoringPreferences, ruleId) and supporting types (StageInput, MetricSource, MetricMonitoringPref) so Plan 02 compiles without touching this file."
    - "internal/rollouts/idempotency.go is deleted (D-10 cleanup): no Idempotency-Key code path remains anywhere in internal/rollouts/."
    - "cmd/cliflags/flags.go declares 7 new flag constants (StagesFlag, TargetVariationFlag, OriginalVariationFlag, RandomizationUnitFlag, PauseOnRegressionFlag, RevertOnRegressionFlag, RuleIDFlag) and 7 paired *Description constants so Plan 02 can call cmd.Flags().String(cliflags.StagesFlag, ...) without inlining literals."
    - "STATE.md stack-research row no longer references `google/uuid` for Idempotency-Key (per D-10 follow-up); the rest of the row (go-retryablehttp, golang.org/x/term) is preserved."
  artifacts:
    - path: internal/rollouts/instructions.go
      provides: "SemanticPatch.EnvironmentKey + full StartInstruction + StageInput + MetricSource + MetricMonitoringPref"
      contains: "EnvironmentKey, ReleaseKind, OriginalVariationID, TargetVariationID, RandomizationUnit, Stages, Metrics, MetricMonitoringPreferences, RuleID"
    - path: cmd/cliflags/flags.go
      provides: "Phase 2 flag constants"
      contains: "StagesFlag, TargetVariationFlag, OriginalVariationFlag, RandomizationUnitFlag, PauseOnRegressionFlag, RevertOnRegressionFlag, RuleIDFlag"
  key_links:
    - from: "internal/rollouts/instructions.go (SemanticPatch.EnvironmentKey)"
      to: "internal/rollouts/start.go (Plan 02) — Start builds SemanticPatch{EnvironmentKey: envKey, Instructions: []interface{}{instr}}"
      via: "JSON field `environmentKey` in the PATCH body"
      pattern: "EnvironmentKey\\s+string\\s+`json:\"environmentKey\"`"
    - from: "cmd/cliflags/flags.go (StagesFlag etc.)"
      to: "cmd/flags/rollouts/start.go (Plan 02) — initStartFlags reads cliflags.StagesFlag"
      via: "exported const string"
      pattern: "StagesFlag\\s+="
---

<objective>
Ship the three small prerequisite changes Plan 02 (the vertical Start slice) depends on:

1. Fix `SemanticPatch` to include the required `environmentKey` field — RESEARCH Pitfall 2 / VERIFIED against gonfalon `instruction_start_automated_release.go` (the server resolves env routing from `request.Body.EnvironmentKey`).
2. Flesh out `StartInstruction` (and the supporting `StageInput` / `MetricSource` / `MetricMonitoringPref` types) per RESEARCH §"StartInstruction Canonical Shape" + D-04/D-05/D-06/D-07/D-12 so Plan 02 can build a request body without re-typing the struct.
3. Register Phase 2's CLI flag constants in `cmd/cliflags/flags.go` (Stages, TargetVariation, OriginalVariation, RandomizationUnit, PauseOnRegression, RevertOnRegression, RuleID) so Plan 02's `initStartFlags` only wires bindings — no flag-name string literals get sprinkled across two files.
4. Delete `internal/rollouts/idempotency.go` per D-10 (Idempotency-Key is out of scope for the entire project; the dormant `SetIdempotencyKey` helper is dead code that contradicts the locked decision).
5. Update STATE.md's stack-research row to strike the `google/uuid` for `Idempotency-Key` reference (D-10 follow-up).

Purpose: Plan 02 ships the vertical slice (Client.Start + cmd/flags/rollouts/start.go + tests + smoke) and needs these foundations in place first. Splitting them out keeps Plan 02 below the 50% context budget and isolates the trivial prerequisite changes from the high-risk Start implementation. Plan 01 has no checkpoint, no tests of its own (the compile + Plan 02's tests cover behavior), and lands in a single small commit.

Output: Plan 02 can `go build ./...` against the updated `instructions.go`, import `cliflags.StagesFlag`, and reference `SemanticPatch.EnvironmentKey` directly without modifying any of these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-start-a-rollout/02-CONTEXT.md
@.planning/phases/02-start-a-rollout/02-RESEARCH.md
@.planning/phases/02-start-a-rollout/02-PATTERNS.md

@internal/rollouts/instructions.go
@internal/rollouts/idempotency.go
@cmd/cliflags/flags.go

<interfaces>
<!-- Plan 02 will consume these contracts. They MUST match exactly. -->

From internal/rollouts/instructions.go (after this plan):
```go
type SemanticPatch struct {
    EnvironmentKey string        `json:"environmentKey"`
    Comment        string        `json:"comment,omitempty"`
    Instructions   []interface{} `json:"instructions"`
}

type StartInstruction struct {
    Kind                        string                          `json:"kind"`                        // always "startAutomatedRelease"
    ReleaseKind                 string                          `json:"releaseKind"`                 // "guarded" | "progressive" (D-05)
    OriginalVariationID         string                          `json:"originalVariationId"`         // UUID _id only (RESEARCH Q1)
    TargetVariationID           string                          `json:"targetVariationId"`           // UUID _id only
    RandomizationUnit           string                          `json:"randomizationUnit"`
    Stages                      []StageInput                    `json:"stages"`
    Metrics                     []MetricSource                  `json:"metrics,omitempty"`
    MetricMonitoringPreferences map[string]MetricMonitoringPref `json:"metricMonitoringPreferences,omitempty"`
    RuleID                      string                          `json:"ruleId,omitempty"`            // D-07: empty = fallthrough
}

type StageInput struct {
    Allocation     int   `json:"allocation"`     // basis points (D-02: percent * 1000)
    DurationMillis int64 `json:"durationMillis"` // D-03: time.ParseDuration(s).Milliseconds()
}

type MetricSource struct {
    Key string `json:"key"`
}

type MetricMonitoringPref struct {
    AutoRollback bool `json:"autoRollback"` // D-04: false = pause, true = revert
}
```

From cmd/cliflags/flags.go (after this plan):
```go
OriginalVariationFlag    = "original-variation"
PauseOnRegressionFlag    = "pause-on-regression"
RandomizationUnitFlag    = "randomization-unit"
RevertOnRegressionFlag   = "revert-on-regression"
RuleIDFlag               = "rule-id"
StagesFlag               = "stages"
TargetVariationFlag      = "target-variation"
// + paired *Description string constants for each
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SemanticPatch + flesh out StartInstruction in internal/rollouts/instructions.go</name>
  <files>internal/rollouts/instructions.go</files>
  <read_first>
    - internal/rollouts/instructions.go (the file you are editing; current stub has SemanticPatch missing EnvironmentKey and StartInstruction has only a Kind field)
    - .planning/phases/02-start-a-rollout/02-PATTERNS.md — section "`internal/rollouts/instructions.go` — flesh out StartInstruction + fix SemanticPatch" has the exact target shapes with paired PAPERCUT comments
    - .planning/phases/02-start-a-rollout/02-RESEARCH.md — §"Pattern 2: StartInstruction Canonical Shape" + §"Pitfall 2: SemanticPatch missing environmentKey" + Open Question 2
    - .planning/phases/02-start-a-rollout/02-CONTEXT.md — D-04, D-05, D-06, D-07, D-12 anchor the field set; D-10 is why Idempotency-Key is absent
    - /Users/alex/code/launchdarkly/gonfalon/internal/flags/instruction/instruction_start_automated_release.go (canonical source for AutomatedReleaseStageInput, originalVariationId/targetVariationId, releaseKind validation rules) — only the field shape, not the validation logic
  </read_first>
  <action>
    Apply two edits to internal/rollouts/instructions.go:

    (a) Add an `EnvironmentKey string \`json:"environmentKey"\`` field at the TOP of the `SemanticPatch` struct (above Comment). Required by the server per RESEARCH Pitfall 2 + gonfalon source. Keep `Comment` and `Instructions` unchanged. Update the SemanticPatch doc comment one-liner to mention that EnvironmentKey is required for routing semantic-patch operations to the correct environment.

    (b) Replace the single-field `StartInstruction` stub (the `type StartInstruction struct { Kind string \`json:"kind"\` }` block) with the full Phase 2 shape and supporting types. Use the exact JSON tags from PATTERNS.md "internal/rollouts/instructions.go" section. Fields and tags:
      - StartInstruction: Kind ("kind"), ReleaseKind ("releaseKind"), OriginalVariationID ("originalVariationId"), TargetVariationID ("targetVariationId"), RandomizationUnit ("randomizationUnit"), Stages ([]StageInput "stages"), Metrics ([]MetricSource "metrics,omitempty"), MetricMonitoringPreferences (map[string]MetricMonitoringPref "metricMonitoringPreferences,omitempty"), RuleID ("ruleId,omitempty").
      - StageInput: Allocation int ("allocation"), DurationMillis int64 ("durationMillis").
      - MetricSource: Key string ("key"). Do NOT include an IsGroup field (D-06: metric groups deferred to v1.1).
      - MetricMonitoringPref: AutoRollback bool ("autoRollback").

    Leave the `StopInstruction` and `DismissRegressionInstruction` stub structs untouched (Phase 4 fleshes those out).

    Add doc comments noting:
      - "PAPERCUT: PC-012 — `releaseKind` in the request body vs `kind` in the response" (on StartInstruction)
      - "PAPERCUT: PC-013 — `originalVariationId` (not `controlVariationId`)" (on OriginalVariationID)
      - "PAPERCUT: PC-014 — `durationMillis` is int64 millis; the CLI converts from a Go duration string at parse time (D-03)" (on StageInput.DurationMillis)
      - "PAPERCUT: PC-010 — `Metrics` and `MetricMonitoringPreferences` are parallel collections keyed by metric key; the CLI command layer reconciles them in a single pass (D-04)" (on MetricMonitoringPreferences)
      - "UUID _id only — NOT variation key (RESEARCH Q1)" (on OriginalVariationID and TargetVariationID)
      - "D-07: empty = fallthrough rule" (on RuleID)
      - "D-04: false = pause-on-regression; true = revert-on-regression" (on MetricMonitoringPref.AutoRollback)

    Do not import anything new. Do not delete the existing file-level package doc comment if any. Run `gofmt -w internal/rollouts/instructions.go` after the edit.
  </action>
  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli && go build ./internal/rollouts/ && grep -c 'EnvironmentKey\s\+string\s\+`json:"environmentKey"`' internal/rollouts/instructions.go | grep -q '^1$' && grep -c 'TargetVariationID\s\+string\s\+`json:"targetVariationId"`' internal/rollouts/instructions.go | grep -q '^1$' && grep -c '`json:"metricMonitoringPreferences,omitempty"`' internal/rollouts/instructions.go | grep -q '^1$' && grep -c 'IsGroup' internal/rollouts/instructions.go | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `internal/rollouts/instructions.go` compiles cleanly (`go build ./internal/rollouts/` succeeds).
    - `SemanticPatch` declares exactly one field with the JSON tag `"environmentKey"`, named `EnvironmentKey`, of type `string` (no `omitempty`).
    - `StartInstruction` declares fields with these exact JSON tags: `"kind"`, `"releaseKind"`, `"originalVariationId"`, `"targetVariationId"`, `"randomizationUnit"`, `"stages"`, `"metrics,omitempty"`, `"metricMonitoringPreferences,omitempty"`, `"ruleId,omitempty"`.
    - `StageInput` declares two fields: `Allocation int` with tag `"allocation"`, and `DurationMillis int64` with tag `"durationMillis"`.
    - `MetricSource` declares exactly one field: `Key string` with tag `"key"`. No `IsGroup` field anywhere in the file (D-06).
    - `MetricMonitoringPref` declares exactly one field: `AutoRollback bool` with tag `"autoRollback"`.
    - `StopInstruction` and `DismissRegressionInstruction` stubs remain unchanged (still have just a `Kind string` field).
    - File ends with a trailing newline (end-of-file-fixer pre-commit hook).
    - `gofmt -l internal/rollouts/instructions.go` produces no output (file is formatted).
  </acceptance_criteria>
  <done>internal/rollouts/instructions.go compiles, exposes the exact contracts in the &lt;interfaces&gt; block, and is formatted per gofmt; Plan 02 can build StartInstruction structs without further edits to this file.</done>
</task>

<task type="auto">
  <name>Task 2: Delete internal/rollouts/idempotency.go and update STATE.md stack row</name>
  <files>internal/rollouts/idempotency.go, .planning/STATE.md</files>
  <read_first>
    - internal/rollouts/idempotency.go (the file you are deleting; sole export is `SetIdempotencyKey`, currently unreferenced anywhere in the codebase per RESEARCH §Pitfall 7)
    - .planning/STATE.md (the "Decisions Made" table row containing the `go-retryablehttp@v0.7.7` / `google/uuid` / `golang.org/x/term` stack reference — line near "Stack research")
    - .planning/phases/02-start-a-rollout/02-CONTEXT.md — D-10 "Idempotency (out of scope for this project)" + "Deferred Ideas" §"Idempotency — strike from REQUIREMENTS.md, do not create a phase for it" (the bullet that explicitly names this STATE.md row)
    - .planning/phases/02-start-a-rollout/02-RESEARCH.md — §User Constraints "Idempotency (out of scope for project)" + §Pitfall 7
  </read_first>
  <action>
    Two file operations:

    (a) Delete `internal/rollouts/idempotency.go` entirely. Use `git rm internal/rollouts/idempotency.go` so the deletion is staged for Plan 01's commit. RESEARCH Pitfall 7 confirms it has no callers anywhere in the repo and the compile-time `var _ Client = RolloutsClient{}` assertion does not reference it. Before deleting, verify there are no callers with `grep -rn 'SetIdempotencyKey\|rollouts.idempotency\|"github.com/launchdarkly/ldcli/internal/rollouts.*Idempotency' --include='*.go' /Users/alex/code/launchdarkly/ldcli` — expect zero hits outside the file itself. If any caller is found, do NOT delete the file; stop and surface the unexpected caller to the human.

    (b) Edit `.planning/STATE.md`: in the "Decisions Made" table find the row whose Decision cell contains `go-retryablehttp@v0.7.7` and `google/uuid` and `golang.org/x/term`. Strike the `google/uuid` (already vendored) for `Idempotency-Key` segment. The corrected row should read: "`go-retryablehttp@v0.7.7` for retries; `golang.org/x/term` for TTY (no `mattn/go-isatty`)" — i.e. drop the middle clause about `google/uuid`. Preserve the Source and Outcome cells. Do not change the table column structure.

    Do NOT edit REQUIREMENTS.md or ROADMAP.md in this plan — those are tracked as a separate `/gsd-phase` follow-up task per CONTEXT.md "Required REQUIREMENTS / ROADMAP follow-ups" (Plan 01 is execution work, not planning-doc revision).
  </action>
  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli && ! test -e internal/rollouts/idempotency.go && go build ./... && grep -v '^#' .planning/STATE.md | grep -c 'google/uuid.*Idempotency-Key' | grep -q '^0$' && grep -c 'go-retryablehttp@v0.7.7' .planning/STATE.md | grep -q '^1$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/rollouts/idempotency.go` no longer exists on disk.
    - `go build ./...` succeeds with the file deleted (no caller exists; RESEARCH Pitfall 7 confirmation).
    - `grep -rn SetIdempotencyKey --include='*.go' /Users/alex/code/launchdarkly/ldcli` returns no matches.
    - `.planning/STATE.md` no longer contains the substring "google/uuid" within the same row that references "go-retryablehttp" (i.e. the strike-out is applied to the stack-research decision row).
    - `.planning/STATE.md` still contains exactly one occurrence of `go-retryablehttp@v0.7.7` (preserved) and at least one occurrence of `golang.org/x/term` (preserved).
    - Both file changes are staged via git so they land in the same Plan 01 commit.
  </acceptance_criteria>
  <done>The dormant Idempotency-Key code path is gone from internal/rollouts/ and the STATE.md stack-research row no longer claims `google/uuid` for `Idempotency-Key`. D-10 is honored in code and in tracked decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Register Phase 2 flag constants in cmd/cliflags/flags.go</name>
  <files>cmd/cliflags/flags.go</files>
  <read_first>
    - cmd/cliflags/flags.go (the file you are editing; existing constants follow `<Name>Flag = "kebab-case-name"` paired with `<Name>FlagDescription = "..."` — see lines 25-70)
    - .planning/phases/02-start-a-rollout/02-PATTERNS.md §"`cmd/cliflags/flags.go` — new flag constants for start verb" — has the exact constant names + description strings to use
    - .planning/phases/02-start-a-rollout/02-RESEARCH.md §"Pattern 1" (variation UUID-only requirement; the description text MUST call this out) + Open Question 1 (the `jq '.variations[]'` workflow hint)
    - .planning/phases/02-start-a-rollout/02-CONTEXT.md — D-01..D-04 anchor the flag set; D-07 anchors --rule-id; D-09 anchors the ABSENCE of --skip-health-checks; D-10 anchors the ABSENCE of --idempotency-key
  </read_first>
  <action>
    Add seven new flag-name constants and seven paired `<Name>FlagDescription` constants to the `const` block in `cmd/cliflags/flags.go`. Insert them in alphabetical order within the existing block (the existing block IS already alphabetized — keep it that way). Use the exact name strings from PATTERNS.md:

    Constants (name → string value):
      - `OriginalVariationFlag    = "original-variation"`
      - `PauseOnRegressionFlag    = "pause-on-regression"`
      - `RandomizationUnitFlag    = "randomization-unit"`
      - `RevertOnRegressionFlag   = "revert-on-regression"`
      - `RuleIDFlag               = "rule-id"`
      - `StagesFlag               = "stages"`
      - `TargetVariationFlag      = "target-variation"`

    Paired descriptions (use the exact strings from PATTERNS.md §"cmd/cliflags/flags.go" — they encode the UUID-only requirement, the percent+duration syntax, and the mutual-exclusivity guarantee between --pause-on-regression and --revert-on-regression):
      - `OriginalVariationFlagDescription`  — "The variation UUID (_id) that represents the original/control variation. Obtain via: ldcli flags get --flag <key> --output json | jq '.variations[]'"
      - `PauseOnRegressionFlagDescription`  — "Metric key to monitor; pauses the rollout at current stage on regression (repeatable). Use --revert-on-regression to auto-rollback instead. A metric cannot appear in both flags."
      - `RandomizationUnitFlagDescription`  — "Randomization unit for the experiment (e.g. user, organization)"
      - `RevertOnRegressionFlagDescription` — "Metric key to monitor; automatically reverts the rollout on regression (repeatable). Use --pause-on-regression to pause instead. A metric cannot appear in both flags."
      - `RuleIDFlagDescription`             — "Existing rule UUID to roll out on. Omit for fallthrough (default rule). Obtain rule IDs from the LaunchDarkly UI or API."
      - `StagesFlagDescription`             — "Comma-separated list of stages as <allocation%>:<duration> (e.g. 25:60m,50:60m,100:60m). Allocation must be a whole percent integer [1-100]; duration must include a unit (60m, 1h30m, 300s). The CLI converts allocation to basis-points and duration to milliseconds for the API."
      - `TargetVariationFlagDescription`    — "The variation UUID (_id) that traffic will be shifted to. Obtain via: ldcli flags get --flag <key> --output json | jq '.variations[]'"

    Do NOT add the new constants to the `AllFlagsHelp()` map (those are per-command flags, not config-persistable per the existing comment on the function). Do NOT register `--idempotency-key` (D-10) or `--skip-health-checks` (D-09). Do NOT add an `ExtensionDurationFlag` (RESEARCH Q5 recommends omitting for Phase 2).

    Run `gofmt -w cmd/cliflags/flags.go` after the edit.
  </action>
  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli && go build ./cmd/cliflags/ && for c in StagesFlag TargetVariationFlag OriginalVariationFlag RandomizationUnitFlag PauseOnRegressionFlag RevertOnRegressionFlag RuleIDFlag StagesFlagDescription TargetVariationFlagDescription OriginalVariationFlagDescription RandomizationUnitFlagDescription PauseOnRegressionFlagDescription RevertOnRegressionFlagDescription RuleIDFlagDescription; do grep -c "^\s*$c\b" cmd/cliflags/flags.go | grep -q '^1$' || { echo "MISSING: $c"; exit 1; }; done && grep -c 'IdempotencyKeyFlag\|SkipHealthChecksFlag\|ExtensionDurationFlag' cmd/cliflags/flags.go | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/cliflags/flags.go` defines all 7 new flag constants with the exact string values listed above (verified by grep).
    - `cmd/cliflags/flags.go` defines all 7 new `*FlagDescription` constants with descriptions containing the load-bearing substrings: "UUID (_id)" (for variation flags), "Comma-separated list of stages" (for StagesFlagDescription), "Existing rule UUID" (for RuleIDFlagDescription), "A metric cannot appear in both flags" (for both regression flags).
    - `cmd/cliflags/flags.go` does NOT define `IdempotencyKeyFlag`, `SkipHealthChecksFlag`, or `ExtensionDurationFlag` (those are out of Phase 2 scope per D-09, D-10, RESEARCH Q5).
    - `AllFlagsHelp()` map is unchanged (none of the 7 new constants appear in its body).
    - `go build ./cmd/cliflags/` succeeds.
    - `gofmt -l cmd/cliflags/flags.go` produces no output.
  </acceptance_criteria>
  <done>cmd/cliflags/flags.go exposes the 7 new flag-name constants and 7 paired descriptions Plan 02 will consume in its initStartFlags; no forbidden constants (idempotency, skip-health-checks, extension-duration) leak into the package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → CLI process | Operator-supplied flag values cross into the process — Plan 01 only defines the surface; Plan 02 wires Cobra validation. No PII/secrets enter via these flags. |
| CLI process → file system | Plan 01 deletes one file (`internal/rollouts/idempotency.go`) and edits two files. No untrusted-content paths involved (file contents are committed source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | `cmd/cliflags/flags.go` description strings (visible in `--help` output and any error string) | mitigate | Descriptions reference only public LD UI workflows (`ldcli flags get | jq '.variations[]'`) — no internal endpoints, no example tokens, no real flag keys or account IDs. |
| T-02-01-02 | Tampering | `internal/rollouts/idempotency.go` deletion via `git rm` | mitigate | RESEARCH Pitfall 7 confirms zero callers; Task 2's verify step re-checks via grep before deletion; deletion is staged in same commit as the SemanticPatch / StartInstruction additions for review-as-a-unit. |
| T-02-01-03 | Tampering | `internal/rollouts/instructions.go` struct shape change (SemanticPatch + StartInstruction) | mitigate | Acceptance criteria pin exact JSON tags by grep-counted regex; Plan 02 tests will round-trip these structs against gonfalon-shaped fixtures, surfacing any silent typo. No compile-time-only fields added. |
| T-02-01-04 | Spoofing | (N/A — no auth surface change in this plan) | accept | Plan 01 changes type shapes and constants only; the access-token path is untouched. |
| T-02-01-05 | Denial of Service | (N/A — no input handler added) | accept | Plan 01 introduces struct fields, not handlers. Stage/duration parsing — and its associated DoS surface — lives in Plan 02's `parseStages`. |
</threat_model>

<verification>
- `cd /Users/alex/code/launchdarkly/ldcli && go build ./...` succeeds.
- `cd /Users/alex/code/launchdarkly/ldcli && go test ./internal/rollouts/ ./cmd/flags/rollouts/ ./cmd/cliflags/` succeeds (Phase 1 tests still pass; no new tests in this plan because the added types are exercised by Plan 02).
- `cd /Users/alex/code/launchdarkly/ldcli && gofmt -l internal/rollouts/instructions.go cmd/cliflags/flags.go` produces no output.
- `cd /Users/alex/code/launchdarkly/ldcli && ! test -e internal/rollouts/idempotency.go`.
- `grep -rn SetIdempotencyKey --include='*.go' /Users/alex/code/launchdarkly/ldcli` returns no matches.
</verification>

<success_criteria>
- All 3 task acceptance_criteria blocks pass (verified by their `<verify>` commands).
- The repository compiles (`go build ./...`).
- Phase 1's existing test suite still passes (`go test ./internal/rollouts/ ./cmd/flags/rollouts/`).
- Plan 02's `read_first` for `internal/rollouts/start.go` can rely on the updated SemanticPatch / StartInstruction shapes existing.
- `internal/rollouts/idempotency.go` is gone; STATE.md reflects the D-10 strike.
</success_criteria>

<output>
After completion, create `.planning/phases/02-start-a-rollout/02-01-SUMMARY.md` summarizing:
- The three concrete prerequisite changes shipped (SemanticPatch.EnvironmentKey added, StartInstruction fleshed out, idempotency.go deleted, 7 flag constants added).
- The `go build` + `go test` evidence those changes are non-breaking.
- Pointers Plan 02 needs: the exact JSON tag list on StartInstruction, the 7 flag constant names, the confirmation that EnvironmentKey is now at the top of SemanticPatch.
- Note any unexpected friction (e.g. a forgotten caller of SetIdempotencyKey found by Task 2's grep) so Plan 02 can adjust.
</output>
