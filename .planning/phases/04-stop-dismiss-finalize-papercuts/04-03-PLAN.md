---
phase: 04-stop-dismiss-finalize-papercuts
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md
  - .planning/API-PAPERCUTS.md
  - .planning/CLI-LEARNINGS.md
autonomous: false
requirements:
  - STOP-01
  - STOP-02
  - STOP-03
  - STOP-04
  - LEARN-02
  - DOC-02
  - DOC-04
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "The stop command runs end-to-end against real LaunchDarkly staging on at least three scenarios (happy path: roll forward to target variation, happy path: roll back to original variation, already-terminal refusal) and the captured envelopes are recorded in 04-SMOKE.md."
    - "The dismiss-regression command runs end-to-end against real LaunchDarkly staging on at least two scenarios (happy path: dismissal lands within the polling budget, no-active-regression refusal) and the captured envelopes are recorded in 04-SMOKE.md; the bounded-backoff timeout case is captured if the staging environment surfaces it organically (note in SMOKE.md if not reproducible)."
    - "Any API contract observation surfaced lands as a new PC-NNN entry in `.planning/API-PAPERCUTS.md` AND a corresponding update to Confluence page_id 4875452435 (DOC-02 + DOC-04). The fetch-first pattern is honored for Confluence updates per memory `feedback_confluence_fetch_first.md`."
    - "Any CLI/UX observation surfaced lands as an updated or new entry in `.planning/CLI-LEARNINGS.md` (LEARN-02). Specifically, the four open questions from Plan 04-02's SUMMARY (polling-budget rightness, dismiss-instruction-body-shape, post-dismiss Status.Kind, BuildUIURL path shape) are answered empirically with findings appended to the existing CL-NNN entries or added as new ones."
    - "Token redaction discipline holds: every captured Command block has the literal access token replaced with `[REDACTED]` BEFORE the file is committed; the grep gate `grep -E '[a-f0-9]{24,}'` returns nothing."
    - "The phase is not complete until both 04-SMOKE.md and the papercut/learnings appends are committed — this gates Plan 04-04 (DOC-03 + LEARN-03 milestone review) which needs the final state of both artifacts."
  artifacts:
    - path: ".planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md"
      provides: "Real-staging exercise log with command invocations, captured envelopes, observations, and explicit findings on the four Plan 04-02 open questions"
      contains: "Smoke A"
    - path: ".planning/API-PAPERCUTS.md"
      provides: "Updated active index + any new PC-NNN entries surfaced by Phase 4 staging exercise"
      contains: "Last updated:"
    - path: ".planning/CLI-LEARNINGS.md"
      provides: "Updated entries reflecting Phase 4 staging observations (LEARN-02 append discipline) and answers to Plan 04-02's open questions"
      contains: "Active Index"
  key_links:
    - from: ".planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md"
      to: ".planning/API-PAPERCUTS.md"
      via: "Observations section cross-references any new PC-NNN anchors"
      pattern: "PC-0"
    - from: ".planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md"
      to: ".planning/CLI-LEARNINGS.md"
      via: "Observations section cross-references CL-NNN anchors that were updated or added"
      pattern: "CL-0"
    - from: ".planning/API-PAPERCUTS.md"
      to: "Confluence page_id 4875452435"
      via: "fetch-first update (mcp__mcp-atlassian__confluence_get_page → confluence_update_page) per memory feedback_confluence_fetch_first.md"
      pattern: "4875452435"
---

<objective>
Run the `stop` AND `dismiss-regression` commands against real LaunchDarkly staging across at least 5 scenarios (3 stop, 2 dismiss; bonus 6th if the eventual-consistency timeout case is reproducible), record outcomes in `04-SMOKE.md`, and feed any new API papercuts or CLI/UX observations into the two learnings docs plus the Confluence page.

Purpose: real-staging exercise is required by PROJECT.md's "real-server validation" constraint and is the gating condition for Phase 4 completion. Mirrors Plan 03-02's structure and rigor. The prototype's whole point is to surface learnings — this is where the abstract Plan 04-01 + 04-02 implementations meet reality.

**Open questions inherited from Plan 04-02 SUMMARY (must be answered empirically):**
1. **Is the polling budget right?** Default is 1s/3s/5s (~9s total). Plan 04-03 measures how long dismissal actually takes to propagate.
2. **Does the upstream dismissRegression instruction body really have no fields besides Kind?** Architecture research suggested empty-besides-Kind; verify against real staging response.
3. **What is the post-dismiss `Status.Kind` value?** "active"? "in_progress"? Something else? Empirical.
4. **Does the `BuildUIURL` path shape match the real LD UI?** Plan 04-01's helper used a best-guess shape; click the URL during smoke and verify.

**This plan is checkpoint-bearing (autonomous: false)** because real-staging exercise requires the operator to have a writer-scoped staging token loaded, to have at least one rollout in a state that can be stopped, to have at least one rollout in a state that can be dismissed (i.e., currently regressed), and to inspect the captured envelopes before papercuts are filed.

Output: an `04-SMOKE.md` log with captured envelopes; any new PC-NNN entries in API-PAPERCUTS.md + matching Confluence update; any LEARN-02 appends in CLI-LEARNINGS.md; explicit findings on the four open questions above.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/API-PAPERCUTS.md
@.planning/CLI-LEARNINGS.md
@.planning/STATE.md

@.planning/phases/04-stop-dismiss-finalize-papercuts/04-01-PLAN.md
@.planning/phases/04-stop-dismiss-finalize-papercuts/04-02-PLAN.md
@.planning/phases/03-status-watch/03-SMOKE.md
@.planning/phases/03-status-watch/03-02-SUMMARY.md
@.planning/phases/02-start-a-rollout/02-SMOKE.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-SMOKE.md

<interfaces>
<!-- Reference patterns for SMOKE.md structure, papercut entry format, and Confluence update flow.
     All inherited from Plan 03-02's pattern. -->

From .planning/phases/03-status-watch/03-SMOKE.md (template structure — the most recent SMOKE.md):
- Top-level "Environment" section (Base URI, token sourcing, binary, project, environment, flag fixtures)
- One section per smoke scenario (## Smoke A, ## Smoke B, ...) with: Command (token redacted), Exit code, Stdout, Stderr, Verdict, Contract observations
- Final "Observations / Follow-ups" section that cross-references new PC-NNN entries

From .planning/API-PAPERCUTS.md (per-entry template at lines 43-51):
- Title / Discovered (with phase milestone) / API behavior / CLI workaround (annotated `// PAPERCUT: PC-NNN`) / What we'd prefer / Status / Removal criteria

From memory feedback_confluence_fetch_first.md:
- Always fetch current Confluence content via `mcp__mcp-atlassian__confluence_get_page` BEFORE calling `confluence_update_page`
- Confluence page_id = 4875452435

From .planning/CLI-LEARNINGS.md (per-entry template):
- Question / What we did in prototype / What's open for production CLI build / Severity (low/medium/high)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm staging credentials and scenario fixtures are available</name>
  <files>(no files — checkpoint task; operator confirms preconditions)</files>

  <what-built>
    Plans 04-01 and 04-02 have shipped working `./ldcli flags rollouts-beta stop` and `./ldcli flags rollouts-beta dismiss-regression` subcommands. Plan 04-03 cannot proceed automatically — real-staging exercise needs:
    1. A writer-scoped LaunchDarkly staging access token.
    2. At least one rollout currently in a non-terminal state (active, paused, or regressed) on the `alex-engelberg-dev` project that the operator is willing to STOP via the CLI.
    3. At least one rollout currently in the `regressed` state on `alex-engelberg-dev` that the operator is willing to DISMISS via the CLI.
    4. Confluence MCP available.

    Phase 1, 2, 3 SMOKE.md files all reference the same staging account; Phase 3 used `ldcli-blitz-2-guarded-rollouts` (paused guarded rollout from 2026-05-13). The operator may need to start fresh rollouts to ensure dismiss-regression has a regressed rollout to operate on. Starting a new rollout (via Phase 2's `start` command, which has been working since 2026-05-13) is a prerequisite step the operator may need to take.
  </what-built>

  <how-to-verify>
    1. **Token availability:** confirm a staging access token is loaded (typical pattern: `~/secret/ld-staging-token` or `ldcli config --set access-token=...`). Operator confirms via "token loaded" or pastes via config command. Do NOT paste the token into chat or any artifact.
    2. **At least one stoppable rollout exists** on `alex-engelberg-dev` / `test`: operator confirms `ldcli flags rollouts-beta list --base-uri https://ld-stg.launchdarkly.com --project alex-engelberg-dev --flag <candidate> --environment test --output json` returns at least one item whose `status.kind` is in {active, regressed, paused}. The operator names two flag fixtures: (a) one for the "roll forward" Smoke A (the operator picks the TARGET variation as the final variation), (b) one for the "roll back" Smoke B (the operator picks the ORIGINAL variation as the final variation). Same flag is acceptable if Smoke A and Smoke B can be executed in sequence (Smoke A's rollout is terminal after the stop, then a new rollout is started for Smoke B).
    3. **At least one regressed rollout exists** on `alex-engelberg-dev` / `test`: operator confirms a flag has a rollout currently in `status.kind == "regressed"`. The Phase 3 fixture `ldcli-blitz-2-guarded-rollouts` was paused due to regression as of 2026-05-13; if that's still the case, it's a candidate. If no regressed rollout currently exists, the operator may need to start a fresh guarded rollout against a known-bad metric to produce one. The operator names the flag fixture.
    4. **At least one terminal rollout exists** for the Smoke C "already-terminal refusal" case: any rollout where `status.kind` is in {completed, reverted}. Most stoppable rollouts from prior smokes have terminated by now; the operator names a candidate.
    5. **Confluence MCP available:** operator confirms `mcp__mcp-atlassian__confluence_get_page` and `confluence_update_page` work in this session.

    If any fixture is missing, the operator declares "skip Smoke X" and the implementer adapts the smoke run accordingly. The bounded-backoff-timeout case (Smoke F — bonus) is opportunistic: it only fires if the staging eventual-consistency window happens to exceed the 9-second polling budget, which is unpredictable. If it does NOT reproduce during the live exercise, the implementer records "could not reproduce empirically" in 04-SMOKE.md and consults CL-NNN appends accordingly (open question #1 stays partially answered).
  </how-to-verify>

  <action>
    Pause execution. The implementer presents the precondition checklist (token availability, stoppable rollout fixture, regressed rollout fixture, terminal rollout fixture, Confluence MCP) to the operator. The operator confirms each precondition by responding with the chosen flag keys and any environment overrides. Once confirmed, the implementer resumes with Task 2.
  </action>

  <verify>Operator responds to the resume-signal prompt with chosen flag keys and any environment overrides; Task 2 cannot start until this checkpoint resolves.</verify>

  <done>Operator has confirmed token availability, named the flag keys for each smoke scenario (A through E at minimum; F optional), confirmed Confluence MCP works, and explicitly skipped any scenario for which a fixture is not reachable.</done>

  <resume-signal>Type "approved" with the chosen flag keys for: (a) Smoke A — stop to target variation, (b) Smoke B — stop to original variation, (c) Smoke C — already-terminal refusal (any terminal flag), (d) Smoke D — dismiss happy path (must be a flag with a currently-regressed rollout), (e) Smoke E — no-active-regression refusal (any flag with a non-regressed rollout). If any scenario is not available, name what IS available and the implementer will adapt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute stop and dismiss-regression against real staging and capture envelopes in 04-SMOKE.md</name>
  <files>.planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md</files>

  <read_first>
    - .planning/phases/03-status-watch/03-SMOKE.md (the most recent SMOKE.md — direct template for layout, section structure, observation tagging)
    - .planning/phases/02-start-a-rollout/02-SMOKE.md (Phase 2 follow-up template; the "New API Contract Discoveries" subsection format with PC-NNN cross-references; the "Action Items" tail)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-01-PLAN.md (the substrate Plan 04-01 shipped — verify the `BuildUIURL` path shape and the post-stop renderer match the real-staging output)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-02-PLAN.md (the substrate Plan 04-02 shipped — verify the bounded-backoff loop's 1s/3s/5s budget and the `meta.warnings` wiring on the timeout case)
    - .planning/API-PAPERCUTS.md (full active index — next free PC-NNN anchor is determined by scanning the index table at the top; at the time of Phase 4 planning, PC-020 was the highest, so new anchors start at PC-021)
    - .planning/CLI-LEARNINGS.md (full file — Phase 3 left the doc at CL-012; new entries continue at CL-013, but Phase 4 may also extend existing entries — e.g., updating CL-004 with the agent-driven workflow's exit-code experience)
  </read_first>

  <action>
    Execute at minimum the following 5 scenarios against real staging. Use the flag keys confirmed in Task 1. Use the `--base-uri https://ld-stg.launchdarkly.com` flag explicitly to make the SMOKE.md log self-contained. Use `--output json` for envelope captures; one plaintext capture is added at the end for sanity.

    **Smoke A — stop to target variation (roll forward):**
    Run `./ldcli flags rollouts-beta stop --base-uri https://ld-stg.launchdarkly.com --project alex-engelberg-dev --flag <flag-with-stoppable-rollout-A> --environment test --to-variation <target-variation-uuid> --output json`. Capture: exit code, full stdout JSON envelope, full stderr.
    Confirm in the captured envelope:
    - `schemaVersion == "rollouts.v1beta1"`
    - `kind == "Rollout"`
    - `data.id` matches the pre-read rollout ID (verify via a prior `status` invocation if needed)
    - `data.status.kind` is in {completed, reverted} (the post-stop terminal state)
    - **`meta.uiURL` is non-empty** (SC#4) — click the URL in a browser; verify it opens the correct rollout page in the LD UI. Record the URL in 04-SMOKE.md (it's not a secret).
    - **Contract observations:**
      - Does `data.status.kind` say `completed` or `reverted` after stopping to the TARGET variation? Empirical — Plan 04-02 open question #3 answered for the stop case.
      - Does the `meta.uiURL` actually resolve to the correct rollout page? Plan 04-01 open question — answered.
      - Did the two-step PATCH+re-fetch (Plan 04-01) take a perceivable amount of time? Record approximate latency.

    **Smoke B — stop to original variation (roll back):**
    Run the same shape as Smoke A but pass `--to-variation <ORIGINAL-variation-uuid>` (the control). Capture the envelope.
    Confirm:
    - Same envelope structure as Smoke A
    - `data.status.kind` — does roll-back show `reverted` instead of `completed`? Verify the bucket assignment.
    - **Contract observation:** does the upstream `stopAutomatedRelease` instruction body need anything beyond `kind` and `finalVariationId`? If the server validation requires a `comment` or rejects empty optional fields, that's a new papercut.

    **Smoke C — already-terminal refusal:**
    Re-run the Smoke A command (the rollout is now terminal because Smoke A stopped it). Capture: exit code, full stdout JSON envelope, full stderr.
    Confirm:
    - exit 1
    - envelope `kind == "Error"`, `error.code == "rollout_already_terminal"` on **stdout**
    - `error.message` names the current state (e.g., "completed")
    - `error.nextAction` points at `ldcli flags rollouts-beta list`
    - stderr shows the short-sentinel `rollouts stop failed` (or whatever sentinel Plan 04-01 chose)

    **Smoke D — dismiss-regression happy path:**
    Run `./ldcli flags rollouts-beta dismiss-regression --base-uri https://ld-stg.launchdarkly.com --project alex-engelberg-dev --flag <flag-with-regressed-rollout> --environment test --output json`. Capture: exit code, full stdout JSON envelope, full stderr.
    Confirm:
    - exit 0
    - envelope `kind == "Rollout"`, `data.status.kind` is no longer `regressed` (likely `active` or `paused`)
    - `meta.uiURL` populated; click it and verify
    - **Crucially: time how long the call took** — if it completed in <2s, the polling budget never fired (the initial post-PATCH List already showed the dismissed state); if it took 4-9s, the bounded-backoff loop fired and resolved before timeout; if it took ~10s AND `meta.warnings` is non-empty, the timeout path fired (open question #1 — the budget needed extension).
    - **Contract observations (Plan 04-02 open questions answered here):**
      - #2: was the dismissRegression instruction body empty-besides-kind? Verify by capturing the actual upstream request body (use Phase 1's verbose logging if available, or by observing whether the PATCH succeeded with only `{"kind": "dismissRegression"}`).
      - #3: what's the post-dismiss `Status.Kind`? Record verbatim.
      - #4: was the polling budget right? Record actual latency.

    **Smoke E — no-active-regression refusal:**
    Re-run the Smoke D command against a flag whose rollout is NOT in the regressed state (Smoke D's rollout is now resumed and no longer regressed, OR pick a fresh flag with an active rollout). Capture: exit code, envelope, stderr.
    Confirm:
    - exit 1
    - envelope `kind == "Error"`, `error.code == "no_active_regression"` on **stdout**
    - `error.message` names the current state
    - `error.nextAction` points at `status`
    - No call to the upstream PATCH was made (verify by approximate latency — should return in <500ms; the only network call should be the pre-read List).

    **Smoke F (optional / bonus) — bounded-backoff-timeout reproduction:**
    Attempt to reproduce the case where the dismiss-regression PATCH succeeds but the polling budget expires before the regressed state clears. This is unpredictable; one approach is to dismiss a regression on a heavily-loaded staging environment OR a rollout with a slow metric monitoring backend. If reproducible:
    - Capture the envelope showing `data.status.kind == "regressed"` (stale state) AND `meta.warnings[0]` containing "PC-007".
    - Note: the operator must verify via a follow-up `status` invocation that the dismissal DID actually land server-side eventually.
    If not reproducible after one attempt, mark "could not reproduce empirically" in 04-SMOKE.md and update CL-008 / CL-NNN accordingly.

    **Smoke G (optional / sanity) — plaintext sanity check:**
    Run one of the happy paths above (Smoke A or D) without `--output json`. Capture the first 10 lines of plaintext output. Confirm `RenderRolloutStopPlaintext` or `RenderRolloutDismissPlaintext` rendered the expected concise header. Skip if Task 1 confirmed plaintext is sufficiently covered by unit tests.

    **Token redaction:** every captured Command block has the literal access token replaced with `[REDACTED]` BEFORE the file is committed. Mirror 03-SMOKE.md's discipline. Run the grep gate `grep -E '[a-f0-9]{24,}' 04-SMOKE.md` and confirm it returns NOTHING before commit.

    Write `04-SMOKE.md` mirroring 03-SMOKE.md's structure. Sections:
    - Title: `# Phase 4 — Real-Staging Smoke Tests (stop + dismiss-regression)`
    - `Last updated: YYYY-MM-DD` (the actual date of the smoke run)
    - `## Environment` (Base URI, token sourcing, binary commit, project, environment, flag fixtures)
    - One `## Smoke <letter> — <one-line>` subsection per smoke (A through E mandatory, F+G optional). Each subsection: Command (token redacted), Exit code, Stdout, Stderr, Verdict, Contract observations.
    - `## Plan 04-02 open questions answered` — explicit one-paragraph answer to each of the four questions (#1 polling budget, #2 instruction body shape, #3 post-dismiss Status.Kind, #4 BuildUIURL path shape).
    - `## Observations / Follow-ups` — for each observation that looks like an API gap, draft a candidate `PC-NNN` line and tag `→ Task 3`. For each observation that's a CLI/UX question, tag `→ CLI-LEARNINGS.md CL-NNN` (extending an existing entry or proposing a new one).
  </action>

  <verify>
    <automated>test -f .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md && grep -c '^## Smoke ' .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md | awk '$1 >= 5 { exit 0 } { exit 1 }' && ! grep -E '[a-f0-9]{40,}' .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md && grep -q '^## Plan 04-02 open questions answered' .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md` exists.
    - File contains at least 5 `## Smoke ` section headings (A through E mandatory; F + G optional). Verify: `grep -c '^## Smoke ' 04-SMOKE.md` returns 5 or more.
    - File contains an `## Environment` section mirroring 03-SMOKE.md.
    - File contains an `## Plan 04-02 open questions answered` section with explicit answers to each of the four questions (polling budget, instruction body shape, post-dismiss Status.Kind, BuildUIURL path shape).
    - File contains an `## Observations / Follow-ups` section (or `## Observations` — executor discretion on punctuation).
    - **No access token is present.** `grep -E '[a-f0-9]{40,}' 04-SMOKE.md` returns NOTHING (the LD staging token is typically 40+ chars; the looser 24+ check used by Plan 03-02 may produce false positives against rollout UUIDs, so use the tighter 40+ regex here).
    - At least one stop smoke (A or B) verdict explicitly notes the actual post-stop `data.status.kind` value (proves the empirical answer to open question #3 is in the doc).
    - The "already-terminal refusal" smoke (C) records exit code 1 and `error.code: "rollout_already_terminal"` on stdout.
    - The "no-active-regression refusal" smoke (E) records exit code 1 and `error.code: "no_active_regression"` on stdout.
    - The dismiss happy-path smoke (D) records exit code 0 and the actual end-to-end latency (proves the empirical answer to open question #4).
    - The `meta.uiURL` value appears in at least one smoke envelope capture (verifies SC#4 wiring end-to-end against real staging).
  </acceptance_criteria>

  <done>04-SMOKE.md captures at least 5 real-staging command invocations across stop AND dismiss, tokens are redacted, the four Plan 04-02 open questions are answered empirically with explicit findings, and the Observations section tags candidate papercuts and CLI-LEARNINGS appends for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Append new papercuts + CLI/UX observations to API-PAPERCUTS.md + CLI-LEARNINGS.md + Confluence</name>
  <files>.planning/API-PAPERCUTS.md, .planning/CLI-LEARNINGS.md</files>

  <read_first>
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md (the file produced by Task 2 — Observations / Follow-ups section is the source of truth for what to append)
    - .planning/API-PAPERCUTS.md (full file; the active index table at the top must be updated, new entries appended to `## Entries`; next free PC-NNN anchor is determined by scanning the index — at time of Phase 4 planning, PC-020 was the highest)
    - .planning/CLI-LEARNINGS.md (full file; the doc was at CL-012 after Plan 03-02. New entries continue at CL-013; existing entries may be EXTENDED with Phase 4 empirical findings — e.g., CL-008 (typed Go structs strip wire fields) may get a Phase 4 confirmation note, CL-004 (exit-code taxonomy) may get a Phase 4 stop/dismiss real-world data point)
    - memory feedback_confluence_fetch_first.md (Confluence fetch-first pattern — always `confluence_get_page` BEFORE `confluence_update_page` for page_id 4875452435)
    - PROJECT.md "API contract learnings → Confluence" section (lines 70-72) — only **contract-shape** observations go to Confluence (confusing field names, missing data, forced workarounds, inconsistencies with the rest of the LD API surface). NOT every smoke observation.
  </read_first>

  <action>
    For each Observation in 04-SMOKE.md tagged `→ Task 3` or `→ CLI-LEARNINGS.md`:

    1. **API papercut path (DOC-02 + DOC-04):**
       - Determine the next free `PC-NNN` anchor by scanning `.planning/API-PAPERCUTS.md` Active Index. At Phase 4 plan time the highest was PC-020; new anchors start at PC-021.
       - Append a new row to the Active Index table: `| PC-NNN | <one-line> | YYYY-MM-DD | stop, dismiss-regression |` (commands affected, comma-separated).
       - Append a new `### PC-NNN - <one-line title>` entry to `## Entries` using the per-entry template (Title / Discovered with phase milestone / API behavior / CLI workaround / What we'd prefer / Status / Removal criteria).
       - **If the papercut captures a contract-shape observation** → update Confluence page_id 4875452435 (DOC-04):
         - First call `mcp__mcp-atlassian__confluence_get_page` with `page_id: 4875452435` to fetch current content.
         - Append the new papercut entry to the appropriate section (mirror the formatting of existing entries).
         - Call `mcp__mcp-atlassian__confluence_update_page` with the merged content. Per memory feedback_confluence_fetch_first.md: DO NOT blind-overwrite.
       - Increment the `Active count: N` line at the top of API-PAPERCUTS.md.
       - Update `Last updated: YYYY-MM-DD`.
       - If the new papercut has a CLI workaround anchored in source code, add a `// PAPERCUT: PC-NNN` comment at the workaround site (mirroring Plan 04-01's PC-001 and Plan 04-02's PC-007 annotation pattern).

    2. **CLI-LEARNINGS path (LEARN-02):**
       - If the observation extends an existing CL-NNN entry (e.g., Phase 4's polling-budget measurement confirms or contradicts Plan 04-02's hardcoded 1s/3s/5s), update that entry's "What we did in prototype" field with a one-line append naming the empirical finding.
       - If the observation is a NEW CLI/UX topic, append a new `### CL-NNN — <one-line>` entry with the 4-field LEARN-01 structure (Question / What we did in prototype / What's open for production CLI build / Severity).
       - Specific candidates for Phase 4 CL-NNN entries (from Plan 04-02's open questions):
         - CL-013 candidate: "Hardcoded bounded-backoff budget for dismiss-regression (1s/3s/5s, ~9s) — production CLI build should consider a `--poll-timeout` flag OR a configurable cap, since the staging eventual-consistency window measured at <Xs>"
         - CL-014 candidate: "Stop's --to-variation is REQUIRED with no implicit default — production CLI build should consider whether the operator's intent ('roll back' vs 'roll forward') deserves a higher-level flag like `--rollback` / `--roll-forward` that resolves to the original/target UUID automatically"
         - CL-015 candidate: "BuildUIURL path shape verified/needs-correction — production CLI build should treat the UI URL as a first-class concern and possibly source it from a `_links.ui` upstream field if the API exposes one"
       - Update `Active count: N` and `Last updated: YYYY-MM-DD` at the top of CLI-LEARNINGS.md.

    3. **Update existing CL-NNN entries:** Phase 3 left CL-008 (typed Go structs strip wire fields) at "high severity, downstream effect on plaintext"; Phase 4 may confirm or refute this for the stop/dismiss envelopes. If observed: append a one-line "Phase 4 confirmation: same gap observed on stop / dismiss-regression envelopes" to CL-008. Mirror for CL-001, CL-002, CL-004, CL-009 if Phase 4 surfaces relevant data.

    4. **If 04-SMOKE.md's Observations section is empty** (no new papercuts and no new CLI/UX questions):
       - Record a single LEARN-02 append on CLI-LEARNINGS.md noting that the Phase 4 smoke produced no new observations beyond CL-001..CL-012 — a finding in itself.
       - Skip Confluence update.
       - Skip API-PAPERCUTS.md append.
  </action>

  <verify>
    <automated>grep -E '^Active count: ' .planning/API-PAPERCUTS.md | head -1 && grep -E '^Active count: ' .planning/CLI-LEARNINGS.md | head -1 && grep -E '^Last updated: ' .planning/CLI-LEARNINGS.md | head -1 && grep -E '^Last updated: ' .planning/API-PAPERCUTS.md | head -1 && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/API-PAPERCUTS.md` `Last updated:` field reflects the current YYYY-MM-DD date.
    - `.planning/API-PAPERCUTS.md` `Active count:` value matches the number of rows in the Active Index table (verify by inspection — if 0 new papercuts surfaced, count stays at 20; if N new, count is 20+N).
    - `.planning/CLI-LEARNINGS.md` `Last updated:` field reflects the current YYYY-MM-DD date.
    - `.planning/CLI-LEARNINGS.md` `Active count:` value matches the number of `### CL-` entries.
    - For every new `PC-NNN` entry added to API-PAPERCUTS.md: the entry has all 7 template fields (Title / Discovered / API behavior / CLI workaround / What we'd prefer / Status / Removal criteria), AND an Active Index row exists with the same anchor.
    - For every contract-shape papercut added: the Confluence update flow followed the fetch-first pattern (verify the implementer's execution log shows `confluence_get_page` called BEFORE `confluence_update_page` for page_id 4875452435).
    - For every new CL-NNN or updated CL-NNN entry: the LEARN-01 4-field structure (Question / What we did in prototype / What's open for production CLI build / Severity) is intact.
    - 04-SMOKE.md's Observations section is consistent with the appends — every `→ Task 3` tag in 04-SMOKE.md has a corresponding PC-NNN entry; every `→ CLI-LEARNINGS.md` tag has a corresponding CL-NNN entry or update.
    - If a `// PAPERCUT: PC-NNN` source annotation is appropriate (the new papercut has a workaround anchored in code), the annotation is added at the workaround site.
  </acceptance_criteria>

  <done>Every observation from 04-SMOKE.md has been routed to its destination (API-PAPERCUTS.md + Confluence for contract gaps; CLI-LEARNINGS.md for CLI/UX questions); if no observations surfaced, that finding is itself recorded in CLI-LEARNINGS.md as a Phase 4 validation note. The doc state is ready for Plan 04-04's milestone review pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ldcli (real staging) | Live access token crosses the CLI → LD API boundary; staging environment data may have business sensitivity. |
| ldcli → 04-SMOKE.md | Captured stdout / stderr may contain tokens, env values, or sensitive flag content — implementer must redact before commit. |
| 04-SMOKE.md / API-PAPERCUTS.md → Confluence | New papercut content propagates to Confluence; concurrent human edits could be clobbered without fetch-first. |
| stop mutation → live staging rollouts | Plan 04-03 actively STOPS real rollouts. The operator confirmed each stop in Task 1's checkpoint; this is an explicitly-consented destructive action. |
| dismiss mutation → live staging rollouts | Plan 04-03 actively DISMISSES real regressions. Same consent pattern as stop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | 04-SMOKE.md captures | mitigate | Task 2 acceptance criteria includes the redaction grep gate (`grep -E '[a-f0-9]{40,}'` — tighter than Plan 03-02's 24+ to avoid false positives on rollout UUIDs). Token sourcing pattern (`~/secret/ld-staging-token`) keeps the literal token out of shell history. Captured envelopes contain rollout IDs, flag keys, env names, UI URLs — operator-internal, not cross-tenant secrets. |
| T-04-03-02 | Tampering | Confluence update | mitigate | Task 3 explicitly mandates fetch-first per memory feedback_confluence_fetch_first.md. |
| T-04-03-03 | Information Disclosure | `meta.uiURL` echoed in captured envelopes | accept | The UI URL embeds projKey + flagKey + envKey + rolloutID — all operator-internal. The URL itself is the same one the operator could construct manually; capturing it in 04-SMOKE.md is intentional (verifies SC#4 wiring). |
| T-04-03-04 | Tampering / Repudiation | The smoke STOPS and DISMISSES real staging rollouts (destructive) | mitigate | The Task 1 checkpoint requires explicit operator approval naming the specific flag keys to stop / dismiss. The implementer does not have unilateral authority to mutate staging rollouts. Each smoke command is executed with operator-supplied flag values; the implementer does not invent flag keys. |
| T-04-03-05 | Information Disclosure | --access-token in argv | accept | Carry-over from Plan 03-02 T-03P2-03; operator-controlled value, single-tenant on the operator's machine. |
| T-04-03-06 | Information Disclosure | `meta.warnings` text in the bounded-backoff-timeout case names "PC-007" | accept | Same disposition as Plan 04-02 T-04-02-05 — the anchor is a public doc reference, no operational secret. |
</threat_model>

<verification>
- `.planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md` exists with at least 5 `## Smoke ` headings, an Environment section, an Observations section, and a "Plan 04-02 open questions answered" section.
- Token redaction holds: `grep -E '[a-f0-9]{40,}' 04-SMOKE.md` returns NOTHING.
- `.planning/API-PAPERCUTS.md` and `.planning/CLI-LEARNINGS.md` have consistent `Last updated:` and `Active count:` fields.
- Every observation tagged `→ Task 3` in 04-SMOKE.md has a matching PC-NNN entry; every observation tagged `→ CLI-LEARNINGS.md` has a matching CL-NNN entry or update.
- For every new contract-shape papercut: Confluence page 4875452435 has been updated using the fetch-first pattern (implementer's execution log confirms).
- All four Plan 04-02 open questions have explicit empirical answers in 04-SMOKE.md's "Plan 04-02 open questions answered" section.
</verification>

<success_criteria>
Phase 4 SC#1 + SC#2 + SC#3 + SC#4: validated against real LaunchDarkly staging via 04-SMOKE.md — the JSON envelope shapes, the `data.status.kind` post-mutation state, the `meta.uiURL` permalink, the no-rollouts-found / already-terminal / no-active-regression refusal paths, and (if reproducible) the bounded-backoff-timeout warning case are all confirmed working end-to-end (not just unit-tested).

LEARN-02 / DOC-02 / DOC-04 (cross-cutting): every CLI/UX observation that surfaced during the staging exercise lands in CLI-LEARNINGS.md; every API contract observation lands in API-PAPERCUTS.md + Confluence. Phase 4's code-shipping work is now complete; Plan 04-04 handles the end-of-milestone review pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stop-dismiss-finalize-papercuts/04-03-SUMMARY.md` capturing: smoke counts (scenarios run, new papercuts filed, CL-NNN entries added/updated, Confluence updates committed); empirical answers to the four Plan 04-02 open questions; any unexpected behaviors observed (e.g., post-stop Status.Kind unexpected, BuildUIURL path needs correction, polling-budget surprises); an explicit "Phase 4 ready for milestone review (Plan 04-04)" or "Phase 4 needs follow-up plan 04-04-fix" recommendation based on smoke outcomes.
</output>
</content>
</invoke>