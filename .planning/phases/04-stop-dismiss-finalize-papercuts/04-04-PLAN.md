---
phase: 04-stop-dismiss-finalize-papercuts
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - .planning/API-PAPERCUTS.md
  - .planning/CLI-LEARNINGS.md
autonomous: false
requirements:
  - DOC-03
  - LEARN-03
  - DOC-04

must_haves:
  truths:
    - "End-of-milestone review pass: every active entry in `.planning/API-PAPERCUTS.md` has been reviewed for accuracy, source-code cross-references have been verified to still exist (`// PAPERCUT: PC-NNN` annotations point at live code), and every entry has a clear, observable `Removal criteria` field. Entries no longer applicable (e.g., upstream fixed during the milestone) are moved to the `## Resolved` section with a date. (DOC-03)"
    - "End-of-milestone review pass: every active entry in `.planning/CLI-LEARNINGS.md` has been reviewed for accuracy and has the four-field structure (Question / What we did in prototype / What's open for production CLI build / Severity) intact. Entries resolved during the milestone (e.g., a question answered by Plan 04-03 smoke) are moved to the `## Resolved` section with a date. (LEARN-03)"
    - "Confluence page_id 4875452435 has been synced one final time with the post-review state of API-PAPERCUTS.md — the fetch-first pattern is honored. (DOC-04 final pass)"
    - "Both artifacts are explicitly circulated to their target audiences: `.planning/API-PAPERCUTS.md` to the API team (channel/recipient confirmed via checkpoint), and `.planning/CLI-LEARNINGS.md` to the production-CLI-build owner (channel/recipient confirmed via checkpoint). Circulation is the gating condition for SC#5 completion — without it, the artifacts are just internal notes."
    - "The phase is marked complete in STATE.md: progress.completed_phases incremented from 3 to 4; progress.completed_plans incremented to reflect all Phase 4 plans; the Roadmap Summary table for Phase 4 changes from 'Not started' to 'Complete'."
    - "The PROJECT.md 'Validated' section is updated to reflect Phase 4's outcomes — `REQ-STOP-01`, `REQ-DISMISS-01` move from Active to Validated; `REQ-DOC-01` (API papercuts maintained) is checked off; `REQ-LEARN-01` (CLI learnings maintained) is checked off."
  artifacts:
    - path: ".planning/API-PAPERCUTS.md"
      provides: "End-of-milestone reviewed state — every entry's Removal criteria is observable; resolved entries are moved to `## Resolved` with a date; Active Index counter reflects the final count; circulation note recorded in a final section."
      contains: "End-of-milestone review"
    - path: ".planning/CLI-LEARNINGS.md"
      provides: "End-of-milestone reviewed state — every entry has the 4-field LEARN-01 structure; resolved entries are moved to `## Resolved` with a date; Active Index counter reflects the final count; circulation note recorded in a final section."
      contains: "End-of-milestone review"
  key_links:
    - from: ".planning/API-PAPERCUTS.md (end-of-milestone)"
      to: "Confluence page_id 4875452435 (final sync)"
      via: "fetch-first update per memory feedback_confluence_fetch_first.md"
      pattern: "4875452435"
    - from: "STATE.md (milestone completion)"
      to: ".planning/ROADMAP.md (Phase 4 marked complete)"
      via: "progress.completed_phases incremented; checkbox `- [x]` marked for Phase 4"
      pattern: "completed_phases: 4"
---

<objective>
Close out the milestone with the end-of-milestone review pass mandated by ROADMAP.md Phase 4 SC#5. This is the gating condition for Phase 4 (and the entire milestone) being marked complete:

1. **DOC-03:** review every entry in `.planning/API-PAPERCUTS.md` for accuracy; verify each `// PAPERCUT: PC-NNN` source-code annotation still points at live code; ensure every entry has an observable `Removal criteria`; move resolved entries to `## Resolved` with a date; circulate the final doc to the API team.

2. **LEARN-03:** review every entry in `.planning/CLI-LEARNINGS.md` for accuracy; verify the 4-field LEARN-01 structure is intact; move resolved entries to `## Resolved` with a date; circulate the final doc to the production-CLI-build owner.

3. **DOC-04 final pass:** sync the post-review state of API-PAPERCUTS.md to Confluence page_id 4875452435 using the fetch-first pattern.

4. **Milestone bookkeeping:** update STATE.md (mark phase 4 complete) and PROJECT.md (move validated requirements). Plan 04-04 is the LAST PLAN of the milestone.

Purpose: the prototype's PRIMARY DELIVERABLES are the two learnings artifacts (`.planning/API-PAPERCUTS.md` and `.planning/CLI-LEARNINGS.md`) per REQUIREMENTS.md "Learnings Deliverables — first-class outputs of this milestone, not the CLI itself" (line 23-24) and PROJECT.md prototype-first framing (memory `project_prototype_first_framing.md`). The CLI is a vehicle; the docs are the cargo. This plan ensures the cargo arrives in shippable shape — no broken cross-references, no half-finished entries, no unresolved entries that should have been moved.

**This plan is checkpoint-bearing (autonomous: false)** at two points: (a) before the circulation step, the operator confirms the target audience / channel for each artifact (API team contact vs production-CLI-build owner); (b) after the review pass, the operator approves the final doc state before the Confluence sync runs.

Output: end-of-milestone-state API-PAPERCUTS.md + CLI-LEARNINGS.md; final Confluence sync; updated STATE.md + PROJECT.md; milestone marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/API-PAPERCUTS.md
@.planning/CLI-LEARNINGS.md

@.planning/phases/04-stop-dismiss-finalize-papercuts/04-01-PLAN.md
@.planning/phases/04-stop-dismiss-finalize-papercuts/04-02-PLAN.md
@.planning/phases/04-stop-dismiss-finalize-papercuts/04-03-PLAN.md

<interfaces>
<!-- The review-pass criteria; no new code interfaces. -->

API-PAPERCUTS.md entry template (each active entry MUST have these fields):
- Title (one-line)
- Discovered (date + phase milestone)
- API behavior (observed)
- CLI workaround (with `// PAPERCUT: PC-NNN` code-anchor reference, if any)
- What we'd prefer (suggested API fix)
- Status (active or resolved)
- Removal criteria (OBSERVABLE condition under which the entry moves to Resolved — e.g., "API returns rollout ID either in body or header; CLI integration test confirms; the follow-up GET in RolloutsClient.Start is deleted")

CLI-LEARNINGS.md entry template (each active entry MUST have these fields):
- Question (the open CLI/UX design question)
- What we did in prototype (one-paragraph factual recap)
- What's open for production CLI build (one-paragraph forward-looking)
- Severity (low / medium / high)

REQUIREMENTS.md cross-cutting requirements being closed out by this plan:
- DOC-03: end-of-milestone review + circulation of API-PAPERCUTS.md
- LEARN-03: end-of-milestone review + circulation of CLI-LEARNINGS.md (paired with DOC-03)
- DOC-04: final Confluence sync (cross-cutting; previously partial completions in Phases 1-3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review .planning/API-PAPERCUTS.md — verify entries, check source-code anchors, sort active vs resolved, update counters</name>
  <files>.planning/API-PAPERCUTS.md</files>

  <read_first>
    - .planning/API-PAPERCUTS.md (FULL current file; the review pass touches every entry — this is the load-bearing read)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md (any final empirical findings from Plan 04-03 smoke that should reshape entries — e.g., PC-007's `Removal criteria` may need adjustment based on the measured eventual-consistency window)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-03-SUMMARY.md (Plan 04-03's outcomes; identifies any papercuts that were RESOLVED during Phase 4 — e.g., if PC-019 was fixed upstream during the milestone, it moves to Resolved)
  </read_first>

  <action>
    A four-step review pass on `.planning/API-PAPERCUTS.md`:

    1. **Verify every entry has all 7 template fields.** Iterate through `## Entries` section. For each `### PC-NNN` block, verify presence of: Title (in the heading), Discovered (with date AND phase milestone), API behavior, CLI workaround, What we'd prefer, Status, Removal criteria. If any field is missing, fill it in based on the entry's existing prose. If a field is genuinely unknown (e.g., a workaround was never written), leave it explicit with "(no workaround — surfaced for awareness only)" rather than leaving the field absent. Run a structural check: `grep -c '^### PC-' .planning/API-PAPERCUTS.md` should equal `grep -c '^**Title:**' .planning/API-PAPERCUTS.md` (modulo entries that put the title in the heading itself rather than a separate Title field — verify by inspection).

    2. **Verify `// PAPERCUT: PC-NNN` source-code anchors still exist.** For each entry whose `CLI workaround` field claims a source-code annotation, run `grep -rn "PAPERCUT: PC-NNN" /Users/alex/code/launchdarkly/ldcli/ --include='*.go'` (substituting the anchor). If the grep returns ZERO matches AND the entry claims an annotation, two cases:
       - **The annotation was deleted accidentally:** restore it at the workaround site (read the entry's "CLI workaround" prose to locate the file/function).
       - **The workaround was removed legitimately** (e.g., refactored away): update the entry's "Status" to "resolved" with a date and reason, and move to `## Resolved` (step 3).
       If the grep returns matches, the annotation is live — no action needed.

    3. **Sort: move resolved entries to `## Resolved`.** For every active entry, evaluate against its `Removal criteria` field — does the criteria hold TODAY? If yes, move the entire entry block from `## Entries` to `## Resolved` (the section at the bottom of the file — currently empty per Plan 03-02). Prefix the moved entry's title with the resolution date: e.g., "### PC-019 - <title> (Resolved 2026-05-XX)". Add a one-line `**Resolved:** YYYY-MM-DD — <reason>` field above `**Status:**`. Update `**Status:** resolved` (not "active"). Remove the row from the `## Active Index` table; add a row to `## Resolved Index` (create the section if absent — mirror Active Index structure but use the resolution date column instead of Discovered).

    4. **Update counters at the top of the file:**
       - `Last updated:` → today's YYYY-MM-DD.
       - `Active count:` → the actual count of `### PC-` entries in `## Entries` after step 3.
       - `Resolved count:` → the count in `## Resolved` after step 3.
       - Add a NEW final-section line under the top header: `End-of-milestone review completed: YYYY-MM-DD. This doc is now the milestone's API-team-facing deliverable per DOC-03.`

    Do NOT add new papercut entries during this task — Plan 04-03 already handled any new findings from the smoke run. Task 1 is review and reorganization only.

    Commit message convention: `docs(04-04): API-PAPERCUTS.md end-of-milestone review — N active, M resolved`.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli && grep -E '^Active count: ' .planning/API-PAPERCUTS.md | head -1 && grep -E '^Resolved count: ' .planning/API-PAPERCUTS.md | head -1 && grep -E '^Last updated: ' .planning/API-PAPERCUTS.md | head -1 && grep -q 'End-of-milestone review completed' .planning/API-PAPERCUTS.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/API-PAPERCUTS.md` `Last updated:` field reflects the current YYYY-MM-DD date.
    - `.planning/API-PAPERCUTS.md` `Active count:` integer equals `grep -c '^### PC-' .planning/API-PAPERCUTS.md` MINUS the count of resolved entries that physically remain in `## Entries` block (verify via inspection — the counter is consistent with the actual content).
    - `.planning/API-PAPERCUTS.md` `Resolved count:` integer is non-negative and equals the count of entries in `## Resolved`.
    - Every active entry has all 7 template fields populated OR explicitly marked with "(no workaround)" / "(unknown)" placeholders where genuinely unfillable.
    - Every `// PAPERCUT: PC-NNN` claim in an entry's "CLI workaround" field corresponds to at least one live grep match in `/Users/alex/code/launchdarkly/ldcli/` `.go` files (verify by spot-checking 3-5 anchors).
    - The literal string `"End-of-milestone review completed"` appears at the top of the file.
    - If any entry was moved to `## Resolved`: each moved entry has a `**Resolved:** YYYY-MM-DD — <reason>` line and its title is suffixed with `(Resolved YYYY-MM-DD)`.
    - The `## Active Index` table at the top of the file has rows matching the `## Entries` section (no orphan rows; no missing rows).
    - `git diff .planning/API-PAPERCUTS.md` shows only review-related changes (counters, structural reorganization, field-filling); no NEW papercut entries are added in this task.
  </acceptance_criteria>

  <done>API-PAPERCUTS.md is in end-of-milestone shippable shape — every active entry has all 7 fields, every source-code anchor is verified live, resolved entries are sorted into `## Resolved` with dates, counters are accurate, and the end-of-milestone review marker is at the top.</done>
</task>

<task type="auto">
  <name>Task 2: Review .planning/CLI-LEARNINGS.md — verify entries, sort active vs resolved, update counters</name>
  <files>.planning/CLI-LEARNINGS.md</files>

  <read_first>
    - .planning/CLI-LEARNINGS.md (FULL current file; review pass touches every entry)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-SMOKE.md (any final findings that resolve open CL-NNN questions — e.g., CL-005 (watch-shaped use cases) may have been answered empirically by the agent's polling behavior during smoke)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-03-SUMMARY.md (Plan 04-03's empirical answers to Plan 04-02's open questions; these may RESOLVE existing CL-NNN entries or convert them from "open question" to "documented decision")
    - REQUIREMENTS.md lines 32-36 (LEARN-01..03 — the structural template)
  </read_first>

  <action>
    Mirror Task 1's structure for `.planning/CLI-LEARNINGS.md`:

    1. **Verify every entry has all 4 LEARN-01 template fields.** Iterate `## Entries`. For each `### CL-NNN` block, verify presence of: Question, What we did in prototype, What's open for production CLI build, Severity. Severity vocabulary: `low` / `medium` / `high`. If any field is missing, fill it in from the entry's prose; if genuinely empty, mark with "(none — entry is informational)" rather than leaving absent.

    2. **Identify resolved entries.** A CLI-LEARNING is "resolved" when the production CLI build no longer needs to decide on it — typically because: (a) the prototype's choice was empirically validated by a smoke (e.g., the polling budget worked correctly), (b) an upstream API change made the question moot (e.g., PC-019 resolution moots CL-009), (c) the question was answered by an explicit decision committed elsewhere. For each candidate-resolved entry: move it to `## Resolved` with a `**Resolved:** YYYY-MM-DD — <reason>` line; update Severity if it changed; suffix the title with `(Resolved YYYY-MM-DD)`. Add the corresponding `## Resolved Index` row if not present.

       Likely candidates after Phase 4:
       - CL-005 (watch-shaped use cases) — RESOLVED if Plan 04-03 confirmed the agent's polling re-invocation pattern works; STAYS ACTIVE otherwise.
       - CL-007 (--rollout-id requiring --environment) — STAYS ACTIVE until PC-004 is upstream-fixed.
       - CL-008 (typed Go structs strip wire fields) — STAYS ACTIVE — this is a production-CLI-build decision, not something Phase 4 resolves.
       - Phase 4 may have ADDED CL-013..CL-015 candidates per Plan 04-03 — those stay ACTIVE since they're new questions.

       This step is judgment-based; the implementer drafts the move list, the operator approves in Task 3's checkpoint.

    3. **Update counters at the top:**
       - `Last updated:` → today's YYYY-MM-DD.
       - `Active count:` → actual count of `### CL-` entries in `## Entries` after step 2.
       - `Resolved count:` → count in `## Resolved`.
       - Add a final-section line under the top header: `End-of-milestone review completed: YYYY-MM-DD. This doc is now the milestone's production-CLI-build-facing deliverable per LEARN-03.`

    Commit message convention: `docs(04-04): CLI-LEARNINGS.md end-of-milestone review — N active, M resolved`.

    Do NOT add new CL-NNN entries in this task — Plan 04-03 already handled new findings.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli && grep -E '^Active count: ' .planning/CLI-LEARNINGS.md | head -1 && grep -E '^Resolved count: ' .planning/CLI-LEARNINGS.md | head -1 && grep -E '^Last updated: ' .planning/CLI-LEARNINGS.md | head -1 && grep -q 'End-of-milestone review completed' .planning/CLI-LEARNINGS.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/CLI-LEARNINGS.md` `Last updated:` field reflects the current YYYY-MM-DD date.
    - `.planning/CLI-LEARNINGS.md` `Active count:` integer equals the count of `### CL-` entries in `## Entries`.
    - `.planning/CLI-LEARNINGS.md` `Resolved count:` integer equals the count of entries in `## Resolved`.
    - Every active entry has all 4 LEARN-01 template fields populated.
    - The literal string `"End-of-milestone review completed"` appears at the top of the file.
    - If any entry was moved to `## Resolved`: each has a `**Resolved:** YYYY-MM-DD — <reason>` line and a date-suffixed title.
    - `git diff .planning/CLI-LEARNINGS.md` shows only review-related changes; no NEW CL-NNN entries are added in this task.
  </acceptance_criteria>

  <done>CLI-LEARNINGS.md is in end-of-milestone shippable shape — every active entry has all 4 fields, resolved entries are sorted with dates, counters are accurate, the review marker is at the top.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator review of post-review doc state + circulation channel confirmation</name>
  <files>(no files — checkpoint task; operator reviews the post-Task-1+2 state)</files>

  <what-built>
    Tasks 1 and 2 have produced the reviewed end-of-milestone state of API-PAPERCUTS.md and CLI-LEARNINGS.md — entries fields-completed, source-code anchors verified, resolved entries sorted into `## Resolved` with dates, counters updated, end-of-milestone review markers at the top.

    Before the final Confluence sync (Task 4) and STATE.md/PROJECT.md updates (Task 5) run, the operator must:
    1. **Review the doc state** — open both files, scan for accuracy, confirm the "move to Resolved" decisions are correct (especially for CL-005 — watch-shaped use cases — which is a judgment call), and confirm the counters are right.
    2. **Confirm circulation channels** — name the recipient/channel for API-PAPERCUTS.md (e.g., "Slack #api-team channel" or "specific person") and for CLI-LEARNINGS.md (e.g., "production CLI build's design lead" or "Slack #cli-future"). The implementer does NOT have the standing relationships to choose these channels; the operator names them.
    3. **Confirm the doc state is ready for Confluence sync** — the post-review API-PAPERCUTS.md state is what the implementer will sync to Confluence page_id 4875452435 in Task 4. Any last-minute corrections happen here.
  </what-built>

  <how-to-verify>
    1. **Open and skim `.planning/API-PAPERCUTS.md`:**
       - Top: `Last updated:` matches today's date; `Active count:` and `Resolved count:` sum to the total entries; `End-of-milestone review completed:` line is present.
       - Spot-check 3-4 random entries in `## Entries` — each has all 7 template fields.
       - Scan `## Resolved` — every entry has `**Resolved:** YYYY-MM-DD` and the title is date-suffixed.
       - Confirm the move-to-Resolved decisions: each moved entry's `Removal criteria` is actually met today (operator's judgment).
    2. **Open and skim `.planning/CLI-LEARNINGS.md`:**
       - Top: same counter / date / review-marker checks.
       - Spot-check entries for the 4 LEARN-01 fields.
       - Confirm the move-to-Resolved decisions (CL-005 is the most judgment-heavy — the operator decides whether the agent-polling pattern is "validated as workable" or "still an open question").
    3. **Name the circulation channels:** the operator declares the target audience for each artifact (e.g., "API team: Slack channel #api-foundation; production CLI build: GH issue in launchdarkly/cli repo"). The implementer records the named channels in Plan 04-04 SUMMARY so the milestone retro can verify circulation actually happened.
    4. **Approve or request changes:** the operator either approves the doc state (resume signal: "approved with circulation channels: API → <X>; CLI → <Y>") or requests specific edits before Task 4 runs.
  </how-to-verify>

  <action>
    Pause execution. The implementer presents the post-review state of both `.planning/API-PAPERCUTS.md` and `.planning/CLI-LEARNINGS.md` (after Tasks 1 and 2 ran) to the operator. The operator reviews the docs, approves any move-to-Resolved decisions, names the circulation channels for both artifacts (API team channel + production-CLI-build channel), and either approves or requests last-minute edits. Once the operator approves and names channels, the implementer resumes with Task 4 (Confluence sync). If the operator requests edits, the implementer applies them and re-presents the checkpoint.
  </action>

  <verify>Operator responds with explicit approval text naming both circulation channels; Task 4 cannot start without this.</verify>

  <done>Operator has reviewed the post-Task-1+2 doc state, approved any move-to-Resolved decisions, named the circulation channels for both artifacts, and explicitly authorized Task 4's Confluence sync to proceed.</done>

  <resume-signal>Type "approved with circulation channels: API → <name the channel/recipient>; CLI → <name the channel/recipient>". If you want any last edits to the docs first, list them and the implementer will apply before re-presenting the checkpoint.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Final Confluence sync (DOC-04) — fetch-first update of page_id 4875452435 with post-review API-PAPERCUTS.md state</name>
  <files>(no .planning/ files — this task only touches Confluence)</files>

  <read_first>
    - .planning/API-PAPERCUTS.md (the post-review state from Task 1)
    - memory feedback_confluence_fetch_first.md (the fetch-first pattern — MANDATORY)
    - .planning/phases/03-status-watch/03-02-PLAN.md Task 3 (the most recent example of a Confluence update flow — observe how Phase 3 appended entries rather than blind-overwriting)
  </read_first>

  <action>
    Single Confluence operation using the fetch-first pattern:

    1. **Fetch current Confluence content:** call `mcp__mcp-atlassian__confluence_get_page` with `page_id: 4875452435`. Capture the current body content (let's call it `current_body`). Inspect the current version number (let's call it `current_version`). Phase 3 Plan 03-02 left this at v4 (per 03-02-SUMMARY.md); the version may have advanced if a human edited the page since 2026-05-14.

    2. **Compute the diff between the post-review API-PAPERCUTS.md and the current Confluence content:**
       - For each new contract-shape papercut added during Phases 1-4 that is NOT yet in Confluence (verify by searching `current_body` for the PC-NNN anchor), prepare a new entry in the same formatting style as existing Confluence entries.
       - For each resolved entry moved to `## Resolved` during Task 1, add a "Resolved" note to the corresponding Confluence entry (do NOT delete the Confluence entry — preserve history; just annotate with the resolution date and reason).
       - For each entry whose "What we'd prefer" or "Removal criteria" field changed during Task 1 (entries that were re-reviewed and clarified), update the matching Confluence entry's prose to reflect the latest text.
       - **DO NOT remove any existing Confluence content** — append-and-annotate only.

    3. **Construct the merged body:** start from `current_body`, apply the diff from step 2 (insertions + annotations). The result is `merged_body`.

    4. **Update Confluence:** call `mcp__mcp-atlassian__confluence_update_page` with:
       - `page_id: 4875452435`
       - `title:` (preserve existing title; the title comes from `current_body`'s metadata)
       - `content:` `merged_body`
       - `version:` `current_version` (Confluence's optimistic-concurrency model — if the page was edited since the fetch, the update will fail and the implementer must re-fetch + re-merge)
       - Add a comment in the update describing the change: e.g., "End-of-milestone review (Plan 04-04): added PC-021..PC-NNN, marked PC-019 as resolved-during-milestone, refreshed removal criteria across all active entries."

    5. **Verify the update landed:** call `confluence_get_page` again with `page_id: 4875452435` and confirm the version number incremented (`current_version + 1`) and the new PC-NNN anchors are present in the returned body.

    **Edge case:** if step 4 fails with a version-conflict error (a human edited the page between fetch and update), re-execute steps 1-4 — fetch the latest body, re-compute the diff against the latest content, re-merge. This is the fetch-first pattern's safety property. Do NOT use force-overwrite.

    Record the operations in the Plan 04-04 execution log:
    - Final Confluence version number after Task 4 completes.
    - Number of new entries added.
    - Number of existing entries annotated.
    - Any version-conflict retries that fired.
  </action>

  <verify>
    The verification for this task is operational, not file-based: the implementer must record (in the eventual SUMMARY.md) the final Confluence version number AND the count of operations performed. Since the SDK cannot grep Confluence content automatically, the operator's reviews in Tasks 3 and 5 are the verification path.
    <automated>echo "Confluence sync is operational — verified by implementer's execution log in Plan 04-04 SUMMARY"</automated>
  </verify>

  <acceptance_criteria>
    - `mcp__mcp-atlassian__confluence_get_page` was called BEFORE `confluence_update_page` (verify via the implementer's tool-call log — the get MUST precede the update).
    - The Confluence page version number incremented by 1 (or more, if version-conflict retries occurred) — verify via the post-update `confluence_get_page` response.
    - Every active PC-NNN papercut from `.planning/API-PAPERCUTS.md` whose entry indicates a contract-shape observation is present in the post-update Confluence content.
    - No existing Confluence content was destroyed (the update is append-and-annotate only — verify by comparing the pre-fetch `current_body` to `merged_body`: every prior bullet/heading from `current_body` MUST still appear in `merged_body`).
    - If a version-conflict retry occurred, the retry's outcome is documented in the eventual SUMMARY.md.
  </acceptance_criteria>

  <done>Confluence page_id 4875452435 reflects the post-review API-PAPERCUTS.md state; the fetch-first pattern was honored; no human content was clobbered; the final version number is recorded for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 5: Milestone bookkeeping — update STATE.md + PROJECT.md to reflect Phase 4 completion and milestone close</name>
  <files>.planning/STATE.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>

  <read_first>
    - .planning/STATE.md (FULL file; the `progress` block at the top and the Roadmap Summary table need updates)
    - .planning/PROJECT.md (FULL file; the `Validated` and `Active` sections need updates — REQ-STOP-01 + REQ-DISMISS-01 move from Active to Validated; REQ-DOC-01 + REQ-LEARN-01 check off)
    - .planning/ROADMAP.md (FULL file; the Phase 4 checkbox `- [ ]` → `- [x]`; the Progress table changes "Not started" → "Complete"; add completion date)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-01-SUMMARY.md (after Plan 04-01 ran)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-02-SUMMARY.md (after Plan 04-02 ran)
    - .planning/phases/04-stop-dismiss-finalize-papercuts/04-03-SUMMARY.md (after Plan 04-03 ran)
  </read_first>

  <action>
    Three file updates:

    (a) **`.planning/STATE.md`** — `progress` block update:
        - `progress.completed_phases:` 3 → 4
        - `progress.completed_plans:` increment by 4 (Plans 04-01, 04-02, 04-03, 04-04 all complete after this plan finishes)
        - `progress.percent:` 100 (or recalc as `completed_phases / total_phases * 100`)
        - `last_updated:` today's ISO-8601 timestamp

        Then **Current Position** section:
        - Change "Phase: 03" → "Phase: 04 — COMPLETE YYYY-MM-DD"
        - Change "Status:" line to "Milestone v1.0 complete — all 4 phases shipped; Phase 4 Plan 04 milestone review complete"
        - Update progress bar `███████████` count to reflect total plans complete.

        Roadmap Summary table:
        - Phase 4 row: change "Not started" → "Complete (YYYY-MM-DD)".

        Session Continuity section:
        - Change "Next command:" to "`/gsd-complete-milestone v1.0` — milestone retro" (the natural next step after phase 4 closes).
        - Update "Files to re-read" if relevant.

    (b) **`.planning/PROJECT.md`** — Active → Validated migration:
        Find these requirements in the `### Active` section (currently lines 32-42 per the read at planning time):
        - `REQ-STOP-01` — move to `### Validated` with a "— validated in Phase 4 Plan 01" suffix.
        - `REQ-DISMISS-01` — move to `### Validated` with a "— validated in Phase 4 Plan 02" suffix.
        - `REQ-AGENT-01` (machine-readable output) — confirm via Phase 4 envelope tests; check off with "— validated in Phase 4 (and Phases 1-3)" suffix.
        - `REQ-DOC-01` (maintain API papercuts doc) — check off with "— delivered as the milestone's primary artifact per DOC-01..04, end-of-milestone review completed in Phase 4 Plan 04".
        - `REQ-LEARN-01` (maintain CLI learnings doc) — check off with "— delivered as the milestone's parallel artifact per LEARN-01..03, end-of-milestone review completed in Phase 4 Plan 04".

        DO NOT touch REQ-START-04 (preflight deferred per D-09) or REQ-STATUS-03 (watch removed from project) — those have explicit deferral / removal markers from prior phases and should NOT move to Validated; they may move to Out of Scope in a future milestone retro.

        Key Decisions table:
        - Update each `Outcome` cell for rows that mention "Pending" to reflect the actual phase outcome (e.g., "Maintain `.planning/API-PAPERCUTS.md` as a milestone deliverable" — outcome is now "— shipped (20+ active entries by end-of-milestone; circulated to API team via channel <X>)" using the channel name confirmed in Task 3).

        Evolution section: append a new dated note at the bottom: `*Milestone v1.0 closed: YYYY-MM-DD — see .planning/RETROSPECTIVE.md (if /gsd-complete-milestone has run) for full milestone retro.*`

    (c) **`.planning/ROADMAP.md`** — Phase 4 checkbox + Progress table:
        - Line 28 (Phase 4 bullet): change `- [ ] **Phase 4:` → `- [x] **Phase 4:`.
        - Progress table at the bottom: change Phase 4 row's "Plans Complete" to "4/4" (or however many plans actually shipped — verify by counting `.planning/phases/04-stop-dismiss-finalize-papercuts/04-*-SUMMARY.md` files). Change "Status" from "Not started" to "Complete". Set "Completed" to today's YYYY-MM-DD.
        - Final line of file: update the `*Last updated: ...` line to today's date.

    Commit the three updates as a single commit: `docs(04-04): milestone v1.0 closed — STATE.md / PROJECT.md / ROADMAP.md mark phase 4 complete`.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli && grep -E '^  completed_phases: 4$' .planning/STATE.md && grep -E '^- \[x\] \*\*Phase 4:' .planning/ROADMAP.md && grep -q 'Milestone v1.0 closed' .planning/PROJECT.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/STATE.md` `progress.completed_phases:` equals 4.
    - `.planning/STATE.md` `progress.completed_plans:` matches the actual count of completed Phase 1-4 plans (3 for Phase 1 + 2 for Phase 2 + 2 for Phase 3 + 4 for Phase 4 = 11; or whatever the actual count is — verify by counting SUMMARY.md files).
    - `.planning/STATE.md` Roadmap Summary table Phase 4 row reads "Complete" (or "Complete (YYYY-MM-DD)").
    - `.planning/PROJECT.md` `### Validated` section contains both REQ-STOP-01 and REQ-DISMISS-01 with phase-completion suffixes.
    - `.planning/PROJECT.md` `### Active` section no longer lists REQ-STOP-01 or REQ-DISMISS-01.
    - `.planning/PROJECT.md` Evolution section contains the `Milestone v1.0 closed: YYYY-MM-DD` note.
    - `.planning/ROADMAP.md` Phase 4 checkbox is `[x]`.
    - `.planning/ROADMAP.md` Progress table Phase 4 row's Status is "Complete".
    - All three files commit together as a single `docs(04-04): milestone v1.0 closed` commit (verify via `git log -1 --name-only` after the commit).
  </acceptance_criteria>

  <done>STATE.md / PROJECT.md / ROADMAP.md reflect the closed v1.0 milestone — Phase 4 marked complete, validated requirements migrated, ROADMAP checkbox flipped. The next command is `/gsd-complete-milestone v1.0` for the milestone retro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review pass → committed docs | The implementer rewrites `## Entries` / `## Resolved` sections during the review pass; an over-aggressive review could lose information. Mitigation: Task 1's "do not destroy existing content" rule + Task 3's operator review checkpoint. |
| API-PAPERCUTS.md → Confluence | Final sync writes to a shared Confluence page; concurrent human edits could be clobbered without fetch-first. Mitigation: Task 4 mandates fetch-first per memory feedback_confluence_fetch_first.md. |
| circulation message → external channels | The operator names the circulation channels; the implementer does NOT post messages to external channels — only records the named channels in the Plan 04-04 SUMMARY. The actual circulation (Slack post, email, etc.) is the operator's responsibility post-plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Confluence update (Task 4) | mitigate | Fetch-first pattern enforced (`confluence_get_page` BEFORE `confluence_update_page`); version-conflict retry is explicit; "append-and-annotate only" rule preserves all existing content. |
| T-04-04-02 | Repudiation | move-to-Resolved decisions during review | mitigate | Task 3 operator checkpoint requires explicit approval of every move-to-Resolved decision before the docs are committed. The implementer cannot unilaterally hide papercuts by marking them resolved without operator approval. |
| T-04-04-03 | Information Disclosure | Circulation channel names recorded in Plan 04-04 SUMMARY | accept | Channel names (Slack workspace, GH repo) are operator-controlled and operationally public within LD; not secrets. |
| T-04-04-04 | Tampering | STATE.md / PROJECT.md / ROADMAP.md edits (Task 5) | accept | All three files are tracked in git; the commit is reviewable; any incorrect edits are visible in `git log` and `git diff`. No DoS or destructive action possible at the plan boundary. |
</threat_model>

<verification>
- `.planning/API-PAPERCUTS.md` Active count + Resolved count reflect the post-review state; `End-of-milestone review completed:` marker is at the top.
- `.planning/CLI-LEARNINGS.md` Active count + Resolved count reflect the post-review state; `End-of-milestone review completed:` marker is at the top.
- Confluence page_id 4875452435 version number is at `prior + 1` (or higher if version-conflict retries fired); fetch-first pattern was honored.
- `.planning/STATE.md` `progress.completed_phases: 4`.
- `.planning/ROADMAP.md` Phase 4 checkbox is `[x]`; Progress table Status is "Complete".
- `.planning/PROJECT.md` REQ-STOP-01 / REQ-DISMISS-01 / REQ-DOC-01 / REQ-LEARN-01 have moved to Validated with Phase 4 references.
- The operator's named circulation channels are recorded in the Plan 04-04 SUMMARY for downstream retro reference.
</verification>

<success_criteria>
**Phase 4 SC#5 — end-of-milestone review pass:**
- DOC-03: `.planning/API-PAPERCUTS.md` reviewed, every active entry has a documented removal criterion, every source-code annotation is verified live, resolved entries are sorted with dates, circulation channel to API team is named.
- LEARN-03: `.planning/CLI-LEARNINGS.md` reviewed, every entry has the 4-field structure, resolved entries are sorted with dates, circulation channel to production-CLI-build owner is named.
- DOC-04: Confluence page_id 4875452435 has been synced with the post-review state using the fetch-first pattern.
- Both syncs are GATING conditions for phase complete — without circulation channels named and confluence sync completed, the phase stays open.

**Milestone v1.0 complete:** all four phases shipped; STATE.md / PROJECT.md / ROADMAP.md updated; the next natural command is `/gsd-complete-milestone v1.0` for the milestone retro.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stop-dismiss-finalize-papercuts/04-04-SUMMARY.md` capturing:
- The end-of-milestone review counts for both docs (Active before review / Active after review; Resolved before review / Resolved after review; how many entries moved).
- The final Confluence version number after Task 4.
- The named circulation channels (API team channel, production CLI build channel).
- The STATE.md / PROJECT.md / ROADMAP.md changes summary.
- Any version-conflict retries during Confluence sync.
- An explicit "Milestone v1.0 ready for `/gsd-complete-milestone v1.0` retro" recommendation OR a list of known follow-up items for the retro to surface.
- A pointer to the final commit hash that lands the milestone-close changes.
</output>
</content>
</invoke>