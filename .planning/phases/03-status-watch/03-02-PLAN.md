---
phase: 03-status-watch
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/phases/03-status-watch/03-SMOKE.md
  - .planning/API-PAPERCUTS.md
  - .planning/CLI-LEARNINGS.md
autonomous: false
requirements:
  - STATUS-01
  - STATUS-02
  - STATUS-03
  - LEARN-02
  - DOC-02
  - DOC-04
  - AGENT-01
  - AGENT-02
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "The status command runs end-to-end against real LaunchDarkly staging on at least three scenarios (most-recent + --rollout-id + no-rollouts-found) and the captured envelopes are recorded in 03-SMOKE.md"
    - "Any API gotcha surfaced during the staging exercise lands as a new PC-NNN entry in .planning/API-PAPERCUTS.md AND a corresponding update to Confluence page_id 4875452435 (DOC-02 + DOC-04)"
    - "Any CLI/UX observation surfaced during the staging exercise lands as an updated or new entry in .planning/CLI-LEARNINGS.md (LEARN-02)"
    - "The phase is not complete until both 03-SMOKE.md and the learnings appends are committed"
  artifacts:
    - path: ".planning/phases/03-status-watch/03-SMOKE.md"
      provides: "Real-staging exercise log with command invocations, captured envelopes, and observations"
      contains: "Smoke A"
    - path: ".planning/API-PAPERCUTS.md"
      provides: "Updated active index + any new PC-NNN entries surfaced by Phase 3 staging exercise"
      contains: "Last updated: 2026-05-14"
    - path: ".planning/CLI-LEARNINGS.md"
      provides: "Updated entries reflecting Phase 3 staging observations (LEARN-02 append discipline)"
      contains: "Active Index"
  key_links:
    - from: ".planning/phases/03-status-watch/03-SMOKE.md"
      to: ".planning/API-PAPERCUTS.md"
      via: "Observations section cross-references any new PC-NNN anchors"
      pattern: "PC-0"
    - from: ".planning/phases/03-status-watch/03-SMOKE.md"
      to: ".planning/CLI-LEARNINGS.md"
      via: "Observations section cross-references CL-NNN anchors that were updated or added"
      pattern: "CL-0"
    - from: ".planning/API-PAPERCUTS.md"
      to: "Confluence page_id 4875452435"
      via: "fetch-first update (mcp__mcp-atlassian__confluence_get_page → confluence_update_page) per memory feedback_confluence_fetch_first.md"
      pattern: "4875452435"
---

<objective>
Run the `ldcli flags rollouts-beta status` command against real LaunchDarkly staging across the three core scenarios (most-recent / specific rollout-id / no-rollouts-found), record outcomes in 03-SMOKE.md, and feed any new API papercuts or CLI/UX observations into the two learnings docs (API-PAPERCUTS.md + CLI-LEARNINGS.md) plus the Confluence page.

Purpose: real-staging exercise is required by PROJECT.md's "real-server validation" constraint and is the gating condition for Phase 3 completion per CONTEXT.md `<specifics>` — the prototype's whole point is to surface learnings, not to write speculative tests. Phase 1 + Phase 2 SMOKE.md files are the templates.

Output: an `03-SMOKE.md` log with captured envelopes; any new PC-NNN entries in API-PAPERCUTS.md + a matching Confluence update; any LEARN-02 appends in CLI-LEARNINGS.md.

**This plan is checkpoint-bearing (autonomous: false)** because real-staging exercise requires the operator to have a writer-scoped staging token loaded into `~/secret/ld-staging-token` (or equivalent) and to inspect / confirm captured envelopes before papercuts are filed. The implementer (Claude) executes the commands and drafts the artifacts; the checkpoint pauses for the operator to confirm staging token availability and to review the staging-call envelopes before Confluence updates are committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/API-PAPERCUTS.md
@.planning/CLI-LEARNINGS.md
@.planning/phases/03-status-watch/03-CONTEXT.md
@.planning/phases/03-status-watch/03-01-PLAN.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-SMOKE.md
@.planning/phases/02-start-a-rollout/02-SMOKE.md

<interfaces>
<!-- Reference patterns for SMOKE.md structure, papercut entry format, and Confluence update flow. -->

From .planning/phases/01-list-foundation-first-end-to-end-slice/01-SMOKE.md (template structure):
- Top-level "Environment" section (Base URI, access token sourcing, binary, project, environment)
- "Bugs surfaced and fixed in this gap closure" section if any
- One section per smoke scenario (Smoke A, Smoke B, Smoke C) with: Command, Exit code, Stdout, Stderr, Verdict, Contract observations
- Final "Observations / Follow-ups" section that explicitly cross-references new PC-NNN entries

From .planning/API-PAPERCUTS.md (per-entry template):
- Title: <one-line>
- Discovered: <date> (<phase milestone>)
- API behavior: <observed>
- CLI workaround: <code site if any, with `// PAPERCUT: PC-NNN` annotation>
- What we'd prefer: <suggested API fix>
- Status: active
- Removal criteria: <observable condition under which entry moves to Resolved>

From memory feedback_confluence_fetch_first.md:
- Always fetch current Confluence content via `mcp__mcp-atlassian__confluence_get_page` BEFORE calling `confluence_update_page` so concurrent human edits aren't clobbered.
- Confluence page_id = 4875452435 (anchored in API-PAPERCUTS.md tagline and CONTEXT.md `<canonical_refs>`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm staging credentials and scenario fixtures are available</name>
  <files>(no files — checkpoint task; operator confirms preconditions)</files>

  <action>
    Pause execution. The implementer presents the precondition checklist to the operator (token availability, flag fixtures, Confluence MCP). The operator confirms each precondition by responding with the chosen flag keys and any environment overrides. Once confirmed, the implementer resumes with Task 2.
  </action>

  <what-built>
    Plan 03-01 has shipped a working `./ldcli flags rollouts-beta status` binary. Plan 03-02 cannot proceed automatically — real-staging exercise needs a writer-scoped LaunchDarkly staging access token and a flag-with-rollouts fixture in the `alex-engelberg-dev` project (or equivalent). Phase 1 and Phase 2 SMOKE.md files reference the same staging account; the fixtures listed there (e.g. `ldcli-blitz-2-guarded-rollouts`, `ldcli-blitz-phase2-start`) are candidates for Phase 3 reuse.
  </what-built>

  <how-to-verify>
    1. **Token availability:** confirm a staging access token is loaded (typical pattern: `~/secret/ld-staging-token` or env var). Operator confirms via "token loaded" or pastes it into config via `ldcli config --set access-token=...`. Do NOT paste the token into the chat or any artifact.
    2. **At least one flag with rollouts exists on `alex-engelberg-dev` / `test`:** operator confirms `ldcli flags rollouts-beta list --base-uri https://ld-stg.launchdarkly.com --project alex-engelberg-dev --flag <candidate> --environment test --output json` returns at least one item. The Phase 1 fixtures `ldcli-blitz-2-guarded-rollouts` and `ldcli-blitz-3-progressive-rollouts` are pre-existing candidates per 01-SMOKE.md.
    3. **At least one flag with ZERO rollouts is reachable:** Phase 1 used `ldcli-blitz-1-no-rollout`. Operator confirms it still exists (or names an alternative).
    4. **Confluence MCP available:** operator confirms `mcp__mcp-atlassian__confluence_get_page` and `confluence_update_page` are working in this session (the implementer will exercise them in Task 3).
  </how-to-verify>

  <verify>Operator responds to the resume-signal prompt with chosen flag keys; Task 2 cannot start until this checkpoint resolves.</verify>

  <done>Operator has confirmed all four preconditions and named the flag keys for each smoke scenario.</done>

  <resume-signal>Type "approved" with the chosen flag keys for: (a) most-recent / progressive scenario, (b) most-recent / regressed scenario if available, (c) --rollout-id / specific lookup, (d) no-rollouts-found. If any scenario is not available, name what IS available and the implementer will adapt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the status command against real staging and capture envelopes in 03-SMOKE.md</name>
  <files>.planning/phases/03-status-watch/03-SMOKE.md</files>

  <read_first>
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-SMOKE.md (template; Smoke A/B/C structure; tone of Verdict + Contract observations sections; "Status-mapping coverage" mini-table; "Observations / follow-ups" final section)
    - .planning/phases/02-start-a-rollout/02-SMOKE.md (Phase 2 follow-up template; "New API Contract Discoveries" subsection format with PC-NNN cross-references; "Action Items" tail)
    - .planning/phases/03-status-watch/03-CONTEXT.md `<specifics>` "API papercuts (primary)" list — expected new papercuts during Phase 3 work (empty-list response shape, Get-vs-List eventual consistency, status-enum sub-condition discriminators, metric-configuration status enum)
    - .planning/API-PAPERCUTS.md (full active index — PC-004, PC-005, PC-006, PC-014, PC-015 are the most relevant priors; new entries continue the PC-NNN numbering — verify next free anchor by reading the index table)
  </read_first>

  <action>
    Execute at minimum these four scenarios against real staging (use the flag keys confirmed in Task 1; substitute as the operator names alternatives). Use the `--base-uri https://ld-stg.launchdarkly.com` flag explicitly to make the SMOKE.md log self-contained. Use `--output json` for all envelope captures (plaintext is verified separately in unit tests; the staging exercise's job is envelope verification + papercut surfacing).

    1. **Smoke A — most-recent / happy path:**
       Run `./ldcli flags rollouts-beta status --base-uri https://ld-stg.launchdarkly.com --access-token [REDACTED] --project alex-engelberg-dev --flag <flag-with-rollouts> --output json`. Capture: exit code, full stdout JSON envelope, full stderr. Confirm: `schemaVersion == "rollouts.v1beta1"`, `kind == "Rollout"`, `data.id` non-empty, `data.status` block has `status`/`kind`/`label`.

    2. **Smoke B — specific rollout via --rollout-id:** take the `data.id` returned by Smoke A and re-fetch it explicitly with the same command but adding `--rollout-id <id-from-smoke-A> --environment test`. Capture and compare: should be the SAME rollout. Verify: `data.id` matches Smoke A's `data.id`. **Contract observation to record:** does `Get` return identical content to `List → items[0]`? If they diverge (eventual-consistency window), that's a new papercut.

    3. **Smoke C — no rollouts found:** run the Smoke A command but substitute `--flag <flag-without-rollouts>`. Capture: exit code, full stdout JSON envelope, full stderr. Confirm: exit 1, envelope `kind == "Error"`, `error.code == "no_rollouts_found"`, envelope on stdout (NOT stderr) per AGENT-04 / D-07.

    4. **Smoke D — validation error (--rollout-id without --environment):** run the Smoke A command but ADD `--rollout-id <any-uuid>` WITHOUT `--environment`. Capture: exit code, full stdout JSON envelope, full stderr. Confirm: exit 1, envelope `error.code == "bad_request"`, envelope on stdout, NO actual API call was made (verify via process-level timing — should return immediately; the operator may also tcpdump if rigor is wanted).

    5. **Optional Smoke E — plaintext output sanity:** run Smoke A without `--output json` (force a TTY via running in a terminal pane) and confirm the sectioned plaintext layout renders the `Stages:` / `Metrics:` / `Events:` blocks. Capture the first ~20 lines of plaintext output. This is sanity, not contract — skip if Task 1 reported plaintext was already verified.

    Write `03-SMOKE.md` mirroring the Phase 1 / Phase 2 SMOKE.md format. Sections:
    - `# Phase 3 — Real-Staging Smoke Tests (03-01)` title
    - `Last updated: 2026-05-14`
    - `## Environment` (Base URI, token sourcing, binary, project, environment, flag fixtures used)
    - One subsection per Smoke (A/B/C/D and optional E): Command (REDACT THE TOKEN — replace the literal token bytes with `[REDACTED]`), Exit code, Stdout (full envelope), Stderr, Verdict, Contract observations
    - `## Status-mapping coverage from this smoke run` mini-table (raw status / derived kind / exercised) — mirror 01-SMOKE.md table
    - `## Observations / Follow-ups` final section. For each observation that looks like an API gap, draft a candidate `PC-NNN` line and tag it `→ Task 3` so the next task picks it up. For each observation that's a CLI/UX question (envelope shape question, agent ergonomics, deferred-decision surprise), tag it `→ CLI-LEARNINGS.md CL-NNN`.

    **Token redaction discipline:** every captured Command block in SMOKE.md MUST have the literal access token replaced with `[REDACTED]` BEFORE the file is committed. If the token leaks into stderr or stdout (it should not — retryablehttp's `Logger: nil` per Phase 1 internal/rollouts/client.go line 84 prevents this), redact it from the captured outputs as well.
  </action>

  <verify>
    <automated>test -f .planning/phases/03-status-watch/03-SMOKE.md &amp;&amp; grep -c '^## Smoke' .planning/phases/03-status-watch/03-SMOKE.md | awk '$1 &gt;= 4 { exit 0 } { exit 1 }' &amp;&amp; ! grep -E '[a-f0-9]{24,}' .planning/phases/03-status-watch/03-SMOKE.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/03-status-watch/03-SMOKE.md` exists
    - File contains at least 4 `## Smoke ` section headings (A through D; E optional). Verify: `grep -c '^## Smoke ' .planning/phases/03-status-watch/03-SMOKE.md` returns 4 or more.
    - File contains an `## Environment` section (mirroring 01-SMOKE.md / 02-SMOKE.md)
    - File contains an `## Observations / Follow-ups` section (or `## Observations` — executor discretion on punctuation)
    - **No access token is present in the file.** Verify: `grep -E '[a-f0-9]{24,}' .planning/phases/03-status-watch/03-SMOKE.md` returns NOTHING (no 24+-hex-char strings — staging tokens typically run 40+ chars). If the test asserts no 24-hex-char strings exist, the file passes redaction. Implementer is responsible for replacing token literals with `[REDACTED]` before committing.
    - At least one Smoke verdict explicitly cites `data.status.kind` and `data.status.label` (proves AGENT-04 / D-02 envelope shape was inspected)
    - The "no rollouts found" smoke records exit code 1 and `error.code: "no_rollouts_found"` in the captured stdout (proves the D-09 wiring works end-to-end)
    - The validation-error smoke records exit code 1 and that the envelope appeared on stdout (NOT stderr)
  </acceptance_criteria>

  <done>03-SMOKE.md captures at least four real-staging command invocations (happy / --rollout-id / no-rollouts-found / validation-error), tokens are redacted, and the Observations section tags candidate papercuts and CLI-LEARNINGS appends for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Append new papercuts + CLI-UX observations to API-PAPERCUTS.md + CLI-LEARNINGS.md + Confluence</name>
  <files>.planning/API-PAPERCUTS.md, .planning/CLI-LEARNINGS.md</files>

  <read_first>
    - .planning/phases/03-status-watch/03-SMOKE.md (the file produced by Task 2 — Observations / Follow-ups section is the source of truth for what to append)
    - .planning/API-PAPERCUTS.md (full file; the active index table at the top must be updated, and new entries appended to `## Entries`; next free PC-NNN anchor is determined by reading the index table — at time of writing PC-018 is the highest)
    - .planning/CLI-LEARNINGS.md (the skeleton produced by Plan 03-01 Task 1; new entries continue the CL-NNN numbering from CL-007)
    - memory feedback_confluence_fetch_first.md (Confluence fetch-first pattern — always `confluence_get_page` BEFORE `confluence_update_page` so concurrent human edits aren't clobbered)
    - CONTEXT.md `<canonical_refs>` "Papercuts" subsection — Confluence page_id 4875452435 is the contract-shape observation log
  </read_first>

  <action>
    For each Observation in 03-SMOKE.md tagged `→ Task 3` or `→ CLI-LEARNINGS.md`:

    1. **API papercut path (DOC-02 + DOC-04):**
       - Determine the next free `PC-NNN` anchor by scanning `.planning/API-PAPERCUTS.md`'s Active Index table — pick the next sequential integer after the highest existing anchor.
       - Append a new row to the Active Index table: `| PC-NNN | <one-line> | 2026-05-14 | status |`.
       - Append a new `### PC-NNN - <one-line title>` entry to `## Entries` using the per-entry template (Title / Discovered / API behavior / CLI workaround / What we'd prefer / Status / Removal criteria).
       - **If the papercut captures a contract-shape observation** (confusing field name, missing data on response, forced consumer workaround, inconsistency with the rest of the LD API surface) → update Confluence page_id 4875452435 (DOC-04):
         - First call `mcp__mcp-atlassian__confluence_get_page` with `page_id: 4875452435` to fetch current content.
         - Append the new papercut entry to the appropriate section of the page (mirror the formatting of existing entries — likely a Markdown / Confluence-storage-format bullet under a "Papercuts" or similar heading).
         - Call `mcp__mcp-atlassian__confluence_update_page` with the merged content (existing + new entry). Per memory feedback_confluence_fetch_first.md: DO NOT blind-overwrite — the fetched body is the basis for the update.
       - Increment the `Active count: N` line at the top of API-PAPERCUTS.md to match the new total.
       - Update the `Last updated: 2026-05-14` line at the top of API-PAPERCUTS.md.

    2. **CLI-LEARNINGS path (LEARN-02):**
       - If the observation extends an existing CL-NNN entry (e.g. Phase 3's actual experience with "most recent" semantics confirms or refutes the prototype assumption captured in CL-006), update that entry's `**What we did in prototype**` field with a one-line append noting the empirical finding.
       - If the observation is a NEW CLI/UX topic not covered by CL-001..CL-007, append a new `### CL-NNN — <one-line>` entry with the structured template (Question / What we did in prototype / What's open for production CLI build / Severity).
       - Update the `Active count: N` line at the top of CLI-LEARNINGS.md.
       - Update the `Last updated: 2026-05-14` line.

    3. **If 03-SMOKE.md's Observations section is empty** (the smoke run surfaced no new papercuts and no new CLI-UX questions — entirely possible for a prototype that exercises well-trodden Phase 1/2 API paths):
       - Record a single LEARN-02 append on CLI-LEARNINGS.md noting that the Phase 3 smoke produced no new observations beyond CL-001..CL-007 (a finding in itself — the prototype-era CLI shape held up under Phase 3 exercise).
       - Skip Confluence update (no contract-shape observation to log).
       - Skip API-PAPERCUTS.md append (no new papercut to log).
  </action>

  <verify>
    <automated>grep -E '^Active count: ' .planning/API-PAPERCUTS.md | head -1 &amp;&amp; grep -E '^Active count: ' .planning/CLI-LEARNINGS.md | head -1 &amp;&amp; grep -E '^Last updated: 2026-05-14' .planning/CLI-LEARNINGS.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/API-PAPERCUTS.md` `Last updated:` field is `2026-05-14`
    - `.planning/API-PAPERCUTS.md` `Active count:` value matches the number of rows in the Active Index table (verify by inspection — if 0 new papercuts surfaced, count stays at 18; if N new papercuts, count is 18 + N)
    - `.planning/CLI-LEARNINGS.md` `Last updated:` field is `2026-05-14`
    - `.planning/CLI-LEARNINGS.md` `Active count:` value matches the number of `### CL-` entries in the file
    - For every new `PC-NNN` entry added to API-PAPERCUTS.md: the entry has all 7 template fields (Title, Discovered, API behavior, CLI workaround, What we'd prefer, Status, Removal criteria), AND an Active Index row exists with the same anchor
    - For every contract-shape papercut added: the Confluence update flow followed the fetch-first pattern (verify the implementer's execution log shows `confluence_get_page` called BEFORE `confluence_update_page` for page_id 4875452435)
    - For every new CL-NNN or updated CL-NNN entry: it has the 4-field LEARN-01 structure (Question / What we did in prototype / What's open for production CLI build / Severity)
    - 03-SMOKE.md's Observations section is consistent with the appends — every `→ Task 3` tag in SMOKE.md has a corresponding PC-NNN entry; every `→ CLI-LEARNINGS.md` tag has a corresponding CL-NNN entry or update
  </acceptance_criteria>

  <done>Every observation from 03-SMOKE.md has been routed to its destination (API-PAPERCUTS.md + Confluence for contract gaps; CLI-LEARNINGS.md for CLI/UX questions); if no observations surfaced, that finding is itself recorded in CLI-LEARNINGS.md as a Phase 3 validation note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ldcli (real staging) | Live access token crosses the CLI → LD API boundary; staging environment data may have business sensitivity |
| ldcli → 03-SMOKE.md | Captured stdout / stderr may contain tokens, env values, or sensitive flag content — operator must redact before commit |
| 03-SMOKE.md / API-PAPERCUTS.md → Confluence | New papercut content propagates to Confluence; concurrent human edits could be clobbered without fetch-first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03P2-01 | Information Disclosure | 03-SMOKE.md captures | mitigate | Task 2 acceptance criteria includes a grep-based redaction check (no 24+-hex-char strings) before commit. Token sourcing pattern (`~/secret/ld-staging-token`) keeps the literal token out of shell history. Captured envelopes contain rollout IDs, flag keys, and environment names — these are operator-internal, not cross-tenant secrets; recording them is acceptable for the prototype framing per CONTEXT.md. |
| T-03P2-02 | Tampering | Confluence update | mitigate | Task 3 explicitly mandates fetch-first (`confluence_get_page` BEFORE `confluence_update_page`) per memory feedback_confluence_fetch_first.md. Blind-overwrite is the documented anti-pattern. |
| T-03P2-03 | Information Disclosure | --access-token in argv | accept | Operator-supplied flag value; the CLI process itself is single-tenant on the operator's machine. argv leakage to other local processes is a host-level concern, not a Phase 3 mitigation surface. |
</threat_model>

<verification>
- `.planning/phases/03-status-watch/03-SMOKE.md` exists with at least 4 `## Smoke ` headings, an Environment section, and an Observations section
- Token redaction holds: `grep -E '[a-f0-9]{24,}' .planning/phases/03-status-watch/03-SMOKE.md` returns nothing
- `.planning/API-PAPERCUTS.md` and `.planning/CLI-LEARNINGS.md` have consistent `Last updated:` and `Active count:` fields
- Every observation tagged `→ Task 3` in 03-SMOKE.md has a matching PC-NNN entry in API-PAPERCUTS.md; every observation tagged `→ CLI-LEARNINGS.md` has a matching CL-NNN entry or update
</verification>

<success_criteria>
Phase 3 SC#1 + SC#2: validated against real LaunchDarkly staging via 03-SMOKE.md — the JSON envelope shape, the `data.status` block (kind/label/raw), and the empty-list error path are confirmed working end-to-end (not just unit-tested).

Phase 3 SC#3 (LEARN-01 / LEARN-02 / DOC-02 / DOC-04): every CLI/UX observation that surfaced during the staging exercise lands in CLI-LEARNINGS.md; every API contract observation lands in API-PAPERCUTS.md + Confluence. The phase is now complete per CONTEXT.md `<specifics>` "Real-staging exercise is required per PROJECT.md constraint."
</success_criteria>

<output>
After completion, create `.planning/phases/03-status-watch/03-02-SUMMARY.md` capturing: the staging exercise outcomes (counts: smokes run, new papercuts filed, CL-NNN entries added/updated, Confluence updates committed), any anomalies encountered (eventual-consistency windows, unexpected status enum values, etc.), and an explicit "Phase 3 ready to mark complete" or "Phase 3 needs follow-up plan 03-03" recommendation based on what Smoke A/B/C/D revealed.
</output>
