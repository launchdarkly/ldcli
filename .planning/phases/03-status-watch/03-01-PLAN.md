---
phase: 03-status-watch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/flags/rollouts/status.go
  - cmd/flags/rollouts/status_test.go
  - cmd/flags/rollouts/plaintext.go
  - cmd/flags/rollouts/rollouts.go
  - cmd/cliflags/flags.go
  - internal/rollouts/errors.go
  - .planning/CLI-LEARNINGS.md
autonomous: true
requirements:
  - STATUS-01
  - STATUS-02
  - STATUS-03
  - STATUS-04
  - LEARN-01
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "D-02: Operator can run `ldcli flags rollouts-beta status --flag <key>` and receive the most-recent rollout's full state for that flag — the command registers EXACTLY the flags `--flag`, `--environment`, `--rollout-id` (plus inherited `--project`) and rejects `--detailed`/`--short`/`--state`/`--limit`"
    - "D-02: Operator can run `ldcli flags rollouts-beta status --flag <key> --rollout-id <id> --environment <env>` and receive the specified rollout's full state"
    - "D-03: When `--rollout-id` is set, `--environment` is also required; the CLI rejects the invocation with a CLI-side validation error BEFORE any API call is made (papers over PC-004 surfacing — GET-by-ID requires environmentKey in the URL path)"
    - "D-09: When no rollouts exist for the flag (and `--rollout-id` is not provided), the CLI emits an error envelope with `error.code == \"no_rollouts_found\"` on stdout in JSON mode and exits 1 — exactly ONE new error-code constant is added: `ErrCodeNoRolloutsFound = \"no_rollouts_found\"`, all other error mapping comes from Phase 1's mapAPIError/mapTransportError"
    - "D-05: JSON-mode output is the existing `rollouts.v1beta1` envelope produced verbatim via `rollouts.NewRolloutEnvelope` — `kind: \"Rollout\"`, `data` carrying the full Rollout including the nested `status` block (status / kind / label), `meta` carrying only `fetchedAt`"
    - "D-07: Plaintext output is sectioned (Overview / Stages / Metrics / Events) and is the default in a TTY when `--output` is not json"
    - "D-13: `.planning/CLI-LEARNINGS.md` exists at the repo root planning/ directory with an Active Index anchor table and seven seeded topic entries (CL-001..CL-007)"
    - "D-01: `--watch` is out of scope for this phase and the project — no watch-related code, flags, tests, or polling helpers are introduced; STATUS-05..09 are struck and polling is the agent's responsibility"
    - "D-04: Most-recent semantics are honored verbatim from Phase 1's `Client.List` sort (`createdAt DESC, ID ASC`) — no Phase-3-specific sort is added; `Limit: 1` + `items[0]` returns the answer"
    - "D-06: No new top-level `state` field is added to the envelope — `status.kind` (active/regressed/reverted/paused/completed) plus the raw upstream `status` value both live inside `data.status` per Phase 1 D-02; ROADMAP SC#2 vocabulary (`running/paused/succeeded/failed/regression-detected`) is reconciled to `status.kind` without a new field"
    - "D-08: No `--detailed`/`--short` toggle in v1 — status output is single-rollout full detail by default; the user explicitly asked for the rollout, so they want the detail"
    - "D-10: Any error path exits 1 — no new exit codes are introduced; honors Phase 1 D-01 (the watch-timeout candidate disappears with `--watch`)"
    - "D-11: Do NOT lift Phase 1 D-03's structured `reason` in this phase — plaintext renderer consumes `status.label` only; the question \"did agents struggle parsing label?\" lands in CLI-LEARNINGS.md (CL-003) instead"
    - "D-12: No new methods on `internal/rollouts/Client` — Phase 3 reuses existing `Get(ctx, token, baseURI, projKey, envKey, rolloutID)` and `List(ctx, token, baseURI, projKey, flagKey, opts)` only; Phase 1 D-08 (incremental Client growth) honored — Phase 3 grows the surface by zero methods"
  artifacts:
    - path: "cmd/flags/rollouts/status.go"
      provides: "NewStatusCmd constructor + statusRunE + resolveRollout (Get path / List-fallback path) + emitStatusSuccess + emitStatusError"
      contains: "func NewStatusCmd(client rollouts.Client) *cobra.Command"
    - path: "cmd/flags/rollouts/status_test.go"
      provides: "Table tests for happy / edge cases against rollouts.MockClient"
      contains: "func TestStatus"
    - path: "cmd/flags/rollouts/plaintext.go"
      provides: "RenderRolloutStatusPlaintext(*rollouts.Rollout) string for sectioned blocks"
      contains: "func RenderRolloutStatusPlaintext"
    - path: "cmd/flags/rollouts/rollouts.go"
      provides: "AddCommand wiring so `flags rollouts-beta status` is discoverable"
      contains: "cmd.AddCommand(NewStatusCmd(client))"
    - path: "cmd/cliflags/flags.go"
      provides: "RolloutIdFlag constant + RolloutIdFlagDescription help text"
      contains: "RolloutIdFlag"
    - path: "internal/rollouts/errors.go"
      provides: "ErrCodeNoRolloutsFound error-code constant for the empty-list case"
      contains: "ErrCodeNoRolloutsFound"
    - path: ".planning/CLI-LEARNINGS.md"
      provides: "Prototype-era CLI/UX learnings doc; mirrors API-PAPERCUTS.md structure; seeded with 7 topics from CONTEXT D-13"
      contains: "Active Index"
  key_links:
    - from: "cmd/flags/rollouts/status.go"
      to: "internal/rollouts.Client.Get"
      via: "client.Get(ctx, token, baseURI, projKey, envKey, rolloutID) when --rollout-id is provided"
      pattern: "client\\.Get\\("
    - from: "cmd/flags/rollouts/status.go"
      to: "internal/rollouts.Client.List"
      via: "client.List(ctx, ..., ListOpts{Environment, Limit:1}) then items[0] when --rollout-id is absent"
      pattern: "client\\.List\\("
    - from: "cmd/flags/rollouts/status.go"
      to: "internal/rollouts.NewRolloutEnvelope"
      via: "envelope construction for success path"
      pattern: "NewRolloutEnvelope"
    - from: "cmd/flags/rollouts/status.go"
      to: "internal/rollouts.NewErrorEnvelope"
      via: "JSON-mode error envelope on stdout (Phase 1 D-07)"
      pattern: "NewErrorEnvelope"
    - from: "cmd/flags/rollouts/rollouts.go"
      to: "cmd/flags/rollouts/status.go:NewStatusCmd"
      via: "cmd.AddCommand(NewStatusCmd(client))"
      pattern: "AddCommand\\(NewStatusCmd"
---

<objective>
Ship the `ldcli flags rollouts-beta status` command end-to-end as a thin vertical slice — CLI flag wiring, command body, plaintext renderer, JSON envelope reuse, error path, tests, and the CLI-LEARNINGS.md skeleton — so an operator (human or agent) can inspect a rollout in one invocation against real staging.

Purpose: deliver Phase 3 SC#1 + SC#2 + SC#3 in a single autonomous plan. This is a prototype demo deliverable due end-of-day; ship working code that surfaces API papercuts and CLI-UX learnings rather than over-engineering.

Output: a working `status` subcommand reusing the existing `Client.Get` / `Client.List` interface (zero new client methods per D-12), a sectioned plaintext renderer, the JSON envelope verbatim from Phase 1/2 (D-05), one new error-code constant (D-09), one new CLI flag constant, and `.planning/CLI-LEARNINGS.md` seeded per D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/API-PAPERCUTS.md

@.planning/phases/03-status-watch/03-CONTEXT.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-SMOKE.md
@.planning/phases/02-start-a-rollout/02-CONTEXT.md

@cmd/flags/rollouts/list.go
@cmd/flags/rollouts/start.go
@cmd/flags/rollouts/plaintext.go
@cmd/flags/rollouts/rollouts.go
@cmd/flags/rollouts/flags.go
@cmd/flags/rollouts/list_test.go
@cmd/cliflags/flags.go
@internal/rollouts/client.go
@internal/rollouts/models.go
@internal/rollouts/errors.go
@internal/rollouts/envelope.go
@internal/rollouts/status_mapping.go
@internal/rollouts/mock_client.go

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase. -->
<!-- Use these directly; no codebase exploration needed beyond what `read_first` requires. -->

From internal/rollouts/client.go:
- type Client interface {
      List(ctx context.Context, accessToken, baseURI, projKey, flagKey string, opts ListOpts) (*RolloutList, error)
      Get(ctx context.Context, accessToken, baseURI, projKey, envKey, rolloutID string) (*Rollout, error)
      Start(ctx context.Context, accessToken, baseURI, projKey, flagKey, envKey string, instr StartInstruction) (*Rollout, error)
  }
- type ListOpts struct { Environment string; Limit int; All bool }

From internal/rollouts/models.go:
- type Rollout struct { ID, FlagKey, Kind, EnvironmentID, EnvironmentKey, OriginalVariationID, TargetVariationID, RandomizationUnit, RuleIDOrFallthrough string; Status StatusBlock; CreatedAt time.Time; StartedAt, EndedAt *time.Time; LatestStageIndex int; ExtensionDurationMillis *int64; Stages []Stage; Events []Event; MetricConfigurations []MetricConfiguration; Links map[string]Link }
- type StatusBlock struct { Status, Kind, Label string }
- type Stage struct { StageIndex, Allocation int; DurationMillis int64; Duration string; StartedAt *time.Time; SafeRollForward *bool }
- type Event struct { Kind string; CreatedAt time.Time; StageIndex int; MetricKey, Message string }
- type MetricConfiguration struct { MetricKey, Kind string; MinSampleSize int; AutoRollback bool; Status string }
- type Envelope struct { SchemaVersion, Kind string; Data interface{}; Meta *EnvelopeMeta; Error *EnvelopeError }
- type RolloutList struct { Items []Rollout; Links map[string]Link }

From internal/rollouts/envelope.go:
- func NewRolloutEnvelope(r *Rollout) Envelope         // kind: "Rollout"
- func NewErrorEnvelope(code, message, nextAction string) Envelope  // kind: "Error"

From internal/rollouts/errors.go (existing constants — REUSE):
- ErrCodeUnauthorized, ErrCodeForbidden, ErrCodeNotFound, ErrCodeBadRequest, ErrCodeConflict, ErrCodeRateLimited, ErrCodeUpstreamUnavailable, ErrCodeNetworkError, ErrCodeBetaGateClosed, ErrCodeUnknownUpstream
- type RolloutError struct { Code, Message, NextAction string; StatusCode int; RawBody []byte }

From cmd/cliflags/flags.go (existing — REUSE):
- AccessTokenFlag = "access-token"
- BaseURIFlag     = "base-uri"
- EnvironmentFlag = "environment"; EnvironmentFlagDescription
- FlagFlag        = "flag"
- ProjectFlag     = "project"
- func GetOutputKind(cmd *cobra.Command) string  // returns "json" or whatever --output is

From cmd/flags/rollouts/rollouts.go (existing — EXTEND):
- func NewRolloutsCmd(client rollouts.Client, analyticsTrackerFn analytics.TrackerFn) *cobra.Command
  // already does: cmd.AddCommand(NewListCmd(client)); cmd.AddCommand(NewStartCmd(client))

From internal/rollouts/mock_client.go (existing — REUSE; covers Get + List + Start; no mockgen rerun needed):
- type MockClient struct { mock.Mock }
- (*MockClient).On("Get", accessToken, baseURI, projKey, envKey, rolloutID).Return(*Rollout, error)
- (*MockClient).On("List", accessToken, baseURI, projKey, flagKey, opts).Return(*RolloutList, error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RolloutIdFlag + ErrCodeNoRolloutsFound + CLI-LEARNINGS.md skeleton</name>
  <files>cmd/cliflags/flags.go, internal/rollouts/errors.go, .planning/CLI-LEARNINGS.md</files>

  <read_first>
    - cmd/cliflags/flags.go (full file; alphabetical-by-name layout of constants and descriptions; need exact insertion points alphabetically — `RolloutIdFlag` lands between `RoleFlag` and `RuleIDFlag`; `RolloutIdFlagDescription` similarly between `RandomizationUnitFlagDescription` and `RevertOnRegressionFlagDescription` — match the surrounding sort discipline)
    - internal/rollouts/errors.go (full file; the existing ErrCode* block ends with `ErrCodeRolloutAlreadyRunning` from Phase 2; new constant lands at the end of the const block)
    - .planning/API-PAPERCUTS.md (lines 1-40; structure template for the new CLI-LEARNINGS.md doc — header, Last updated stamp, Active count, Active Index table with Anchor / One-line / Discovered / Affected commands columns, then per-entry sections)
    - .planning/phases/03-status-watch/03-CONTEXT.md (D-13 — CLI-LEARNINGS doc requirement and the 7 seeded topics in `<specifics>` "CLI / UX complexities" list)
  </read_first>

  <action>
    1. In `cmd/cliflags/flags.go`, add inside the existing const block (alphabetically between `RoleFlag` and `RuleIDFlag`):
       - `RolloutIdFlag = "rollout-id"`
       And add inside the existing description const block (alphabetically — placement is executor discretion within the description block, since the existing block is grouped roughly by name):
       - `RolloutIdFlagDescription = "Specific rollout UUID to inspect; when set, --environment is also required (API-PAPERCUTS.md PC-004 — GET-by-ID requires environmentKey in the URL path). Omit this flag to get the most-recent rollout on the flag."`
       Do NOT add `RolloutIdFlag` to the `AllFlagsHelp()` map — this is a per-invocation flag, not a persisted config-file key.

    2. In `internal/rollouts/errors.go`, add ONE new constant inside the existing const block, after `ErrCodeRolloutAlreadyRunning`:
       - `ErrCodeNoRolloutsFound = "no_rollouts_found"`
       Add a one-line Go comment above the new constant: `// Phase 3 status-specific error code (D-09): emitted when --rollout-id is absent and the flag has zero rollouts.`

    3. Create `.planning/CLI-LEARNINGS.md` with this structure (mirroring API-PAPERCUTS.md):
       - Title: `# CLI / UX Learnings: ldcli flags rollouts-beta`
       - Tagline blockquote: prototype-era learnings doc that catalogues open CLI/UX questions surfaced by the rollouts-beta prototype; sibling to API-PAPERCUTS.md; feeds production CLI build's design discussions per REQ-LEARN-01 (PROJECT.md) and LEARN-01..03 (REQUIREMENTS.md).
       - `**Last updated:** 2026-05-14`
       - `Active count: 7`
       - `Resolved count: 0`
       - One-paragraph framing: seeded by Phase 3 plan-phase; entries are open CLI/UX questions, not API gaps (which live in API-PAPERCUTS.md); production CLI build revisits per LEARN-03.
       - `## Active Index` with a Markdown table; columns: Anchor | One-line | Discovered | Affected commands.
       - Seven rows in the table, using anchors `CL-001` through `CL-007`:
         - CL-001 — JSON envelope vs raw-resource wire shape (gh/kubectl style) — 2026-05-14 — all rollouts-beta
         - CL-002 — AGENT-04 timestamp format: RFC 3339 vs raw int64 millis pass-through — 2026-05-14 — all rollouts-beta
         - CL-003 — Phase 1 D-03 structured `reason` lift vs single `label` string — 2026-05-14 — status, list
         - CL-004 — Exit-code taxonomy richness (exit 1 + error.code vs distinct numeric codes) — 2026-05-14 — all rollouts-beta
         - CL-005 — Watch-shaped use cases after --watch removal (poll cadence, event-driven monitoring) — 2026-05-14 — status
         - CL-006 — "Most recent" semantics (createdAt DESC vs most-recent-running) — 2026-05-14 — status, list
         - CL-007 — `--rollout-id` requiring `--environment` (PC-004 surface) — 2026-05-14 — status
       - `## Entries` section, with one `### CL-NNN — <one-line title>` subsection per anchor. Each entry uses the fields specified by REQUIREMENTS.md LEARN-01: **Question**, **What we did in prototype**, **What's open for production CLI build**, **Severity**. Severity vocabulary: `low` / `medium` / `high`. Content for each entry comes from CONTEXT.md `<specifics>` "CLI / UX complexities" list — keep entries concise (3-5 sentences per field max). Reasonable severity guesses per entry are executor discretion.
       - Final line: `---` separator followed by `## Resolved` with an "(empty)" placeholder.
  </action>

  <verify>
    <automated>go build ./... &amp;&amp; grep -q 'RolloutIdFlag *= "rollout-id"' cmd/cliflags/flags.go &amp;&amp; grep -q 'ErrCodeNoRolloutsFound *= "no_rollouts_found"' internal/rollouts/errors.go &amp;&amp; test -f .planning/CLI-LEARNINGS.md &amp;&amp; grep -c '^### CL-00' .planning/CLI-LEARNINGS.md | grep -q '^7$'</automated>
  </verify>

  <acceptance_criteria>
    - `cmd/cliflags/flags.go` contains the literal token `RolloutIdFlag = "rollout-id"` (exact string match including spaces)
    - `cmd/cliflags/flags.go` contains a `RolloutIdFlagDescription` const whose value mentions both `--environment` and `PC-004`
    - `cmd/cliflags/flags.go` `AllFlagsHelp()` does NOT include `RolloutIdFlag` (verify: `grep -A 20 'func AllFlagsHelp' cmd/cliflags/flags.go | grep -c RolloutIdFlag` returns 0)
    - `internal/rollouts/errors.go` contains the literal `ErrCodeNoRolloutsFound = "no_rollouts_found"`
    - `internal/rollouts/errors.go` no other ErrCode* constants are renamed or removed (verify by diff scope)
    - `.planning/CLI-LEARNINGS.md` exists at the repo `.planning/` directory
    - `.planning/CLI-LEARNINGS.md` contains an `## Active Index` heading
    - `.planning/CLI-LEARNINGS.md` contains exactly 7 `### CL-` subsection headings (verify: `grep -c '^### CL-00' .planning/CLI-LEARNINGS.md` returns 7)
    - `.planning/CLI-LEARNINGS.md` contains each of `**Question**`, `**What we did in prototype**`, `**What's open for production CLI build**`, `**Severity**` at least 7 times each (one per entry)
    - `go build ./...` exits 0
  </acceptance_criteria>

  <done>RolloutIdFlag and ErrCodeNoRolloutsFound constants exist and compile; CLI-LEARNINGS.md skeleton is committed with 7 seeded entries using the LEARN-01 structured template.</done>
</task>

<task type="auto">
  <name>Task 2: Implement status.go (command + RunE + resolveRollout + emit helpers) and plaintext renderer + wire into rollouts.go</name>
  <files>cmd/flags/rollouts/status.go, cmd/flags/rollouts/plaintext.go, cmd/flags/rollouts/rollouts.go</files>

  <read_first>
    - cmd/flags/rollouts/list.go (full file; this is the closest analog — `runE` closure shape, viper-at-RunE-time pattern, `emitSuccess` / `emitError` split, JSON-mode error envelope on stdout via `NewErrorEnvelope`, short sentinel return from `emitError` for AGENT-04 / D-07 compliance, `errors.As` against `*rollouts.RolloutError`)
    - cmd/flags/rollouts/start.go (Phase 2 analog; `NewRolloutEnvelope` usage at lines 270-271, `emitStartSuccess` pattern at lines 277-289 — single-rollout envelope wrap)
    - cmd/flags/rollouts/plaintext.go (full file; existing `renderDetailed` is the closest layout reference for sectioned per-record output; `emptyDash`, `timePtrOrDash`, `formatStage`, RFC 3339 formatting are reusable helpers to call from the new renderer)
    - cmd/flags/rollouts/rollouts.go (lines 52-54 show the existing `cmd.AddCommand(NewListCmd(client))` / `cmd.AddCommand(NewStartCmd(client))` registration site)
    - cmd/flags/rollouts/flags.go (existing `initListFlags` pattern; mirror for `initStatusFlags`)
    - internal/rollouts/models.go (Rollout / StatusBlock / Stage / Event / MetricConfiguration field layout)
    - internal/rollouts/envelope.go (NewRolloutEnvelope / NewErrorEnvelope helpers)
    - .planning/phases/03-status-watch/03-CONTEXT.md sections: D-02 (status surface), D-03 (most-recent resolution + rollout-id requires environment), D-04 (createdAt DESC most-recent semantics), D-05 (envelope reuse verbatim), D-07 (plaintext layout reference), D-09 (ErrCodeNoRolloutsFound), D-12 (no new Client methods)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-CONTEXT.md D-01 (any error → exit 1), D-07 (JSON errors on stdout, plaintext errors on stderr via root)
  </read_first>

  <action>
    1. Create `cmd/flags/rollouts/status.go` with package `rollouts` and these elements (mirror `list.go` and `start.go` shape and import layout):

       1a. Package doc string `statusLongDescription` describing the command surface (D-02 — `status --flag <key> [--environment <env>] [--rollout-id <id>]`). Include in the help text:
           - The "most-recent by createdAt DESC" default behavior (cite AGENT-05 wording style from list.go)
           - That `--rollout-id` requires `--environment` (cite PC-004 from API-PAPERCUTS.md by anchor)
           - That output is the full rollout detail (no `--detailed` toggle — D-08)
           - The BETA banner sentence used by list/start

       1b. `func NewStatusCmd(client rollouts.Client) *cobra.Command` constructor mirroring `NewListCmd` shape: `Args: validators.Validate()`, `Long: statusLongDescription`, `RunE: statusRunE(client)`, `Short: "Show automated rollout status for a feature flag (beta)"`, `Use: "status"`. Apply `cmd.SetUsageTemplate(resourcescmd.SubcommandUsageTemplate())` and call `initStatusFlags(cmd)`.

       1c. `func initStatusFlags(cmd *cobra.Command)` in this file (or in `flags.go` — executor discretion):
           - `--flag` REQUIRED, bound to `cliflags.FlagFlag` (mirror `initListFlags` lines 18-21)
           - `--project` REQUIRED, bound to `cliflags.ProjectFlag` (mirror lines 23-26)
           - `--environment` OPTIONAL, bound to `cliflags.EnvironmentFlag`, help string explicitly notes "Required when --rollout-id is set; see API-PAPERCUTS.md PC-004."
           - `--rollout-id` OPTIONAL, bound to `cliflags.RolloutIdFlag`, help string is `cliflags.RolloutIdFlagDescription`
           Do NOT register `--limit`, `--all`, `--detailed`, `--state` — they are not in the D-02 surface.

       1d. `func statusRunE(client rollouts.Client) func(*cobra.Command, []string) error` returning a closure. Inside the closure:
           - Read flag values at RunE time via `viper.GetString(...)` for: AccessTokenFlag, BaseURIFlag, ProjectFlag, FlagFlag, EnvironmentFlag, RolloutIdFlag.
           - **D-03 validation BEFORE the API call:** if `rolloutID != "" && envKey == ""`, call `emitStatusError(cmd, errors.NewError("--environment is required when --rollout-id is set (API-PAPERCUTS.md PC-004 — GET-by-ID requires environmentKey in the URL path)"))` and return. The thrown error must surface as `error.code: "bad_request"` in JSON mode by virtue of falling through to `ErrCodeBadRequest`/`ErrCodeUnknownUpstream` in `emitStatusError`; executor may choose to wrap it as a typed `*rollouts.RolloutError{Code: ErrCodeBadRequest, Message: "...", NextAction: "Pass --environment alongside --rollout-id"}` so the envelope carries a precise code — preferred. Use `ErrCodeBadRequest` since this is a user input validation failure.
           - **Resolve the rollout:** call a helper `resolveRollout(ctx, client, accessToken, baseURI, projKey, flagKey, envKey, rolloutID)` (defined below) which returns `(*rollouts.Rollout, error)`.
           - If `err != nil`, call `emitStatusError(cmd, err)` and return.
           - Build the envelope via `rollouts.NewRolloutEnvelope(rollout)` (D-05 — reuse verbatim).
           - Call `emitStatusSuccess(cmd, env, rollout)`.

       1e. `func resolveRollout(ctx context.Context, client rollouts.Client, accessToken, baseURI, projKey, flagKey, envKey, rolloutID string) (*rollouts.Rollout, error)`:
           - **If `rolloutID != ""`:** return `client.Get(ctx, accessToken, baseURI, projKey, envKey, rolloutID)` directly.
           - **Else (most-recent path):** call `client.List(ctx, accessToken, baseURI, projKey, flagKey, rollouts.ListOpts{Environment: envKey, Limit: 1})`. If `err != nil`, return `(nil, err)`. If the returned list is nil OR `len(list.Items) == 0`, return `(nil, &rollouts.RolloutError{Code: rollouts.ErrCodeNoRolloutsFound, Message: fmt.Sprintf("No rollouts found for flag %q", flagKey), NextAction: "Verify the flag has at least one rollout, or pass --rollout-id <id> --environment <env> to address a specific rollout"})`. Otherwise return `(&list.Items[0], nil)`. **D-04 note:** the most-recent semantics are honored verbatim from Phase 1 list (`CreatedAt DESC, ID ASC` sort already enforced server-side AND by the Client.List implementation); no additional sort is needed here because we request `Limit: 1`.

       1f. `func emitStatusSuccess(cmd *cobra.Command, env rollouts.Envelope, rollout *rollouts.Rollout) error` — mirror `emitStartSuccess` (start.go lines 277-289). JSON branch: `json.MarshalIndent(env, "", "  ")`, write to `cmd.OutOrStdout()`, return nil. Plaintext branch: `fmt.Fprint(cmd.OutOrStdout(), RenderRolloutStatusPlaintext(rollout))`.

       1g. `func emitStatusError(cmd *cobra.Command, err error) error` — mirror `emitStartError` (start.go lines 298-323) and `emitError` from list.go (lines 153-180). Default `code = rollouts.ErrCodeUnknownUpstream`, then `errors.As` against `*rollouts.RolloutError` to extract code/message/nextAction. JSON mode: marshal `rollouts.NewErrorEnvelope(code, message, nextAction)`, write envelope to stdout, return `errors.NewError("rollouts status failed")` short sentinel. Plaintext mode: return `errors.NewError(message)` so root's `Fprintln(os.Stderr, err)` surfaces it.

       Imports the file needs: `context`, `encoding/json`, `stderrors "errors"`, `fmt`, `github.com/spf13/cobra`, `github.com/spf13/viper`, `github.com/launchdarkly/ldcli/cmd/cliflags`, `resourcescmd "github.com/launchdarkly/ldcli/cmd/resources"`, `github.com/launchdarkly/ldcli/cmd/validators`, `github.com/launchdarkly/ldcli/internal/errors`, `github.com/launchdarkly/ldcli/internal/rollouts`.

    2. In `cmd/flags/rollouts/plaintext.go`, add `func RenderRolloutStatusPlaintext(r *rollouts.Rollout) string`:

       2a. Nil-safe: if `r == nil`, return `"No rollout.\n"`.

       2b. Use `strings.Builder` (or `bytes.Buffer`) and emit sectioned blocks per D-07. Section order:
           - **Header**: `Rollout: <id>`, `Flag: <flagKey>            Env: <envKey>`, `Kind: <guarded|progressive>   State: <status.kind>`, `Label: <status.label>`, `Started: <RFC 3339>           Ended: <RFC 3339 or —>`, `Target var: <id>              Original var: <id>`. Use `emptyDash`, `timePtrOrDash`, RFC3339 formatting from existing helpers; the `CreatedAt` field is a non-pointer `time.Time` (zero check via `IsZero()`).
           - Blank line, then `Stages:`. For each Stage in `r.Stages`, render one line: `  [<marker>] <alloc>%  <duration>  <stageState>` where:
             - `<marker>` is `✓` for stages with `StageIndex < r.LatestStageIndex`, `→` for the stage at `r.LatestStageIndex`, and a space for stages beyond
             - `<alloc>` is `Stage.Allocation / 1000` (basis-points → percent — match `formatStage` in existing plaintext.go line 135)
             - `<duration>` is `Stage.Duration` (already a Go-duration string per AGENT-04 / PC-014 conversion in `rawStage.toStage()`); fall back to `<DurationMillis>ms` only if Duration is empty
             - `<stageState>` is a short string derived from index position: `completed` (< current), `in progress` (= current), `pending` (> current). When the rollout's `Status.Kind` is `completed` or `reverted`, render all stages as `completed`. Executor discretion on exact wording.
           - If `len(r.Stages) == 0`, render `  (no stages)`.
           - Blank line, then `Metrics:`. For each MetricConfiguration in `r.MetricConfigurations`, render one line: `  <metricKey>    <metric.status>    auto-rollback: <true|false>`. Use a `text/tabwriter` for alignment, mirroring `renderTable` in existing plaintext.go (lines 41-56). If `len(r.MetricConfigurations) == 0`, render `  (no metrics monitored)`.
           - Blank line, then `Events:`. For each Event in `r.Events`, render one line: `  <RFC3339 hh:mmZ subset or full RFC3339> <event.kind> <event.metricKey-if-present>`. Use the same alignment approach. If `len(r.Events) == 0`, render `  (no events)`.

       2c. Reasonable defaults for missing/zero values: use `emptyDash` for empty strings; use `timePtrOrDash` for `*time.Time`; use a dedicated `timeOrDash(time.Time)` helper for the non-pointer `CreatedAt` (executor may add a small helper next to `timePtrOrDash`).

    3. In `cmd/flags/rollouts/rollouts.go`, after the existing `cmd.AddCommand(NewStartCmd(client))` line, add:
       - `cmd.AddCommand(NewStatusCmd(client))`
  </action>

  <verify>
    <automated>go build ./... &amp;&amp; ./ldcli flags rollouts-beta --help 2>&amp;1 | grep -q ' status ' &amp;&amp; ./ldcli flags rollouts-beta status --help 2>&amp;1 | grep -q -- '--rollout-id'</automated>
  </verify>

  <acceptance_criteria>
    - `cmd/flags/rollouts/status.go` exists and contains `func NewStatusCmd(client rollouts.Client) *cobra.Command`
    - `cmd/flags/rollouts/status.go` registers `cliflags.FlagFlag` as required and `cliflags.ProjectFlag` as required
    - `cmd/flags/rollouts/status.go` registers `cliflags.EnvironmentFlag` (optional) and `cliflags.RolloutIdFlag` (optional)
    - `cmd/flags/rollouts/status.go` contains a code path calling `client.Get(` (the --rollout-id branch)
    - `cmd/flags/rollouts/status.go` contains a code path calling `client.List(` with `Limit: 1` (the most-recent branch)
    - `cmd/flags/rollouts/status.go` contains `rollouts.NewRolloutEnvelope(` for the success path
    - `cmd/flags/rollouts/status.go` contains `rollouts.NewErrorEnvelope(` for the JSON-mode error path
    - `cmd/flags/rollouts/status.go` contains `rollouts.ErrCodeNoRolloutsFound` reference (the empty-list mapping)
    - `cmd/flags/rollouts/status.go` contains a validation guard producing an error when `rolloutID != ""` and `envKey == ""` BEFORE any client call (verify by reading the source — the guard must precede the `resolveRollout`/`client.Get`/`client.List` calls)
    - `cmd/flags/rollouts/status.go` emits its JSON error envelope to `cmd.OutOrStdout()` (NOT stderr) and returns a short sentinel error for AGENT-04 / D-07 compliance — mirror list.go `emitError` (line 175-176)
    - `cmd/flags/rollouts/plaintext.go` contains `func RenderRolloutStatusPlaintext(r *rollouts.Rollout) string`
    - The renderer output for a non-nil rollout contains the literal section headers `Stages:`, `Metrics:`, `Events:` and a `Rollout:` header line
    - `cmd/flags/rollouts/rollouts.go` contains the literal call `cmd.AddCommand(NewStatusCmd(client))`
    - `go build ./...` exits 0
    - `./ldcli flags rollouts-beta --help` lists `status` as a subcommand
    - `./ldcli flags rollouts-beta status --help` advertises `--flag`, `--project`, `--environment`, `--rollout-id`
  </acceptance_criteria>

  <done>The status subcommand exists, compiles, registers with the rollouts-beta parent, accepts the documented flag surface, resolves rollouts via Client.Get or Client.List, and emits the existing envelope on success / a typed error envelope on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write status_test.go covering happy paths and edge cases</name>
  <files>cmd/flags/rollouts/status_test.go</files>

  <read_first>
    - cmd/flags/rollouts/list_test.go (full file; this is the test scaffolding template — `cmd.CallCmd` / `cmd.CallCmdWithStderr` harness, `rollouts.MockClient` usage with `mock.MatchedBy`, JSON envelope round-trip via `json.Unmarshal`, error envelope assertion shape against `RolloutError{Code, Message, NextAction}`)
    - cmd/flags/rollouts/start_test.go (Phase 2 analog for single-rollout return assertions)
    - cmd/flags/rollouts/status.go (the file produced by Task 2 — the test must match its actual call signatures into the mock)
    - internal/rollouts/mock_client.go (full file; signature of `MockClient.Get(accessToken, baseURI, projKey, envKey, rolloutID)` and `MockClient.List(accessToken, baseURI, projKey, flagKey, opts)` — note the argument names matter for `.On(...)` matching)
    - internal/rollouts/models.go (Rollout / Stage / Event / MetricConfiguration field layout for fixture construction)
  </read_first>

  <behavior>
    Tests, each as a sub-test under one top-level `TestStatus` function (or split into named top-level functions — executor discretion):

    1. **TestStatus_MostRecentPath_JSONOutput** — `--flag X --project P` only (no --rollout-id, no --environment). Mock expects: `List(token, _, "P", "X", opts)` where `opts.Limit == 1` (verify with `mock.MatchedBy`). Mock returns: `&RolloutList{Items: []Rollout{{ID: "r1", Kind: "progressive", Status: StatusBlock{Status: "in_progress", Kind: "active", Label: "Monitoring"}, ...}}}`. Assert: exit 0, stdout is a JSON envelope with `schemaVersion == "rollouts.v1beta1"`, `kind == "Rollout"`, `data.id == "r1"`, `data.status.kind == "active"`.

    2. **TestStatus_RolloutIdPath_JSONOutput** — `--flag X --project P --rollout-id RID --environment test`. Mock expects: `Get(token, _, "P", "test", "RID")` (NOT `List`). Mock returns: `&Rollout{ID: "RID", Kind: "guarded", Status: StatusBlock{Status: "monitoring_regressed", Kind: "regressed", Label: "Regression on m-latency"}}`. Assert: exit 0, stdout JSON envelope contains `data.id == "RID"`, `data.kind == "guarded"`, `data.status.kind == "regressed"`. Assert `mockClient.AssertExpectations(t)` to prove `List` was NOT called.

    3. **TestStatus_RolloutIdWithoutEnvironment_ValidationError** — `--flag X --project P --rollout-id RID` (no --environment), with `--output json`. Mock: `On("Get", ...).Maybe()` and `On("List", ...).Maybe()` — neither should be called. Assert: exit non-zero, the test harness `err != nil`. Stdout JSON envelope with `kind == "Error"`, `error.code == "bad_request"`, `error.message` mentions "--environment" and `--rollout-id` (substring assertions). Assert `mockClient.AssertNotCalled(t, "Get", mock.Anything, mock.Anything, mock.Anything, mock.Anything, mock.Anything)` AND `mockClient.AssertNotCalled(t, "List", mock.Anything, mock.Anything, mock.Anything, mock.Anything, mock.Anything)`.

    4. **TestStatus_NoRolloutsFound_ErrorEnvelopeOnStdout** — `--flag X --project P --output json`. Mock: `List` returns `&RolloutList{Items: []Rollout{}}` (empty), nil error. Assert: exit non-zero, stdout JSON envelope `kind == "Error"`, `error.code == "no_rollouts_found"` (literal value, not just the constant), `error.message` mentions the flag key. Assert stderr does NOT contain the literal string `"kind": "Error"` (envelope must not leak to stderr — AGENT-04 / D-07; mirror list_test.go TestListErrorEnvelope assertion shape).

    5. **TestStatus_NoRolloutsFound_NilList_ErrorEnvelope** — same as test 4 but mock returns `(nil, nil)` instead of an empty list. Assert same outcome (`error.code == "no_rollouts_found"`). This proves the nil-guard in `resolveRollout`.

    6. **TestStatus_PlaintextOutput_ContainsSectionHeaders** — `--flag X --project P` (no --output json; force TTY via `t.Setenv("FORCE_TTY", "1")` — mirror list_test.go line 35). Mock `List` returns a Rollout with at least one Stage, one MetricConfiguration, and one Event. Assert stdout contains the substrings `Stages:`, `Metrics:`, `Events:`, and a `Rollout:` header.

    7. **TestStatus_ListClientError_ErrorEnvelope** — `--flag X --project P --output json`. Mock `List` returns `(nil, &rollouts.RolloutError{Code: rollouts.ErrCodeNotFound, Message: "Feature flag not found", NextAction: "Verify --flag value"})`. Assert exit non-zero, JSON envelope `error.code == "not_found"`, `error.message == "Feature flag not found"`, `error.nextAction == "Verify --flag value"`. This proves Phase 1 error mapping is reused verbatim.

    Use `cmd.CallCmd` (stdout only) or `cmd.CallCmdWithStderr` (stdout + stderr) per list_test.go conventions; use `cmd.APIClients{RolloutsClient: mockClient}` and `analytics.NoopClientFn{}.Tracker()` for harness wiring.
  </behavior>

  <action>
    Create `cmd/flags/rollouts/status_test.go` with `package rollouts_test`. Import `encoding/json`, `testing`, `time`, `github.com/stretchr/testify/assert`, `github.com/stretchr/testify/mock`, `github.com/stretchr/testify/require`, `github.com/launchdarkly/ldcli/cmd`, `github.com/launchdarkly/ldcli/internal/analytics`, `github.com/launchdarkly/ldcli/internal/rollouts`.

    Implement all 7 tests above. Use the `makeRollout` helper pattern from list_test.go for fixture construction; either copy/duplicate it or factor a tiny private helper in this file (executor discretion — test files in this package already use copy/paste fixture helpers).

    For Test 3 (validation error), use `--output json` to inspect the envelope without depending on stderr behavior of the harness; the validation guard must produce a typed `*rollouts.RolloutError` so the envelope carries `error.code == "bad_request"`. Match the literal envelope error.code string in the assertion.
  </action>

  <verify>
    <automated>go test ./cmd/flags/rollouts/... -run TestStatus -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `cmd/flags/rollouts/status_test.go` exists with `package rollouts_test`
    - File contains a test function whose name begins with `TestStatus` (verify: `grep -c '^func TestStatus' cmd/flags/rollouts/status_test.go` returns 1 or more)
    - The test file contains 7 distinct test scenarios (sub-tests or separate top-level functions): MostRecentPath, RolloutIdPath, RolloutIdWithoutEnvironment, NoRolloutsFound (empty list), NoRolloutsFound (nil list), PlaintextOutput, ListClientError. Verify: `grep -E -c 'MostRecentPath|RolloutIdPath|WithoutEnvironment|NoRollouts|PlaintextOutput|ClientError' cmd/flags/rollouts/status_test.go` returns at least 7.
    - At least one test invokes `mockClient.On("Get", ...)` and another invokes `mockClient.On("List", ...)` (proves both code paths are exercised)
    - At least one test asserts `mockClient.AssertNotCalled(t, "Get", ...)` AND `mockClient.AssertNotCalled(t, "List", ...)` for the validation-error case
    - At least one test asserts the literal string `"no_rollouts_found"` is the value of `error.code` in the envelope (proves the D-09 constant value is wired correctly)
    - `go test ./cmd/flags/rollouts/... -run TestStatus -count=1` exits 0
    - `go test ./cmd/flags/rollouts/... -count=1` exits 0 (the whole package — proves no regression in list_test.go or start_test.go)
  </acceptance_criteria>

  <done>All seven test scenarios pass against the implementation from Task 2 (no implementation changes required after the tests land). The whole `cmd/flags/rollouts/...` package test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ldcli CLI process | --rollout-id, --environment, --flag, --project, --access-token flag values flow in from operator-controlled argv / config / env; the CLI must not echo unsanitized argv into stderr / logs in shell-interpreted contexts |
| ldcli → LaunchDarkly staging/prod API | `Authorization: <access-token>` header crosses a network boundary; existing Phase 1 retryablehttp client + setStandardHeaders already mitigate (logger=nil; LD-API-Version: beta) |
| LD API → ldcli (error path) | Upstream 4xx/5xx response bodies flow back into the CLI; existing `mapAPIError` already discards 5xx bodies into a generic message (T-02-03 from Phase 1 threat model) — Phase 3 reuses this verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | emitStatusError / NewErrorEnvelope on stdout | mitigate | Reuse Phase 1 `mapAPIError` 5xx body-suppression (T-02-03 carry-over); Phase 3 adds no new error paths that echo upstream bodies. The new `ErrCodeNoRolloutsFound` envelope is constructed from CLI-side strings only (flag key from --flag; no upstream body content). |
| T-03-02 | Information Disclosure | access token in error path | mitigate | viper.GetString(AccessTokenFlag) is read at RunE time and passed to `client.Get`/`client.List` — never logged or echoed. retryablehttp client `Logger: nil` already suppresses Authorization header logging (carried over from Phase 1 internal/rollouts/client.go line 84). Status command introduces no new logging sites. |
| T-03-03 | Information Disclosure | --rollout-id / --flag in JSON error envelope | accept | The flag key is interpolated into the `no_rollouts_found` error message ("No rollouts found for flag %q"). This is operator-supplied input echoed back to the same operator's terminal; no cross-tenant exposure. Per CONTEXT.md prototype framing, this is in-scope and not a HIGH-severity threat. |
| T-03-04 | Tampering | resolveRollout List → items[0] | accept | The "most-recent" selection trusts the server-side sort + Phase 1 client-side `CreatedAt DESC, ID ASC` re-sort (internal/rollouts/client.go lines 160-166). A malicious server could return an arbitrary item as items[0], but the operator has no higher-trust source — this is the API contract Phase 1 already accepts. |
| T-03-05 | Denial of Service | Long --flag / --rollout-id values | accept | retryablehttp.NewRequestWithContext returns an error on malformed URLs; URL-path components are PathEscape'd in `internal/rollouts/client.go` (lines 102-105 for List, lines 180-185 for Get). No new attack surface in Phase 3. |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go test ./cmd/flags/rollouts/... -count=1` exits 0 (full package, including the new status_test.go and the existing list_test.go / start_test.go / rollouts_test.go)
- `go test ./internal/rollouts/... -count=1` exits 0 (no regression on the unchanged client / models / errors / envelope / status_mapping packages)
- `./ldcli flags rollouts-beta --help` lists `status` as a subcommand alongside `list` and `start`
- `./ldcli flags rollouts-beta status --help` shows `--flag`, `--project`, `--environment`, `--rollout-id` in its flag list and the help long-description mentions PC-004
- `grep -c 'ErrCodeNoRolloutsFound' internal/rollouts/errors.go` returns 1
- `grep -c 'RolloutIdFlag' cmd/cliflags/flags.go` returns at least 2 (constant + description, plus references)
- `test -f .planning/CLI-LEARNINGS.md` succeeds
- `grep -c '^### CL-00' .planning/CLI-LEARNINGS.md` returns 7
</verification>

<success_criteria>
Phase 3 SC#1: an operator can run `ldcli flags rollouts-beta status --flag <key>` and receive full rollout detail (stages / metrics / events / state / variations / timestamps) — verified by Task 3 tests TestStatus_MostRecentPath_JSONOutput, TestStatus_RolloutIdPath_JSONOutput, TestStatus_PlaintextOutput_ContainsSectionHeaders.

Phase 3 SC#2: JSON-mode output exposes the bucketed `status.kind` lifecycle classifier alongside the raw upstream `status` value inside the `rollouts.v1beta1` envelope's `data.status` block — already provided by Phase 1's `StatusBlock` (status / kind / label) which the status command surfaces unchanged via `NewRolloutEnvelope`. Verified by TestStatus tests asserting `data.status.kind` is one of `active/regressed/...` and `data.status.status` is the raw enum.

Phase 3 SC#3 (LEARN-01): `.planning/CLI-LEARNINGS.md` exists with the structured template + 7 seeded entries per D-13 — verified by Task 1 acceptance criteria. LEARN-02 cross-cutting append discipline is wired into Plan 03-02's smoke-and-capture loop. DOC-02 / DOC-04 cross-cutting are also wired into Plan 03-02.
</success_criteria>

<output>
After completion, create `.planning/phases/03-status-watch/03-01-SUMMARY.md` capturing: files created/modified, the deliberate scope (vertical slice — no new client methods, envelope reuse verbatim, sectioned plaintext layout decisions), and any in-flight observations the executor noticed that should land in CLI-LEARNINGS.md or API-PAPERCUTS.md when the real-staging smoke (Plan 03-02) runs.
</output>
</content>
</invoke>