---
phase: 01-list-foundation-first-end-to-end-slice
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - internal/rollouts/client.go
  - internal/rollouts/models.go
  - internal/rollouts/errors.go
  - internal/rollouts/status_mapping.go
  - internal/rollouts/status_mapping_test.go
  - internal/rollouts/client_test.go
  - internal/rollouts/testdata/list_progressive_in_progress.json
  - internal/rollouts/testdata/list_guarded_regressed.json
  - internal/rollouts/testdata/get_guarded_completed.json
autonomous: true
mvp_mode: true
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-08
  - LIST-02
  - AGENT-03
  - AGENT-04

must_haves:
  truths:
    - "RolloutsClient.List makes a real HTTP GET to `{baseURI}/internal/projects/{p}/flags/{flagKey}/automated-releases` via the retryablehttp client (replacing Plan 01's stub body)"
    - "Transient 5xx responses are retried up to 4 times (max ~16s total wall time); 4xx responses are NEVER retried (D-01 + FOUND-05 envelope)"
    - "401/403/404/409/400/429/5xx all map to distinct `error.code` enum values in the JSON envelope (D-01: structured error.code in envelope, not numeric exit codes)"
    - "Each of the 13 documented raw API statuses produces the correct (`kind`, `label`) tuple per the UI-parity table in CONTEXT.md `<specifics>` and RESEARCH.md §Status Mapping (D-02)"
    - "API int64-millis timestamps are converted to time.Time before marshaling, and JSON output emits RFC 3339 UTC strings (AGENT-04)"
    - "Stage durations are emitted as Go-style unit-bearing strings (e.g., `15m0s`, `1h0m0s`) alongside the raw `durationMillis` (AGENT-04)"
    - "The Idempotency-Key header is sent on mutation methods (none in Phase 1 — the helper is wired and unit-tested but no Phase 1 call site exercises it; Phase 2 will)"
    - "The Plan 01 stub body in RolloutsClient.List is replaced with the real HTTP path; Plan 01's sanity test in client_test.go is replaced with httptest.NewServer round-trip tests"
  artifacts:
    - path: "internal/rollouts/client.go"
      provides: "Real RolloutsClient.List + Get bodies — retryablehttp request, header construction, status code branching, DTO unmarshal + conversion"
    - path: "internal/rollouts/status_mapping.go"
      provides: "MapStatus + mapStatusToKind + formatLabel covering all 13 documented raw statuses → 5 kinds with reason inline"
      exports: ["MapStatus", "DeriveStatusBlock"]
    - path: "internal/rollouts/status_mapping_test.go"
      provides: "Table-driven test asserting each of 13 raw statuses → expected (kind, label) tuple per CONTEXT.md <specifics>"
    - path: "internal/rollouts/models.go"
      provides: "Raw API DTO types (rawRolloutList, rawRollout, rawStage) and converter functions (toRolloutList, toRollout, toStage) handling int64-millis → time.Time and durationMillis → Go duration string"
      exports: ["RolloutList", "Rollout"]
    - path: "internal/rollouts/errors.go"
      provides: "Full mapAPIError mapping 401/403/404/409/400/429/5xx → ErrCode* constants with operator-facing message + nextAction hint; mapTransportError for network failures after retries"
    - path: "internal/rollouts/client_test.go"
      provides: "httptest.NewServer round-trip tests for List + Get: happy path, 5xx-retry behavior, 4xx-no-retry behavior, 401/404 error.code mapping, header assertions (Authorization, User-Agent), URL/query-string construction"
    - path: "internal/rollouts/testdata/"
      provides: "Three golden response fixtures captured from staging or hand-crafted to exercise progressive in_progress, guarded regressed, and a completed Get response"
  key_links:
    - from: "RolloutsClient.List"
      to: "retryablehttp.Client.Do"
      via: "retryablehttp.NewRequestWithContext + req.Header.Set + c.httpClient.Do"
      pattern: "c\\.httpClient\\.Do\\("
    - from: "RolloutsClient.List response handling"
      to: "raw.toRolloutList()"
      via: "json.Unmarshal into rawRolloutList then convert"
      pattern: "\\.toRolloutList\\(\\)"
    - from: "raw.toRollout"
      to: "DeriveStatusBlock"
      via: "explicit call inside the converter so every emitted Rollout has Status populated"
      pattern: "DeriveStatusBlock\\("
    - from: "RolloutsClient.List error path"
      to: "mapAPIError or mapTransportError"
      via: "branching on resp.StatusCode >= 400 vs c.httpClient.Do error"
      pattern: "mapAPIError\\("
---

<objective>
Replace the Plan 01 stub bodies in `RolloutsClient.List` and `RolloutsClient.Get` with the
real HTTP path against `/internal/projects/{p}/flags/{flagKey}/automated-releases`. Ship the
13-state status mapping (D-02), the full `error.code` taxonomy (D-01 + FOUND-08), the
int64-millis → time.Time + Go-duration converters (AGENT-04), and `httptest.NewServer`
round-trip tests that prove the retry envelope, header construction, URL/query
assembly, and error mapping are correct end-to-end.

Purpose: Make `list` return real data. After Plan 02, `./ldcli flags rollouts-beta list`
against a live or stubbed-with-fixture API returns a fully-populated envelope with
all the status decoration the UI parity table specifies, and any upstream error is
classified into one of the documented `error.code` values rather than a generic
"something failed."

Output: Plan 02 ends with `make test` green AND `./ldcli flags rollouts-beta list --flag X
--project Y --base-uri http://localhost:NNNN` (against a local httptest fixture) producing
a fully realistic envelope. Plan 03 then layers ergonomics (flags, sort, plaintext table)
and the papercuts doc on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-PATTERNS.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From Plan 01 (already on disk by the time Plan 02 executes): -->

internal/rollouts/client.go (Plan 01 stub — Plan 02 replaces the method bodies):
```go
func (c RolloutsClient) List(ctx context.Context, accessToken, baseURI, projKey, flagKey string, opts ListOpts) (*RolloutList, error) {
    // Plan 01 stub: return &RolloutList{Items: []Rollout{}}, nil
    // Plan 02 replaces with real HTTP path below.
}
```

internal/rollouts/models.go (Plan 01 — Plan 02 adds raw types + converters):
```go
type Rollout struct {
    ID                  string       `json:"id"`
    Kind                string       `json:"kind"`              // rollout kind: guarded|progressive
    Status              StatusBlock  `json:"status"`            // nested 3-field per D-02
    CreatedAt           time.Time    `json:"createdAt"`
    StartedAt           *time.Time   `json:"startedAt,omitempty"`
    EndedAt             *time.Time   `json:"endedAt,omitempty"`
    // ... other fields
}
type StatusBlock struct {
    Status string `json:"status"`
    Kind   string `json:"kind"`
    Label  string `json:"label"`
}
```

internal/rollouts/errors.go (Plan 01 skeleton — Plan 02 fills mapAPIError):
```go
type RolloutError struct {
    Code       string
    Message    string
    NextAction string
    StatusCode int
    RawBody    []byte // never JSON-tagged (security)
}
// ErrCodeUnauthorized = "unauthorized" / ErrCodeForbidden = "forbidden" / ErrCodeNotFound = "not_found"
// ErrCodeConflict = "conflict" / ErrCodeBadRequest = "bad_request" / ErrCodeRateLimited = "rate_limited"
// ErrCodeUpstreamUnavailable = "upstream_unavailable" / ErrCodeNetworkError = "network_error"
// ErrCodeUnknownUpstream = "unknown_upstream"
```

Target Request flow inside `RolloutsClient.List` (per RESEARCH.md §"Request flow"):
```go
path := fmt.Sprintf("%s/internal/projects/%s/flags/%s/automated-releases",
    strings.TrimRight(baseURI, "/"),
    url.PathEscape(projKey),
    url.PathEscape(flagKey),
)
q := url.Values{}
if opts.Environment != "" {
    // PAPERCUT: PC-002 — `filter` accepts array but only honors element [0]
    q.Set("filter", "environmentKey:"+opts.Environment)
}
limit := opts.Limit
if limit <= 0 { limit = 20 }
if opts.All {
    // PAPERCUT: PC-003 — No pagination; we ask for a large limit best-effort
    limit = 1000
}
q.Set("limit", strconv.Itoa(limit))

req, err := retryablehttp.NewRequestWithContext(ctx, "GET", path+"?"+q.Encode(), nil)
if err != nil { return nil, errors.NewErrorWrapped("failed to build request", err) }
req.Header.Set("Authorization", accessToken)
req.Header.Set("Content-Type", "application/json")
req.Header.Set("User-Agent", fmt.Sprintf("launchdarkly-cli/v%s", c.cliVersion))
// No Idempotency-Key on GETs

resp, err := c.httpClient.Do(req)
if err != nil { return nil, mapTransportError(err) }
defer resp.Body.Close()
body, err := io.ReadAll(resp.Body)
if err != nil { return nil, errors.NewErrorWrapped("failed to read response body", err) }
if resp.StatusCode >= 400 { return nil, mapAPIError(body, resp.StatusCode) }

var raw rawRolloutList
if err := json.Unmarshal(body, &raw); err != nil {
    return nil, errors.NewErrorWrapped("failed to parse response", err)
}
return raw.toRolloutList(), nil
```

13-state mapping table (CANONICAL — copy from CONTEXT.md `<specifics>` + RESEARCH.md §Status Mapping):

| Raw API `status` | Sub-condition | `kind` | `label` template |
|---|---|---|---|
| `not_started` | — | `active` | `"Monitoring {rule}"` |
| `waiting` | — | `active` | `"Monitoring {rule}"` |
| `in_progress` | min sample not reached (guarded) | `active` | `"Monitoring {rule} for regressions… (not enough data)"` |
| `in_progress` | min sample reached (guarded) | `active` | `"Monitoring {rule} for regressions…"` |
| `in_progress` | extension active (guarded) | `active` | `"Monitoring extended by {duration}"` |
| `in_progress` | progressive (no metrics) | `active` | `"Monitoring {rule}"` |
| `monitoring_regressed` | — | `regressed` | `"Regressions detected on {rule} for {metric names}"` |
| `monitoring_stopped` | — | `paused` | `"{rule} paused at {N}%: regressions detected for {metric names}"` |
| `srm_stopped` | — | `paused` | `"{rule} paused at {N}%: sample ratio mismatch detected"` |
| `completed` | — | `completed` | `"Monitoring completed on {rule}"` |
| `manually_completed` | — | `completed` | `"{rule} rolled forward manually"` |
| `manually_reverted` | — | `reverted` | `"{rule} rolled back manually"` |
| `reverted` | insufficient sample | `reverted` | `"{rule} rolled back due to insufficient sample size"` |
| `reverted` | SRM event in events | `reverted` | `"{rule} rolled back automatically"` |
| `reverted` | regression event in events | `reverted` | `"{rule} rolled back automatically after detecting a regression for {metric names}"` |
| `archived` | — | `paused` | `"Monitoring of {rule} stopped early"` |

Placeholder derivations:
- `{rule}` ← `"the default rule"` if `RuleIDOrFallthrough == "fallthrough"` else `"rule " + RuleIDOrFallthrough`
- `{N}%` ← `currentStage.Allocation / 1000` (basis points → percent; current stage = `Stages[LatestStageIndex]`)
- `{metric names}` ← comma-joined `MetricKey` from `MetricConfigurations` where `Status == "regressed"`, falling back to `Events[*].MetricKey` where `Events[*].Kind == "regression_detected"`
- `{duration}` ← `(time.Duration(ExtensionDurationMillis) * time.Millisecond).String()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the 13-state status mapping (`status_mapping.go`) with full table-driven test coverage BEFORE wiring it into the client</name>
  <files>
    internal/rollouts/status_mapping.go,
    internal/rollouts/status_mapping_test.go
  </files>

  <read_first>
    - internal/rollouts/models.go (the Rollout, StatusBlock, Stage, Event, MetricConfiguration types from Plan 01 — status_mapping must operate against these exact shapes)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-CONTEXT.md <specifics> section (lines 132-148: the canonical 13-state table — authoritative source for label strings)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md section "Status Mapping: 13 UI States → 5 Kinds" (lines ~835+: the table plus the disambiguation rules for `in_progress` sub-conditions and `reverted` sub-conditions)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-PATTERNS.md section "internal/rollouts/status_mapping.go" (no analog; spec lives in RESEARCH.md + CONTEXT.md)
    - internal/output/plaintext_fns_internal_test.go (pattern for table-driven tests in this codebase)
  </read_first>

  <behavior>
    Test (write first):
    - `status_mapping_test.go` is table-driven. The table has at minimum 14 entries: one per row in the 13-state table above, plus an "unknown status" defensive entry. Each entry specifies an input `Rollout` (with `Status.Status` raw value plus enough fields to discriminate sub-conditions) and an expected `(kind, label)` tuple.
    - For `in_progress`, FOUR sub-cases must be tested: (a) guarded with `MetricConfigurations[i].MinSampleSize > 0 && status indicates min not reached` → "for regressions… (not enough data)"; (b) guarded with min reached → "for regressions…"; (c) guarded with `ExtensionDurationMillis != nil && non-zero` → "Monitoring extended by {duration}"; (d) progressive (no metric configs) → "Monitoring {rule}".
    - For `reverted`, THREE sub-cases must be tested: (a) `Events` contains `{Kind: "srm_detected"}` and no regression event → "rolled back automatically"; (b) `Events` contains `{Kind: "regression_detected", MetricKey: "latency-p99"}` → "rolled back automatically after detecting a regression for latency-p99"; (c) `Events` empty or only generic events → "rolled back due to insufficient sample size".
    - Each test asserts `MapStatus(&r).Kind == want.kind` AND `MapStatus(&r).Label == want.label` AND `MapStatus(&r).Status == r.Status.Status` (raw passthrough).
    - Default/fallback case: a raw status not in the documented set returns `kind = "active"` and `label = "Monitoring (unknown status: {raw})"`.

    Implementation (after tests fail):
    - `MapStatus(r *Rollout) StatusBlock` is the single public entry point. It uses `mapStatusToKind(r)` and `formatLabel(r)` internally.
    - `mapStatusToKind` is a switch on `r.Status.Status` returning one of {"active", "regressed", "reverted", "paused", "completed"}.
    - `formatLabel` is a switch on `r.Status.Status` that branches into sub-conditions for `in_progress` and `reverted` per the disambiguation rules.
    - Helpers (unexported): `formatRule(ruleIDOrFallthrough string) string`, `currentAllocationPct(r *Rollout) int`, `formatMetricNames(r *Rollout) string`, `formatDuration(millis int64) string`.
    - `DeriveStatusBlock(r *Rollout) StatusBlock` is an exported alias / convenience for use inside the converter — equivalent to `MapStatus`.
    - The function MUST carry the doc comment `// PAPERCUT: PC-005 — status enum mixes lifecycle + action-required + meta states; this 13 → 5 mapping is the CLI's normalization layer.` (the actual papercut anchor entry is seeded in Plan 03's API-PAPERCUTS.md).
  </behavior>

  <action>
    Write `status_mapping_test.go` first; run it; confirm all entries fail because `status_mapping.go` is empty. Then implement `status_mapping.go` in the order: helpers → `mapStatusToKind` → `formatLabel` → `MapStatus` → `DeriveStatusBlock`. Re-run the test suite until all 14+ table entries pass.

    Use Go's standard library only (no new dependencies). `time.Duration(ms * time.Millisecond).String()` produces the `"15m0s"` / `"1h0m0s"` form expected by AGENT-04 — verify this against the test expectations.

    For the `{N}%` placeholder when a rollout has no stages (defensive), fall back to `"0"` so the label is still well-formed.

    DO NOT yet call `MapStatus` from the converter in `models.go` — Task 2 will wire it in. Plan-02 task 1 strictly delivers the mapping function plus its test.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli &amp;&amp; go test ./internal/rollouts/... -run TestStatusMapping -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - `grep -c "func MapStatus(" internal/rollouts/status_mapping.go` returns 1.
    - `grep -c "func mapStatusToKind(" internal/rollouts/status_mapping.go` returns 1.
    - `grep -c "func formatLabel(" internal/rollouts/status_mapping.go` returns 1.
    - `grep -c "PAPERCUT: PC-005" internal/rollouts/status_mapping.go` returns ≥ 1.
    - All 5 kind values are referenced as string literals: `grep -oE "\"(active|regressed|reverted|paused|completed)\"" internal/rollouts/status_mapping.go | sort -u | wc -l` returns 5.
    - All documented raw statuses appear in the switch (at least once each): for each of `not_started`, `waiting`, `in_progress`, `monitoring_regressed`, `monitoring_stopped`, `srm_stopped`, `completed`, `manually_completed`, `manually_reverted`, `reverted`, `archived`, `grep -c "\"<status>\"" internal/rollouts/status_mapping.go` returns ≥ 1.

    Behavior assertions (test command):
    - `go test ./internal/rollouts/... -run TestStatusMapping -count=1 -v` exits 0 with at least 14 sub-test PASS lines.
    - At least one sub-test name contains each of these substrings: `not_started`, `waiting`, `in_progress`, `monitoring_regressed`, `monitoring_stopped`, `srm_stopped`, `completed`, `manually_completed`, `manually_reverted`, `reverted`, `archived` (asserted via `go test -v` output grep).
    - `MapStatus` produces RAW passthrough for `Status.Status`: a sub-test asserts that for input with `Status.Status = "monitoring_regressed"`, the output `StatusBlock.Status` equals `"monitoring_regressed"` exactly.
  </acceptance_criteria>

  <done>The 13-state mapping is fully implemented and tested with table-driven coverage. Each documented raw status produces the correct `kind` bucket and the correct `label` string per the UI-parity table. The function is not yet called by the converter (Task 2 wires it in). PC-005 papercut comment is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire real HTTP (retryablehttp + DTO conversion + status decoration + error mapping) into RolloutsClient.List and RolloutsClient.Get with httptest.NewServer round-trip tests</name>
  <files>
    internal/rollouts/client.go,
    internal/rollouts/models.go,
    internal/rollouts/errors.go,
    internal/rollouts/client_test.go,
    internal/rollouts/testdata/list_progressive_in_progress.json,
    internal/rollouts/testdata/list_guarded_regressed.json,
    internal/rollouts/testdata/get_guarded_completed.json
  </files>

  <read_first>
    - internal/rollouts/client.go (Plan 01 stub — Task 2 replaces method bodies)
    - internal/rollouts/models.go (Plan 01 DTOs — Task 2 adds raw types + converter funcs alongside)
    - internal/rollouts/errors.go (Plan 01 skeleton — Task 2 fills mapAPIError + mapTransportError)
    - internal/rollouts/status_mapping.go (Task 1's MapStatus — Task 2's converter calls it)
    - internal/resources/client.go lines 46-112 (HTTP request construction + auth header + Content-Type + User-Agent + error-response parsing — analog for the non-retry path; rollouts uses retryablehttp variants)
    - internal/resources/client_test.go (httptest.NewServer pattern: `makeServer(t, statusCode, body)` helper, `defer server.Close()`, `c.MakeRequest(server.URL, ...)`, header + body assertions)
    - internal/errors/errors.go (NewError, NewErrorWrapped, SuggestionForStatus — Task 2 reuses SuggestionForStatus for the nextAction hint where status code matches)
    - internal/errors/suggestions.go (SuggestionForStatus's implemented status codes — to know which 4xx codes have existing operator hints to reuse)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md sections "Retry Layer Wiring" (full code block ~430), "Request flow inside RolloutsClient.List" (~450), "rawRolloutList vs RolloutList" (~508), "error.code Enum & Mapping" (~730), "Mapping function" (~765), "Testing Approach" (~1094)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-PATTERNS.md sections "internal/rollouts/client.go" (especially "Adaptations (significant divergence from analog)"), "internal/rollouts/errors.go", "internal/rollouts/client_test.go"
  </read_first>

  <behavior>
    Tests (write first):
    - `client_test.go` REPLACES Plan 01's sanity test entirely. It defines a `makeServer(t, statusCode, body)` helper (mirror `internal/resources/client_test.go:makeServer`) that returns an `*httptest.Server` whose handler returns the given status + body and records the last received `*http.Request` on a `*sync.Mutex`-guarded shared struct.
    - Required sub-tests (each runs against a fresh httptest.NewServer):
      1. `List returns parsed RolloutList on 200 with valid fixture` — server returns 200 + contents of `testdata/list_progressive_in_progress.json`; assert `len(list.Items) > 0`, assert first item has `Kind == "progressive"`, assert `Status.Kind` is one of the 5 buckets (via Task 1's MapStatus result), assert timestamps are non-zero `time.Time` values.
      2. `List sends correct URL path and query` — server returns 200 + `{"items":[]}`; client.List called with `(projKey="myproj", flagKey="myflag", opts={Environment:"prod", Limit:5})`; assert server received `GET /internal/projects/myproj/flags/myflag/automated-releases?filter=environmentKey%3Aprod&limit=5` (or equivalent URL-encoded form).
      3. `List sends required headers` — server returns 200 + `{"items":[]}`; assert received headers include `Authorization: <token>` (no Bearer prefix), `Content-Type: application/json`, and `User-Agent: launchdarkly-cli/v<version>`. Assert NO `Idempotency-Key` header is set (GET requests don't get one).
      4. `List retries 5xx up to 4 times then succeeds` — server returns 503 on first 2 calls, then 200 + `{"items":[]}`; assert total of exactly 3 requests; assert no error returned.
      5. `List retries 5xx up to 4 times then fails with upstream_unavailable` — server returns 503 on all calls; assert total of 5 requests (1 initial + 4 retries); assert returned error is `*RolloutError` with `Code == ErrCodeUpstreamUnavailable`.
      6. `List does NOT retry 4xx` — server returns 404 once; assert exactly 1 request; assert error.Code == ErrCodeNotFound.
      7. `List maps 401 → unauthorized` — server returns 401 + `{"code":"unauthorized","message":"bad token"}`; assert error.Code == ErrCodeUnauthorized, error.Message non-empty, error.NextAction mentions `access-token` or `login`.
      8. `List maps 403 → forbidden` — server returns 403; assert error.Code == ErrCodeForbidden, error.NextAction mentions role/scope.
      9. `List maps 404 → not_found` — server returns 404; assert error.Code == ErrCodeNotFound, error.NextAction mentions `--flag`/`--project`/`--environment`.
      10. `List maps 429 → rate_limited` — server returns 429; assert error.Code == ErrCodeRateLimited.
      11. `List maps 400 → bad_request` — server returns 400 + body; assert error.Code == ErrCodeBadRequest, error.Message includes the API's message text.
      12. `List converts int64 unix-millis timestamps to RFC 3339 UTC` — fixture has `"createdAt": 1715353200000` (epoch ms); assert that after JSON re-marshaling of the returned Rollout, the createdAt field is `"2024-05-10T15:00:00Z"` or equivalent ISO-8601 UTC. (Use `time.Unix(0, ms*int64(time.Millisecond)).UTC()`.)
      13. `List converts stage durationMillis to Go duration string` — fixture has `"durationMillis": 900000`; assert resulting `Stage.Duration == "15m0s"`.
      14. `List populates Status block via DeriveStatusBlock` — fixture has `"status": "monitoring_regressed"`; assert `Items[0].Status.Status == "monitoring_regressed"`, `Items[0].Status.Kind == "regressed"`, `Items[0].Status.Label` non-empty and matches the expected template from Task 1.
      15. `Get sends correct URL path` — `client.Get(ctx, token, baseURI, "myproj", "myenv", "abc-123")` hits `GET /internal/projects/myproj/environments/myenv/automated-releases/abc-123` (path per RESEARCH.md ARCHITECTURE inventory; Plan 02 task 2 verifies the path against the captured fixture).
    - Each test uses `defer server.Close()` and constructs the client via `NewClient("test")` (passing the test cliVersion).

    Implementation (after tests fail):
    - `internal/rollouts/models.go` gains a parallel set of raw types alongside the existing CLI types:
      - `rawRolloutList struct { Items []rawRollout `json:"items"`; Links map[string]Link `json:"_links,omitempty"` }`
      - `rawRollout struct { ID string `json:"id"`; Kind string `json:"kind"`; Status string `json:"status"`; CreatedAt int64 `json:"createdAt"`; StartedAtMillis *int64 `json:"startedAtMillis,omitempty"`; EndedAtMillis *int64 `json:"endedAtMillis,omitempty"`; LatestStageIndex int `json:"latestStageIndex"`; ExtensionDurationMillis *int64 `json:"extensionDurationMillis,omitempty"`; Stages []rawStage `json:"stages"`; Events []Event `json:"events,omitempty"`; MetricConfigurations []MetricConfiguration `json:"metricConfigurations,omitempty"`; OriginalVariationID string `json:"originalVariationId"`; TargetVariationID string `json:"targetVariationId"`; RandomizationUnit string `json:"randomizationUnit"`; RuleIDOrFallthrough string `json:"ruleIdOrFallthrough"`; EnvironmentID string `json:"environmentId,omitempty"`; EnvironmentKey string `json:"environmentKey,omitempty"`; FlagKey string `json:"flagKey"`; Links map[string]Link `json:"_links,omitempty"` }`
      - `rawStage struct { StageIndex int `json:"stageIndex"`; Allocation int `json:"allocation"`; DurationMillis int64 `json:"durationMillis"`; StartedAtMillis *int64 `json:"startedAtMillis,omitempty"`; SafeRollForward *bool `json:"safeRollForward,omitempty"` }`
      - Converters: `(raw rawRolloutList) toRolloutList() *RolloutList`, `(raw rawRollout) toRollout() Rollout`, `(raw rawStage) toStage() Stage`. The converters convert int64-millis → time.Time via `time.Unix(0, ms*int64(time.Millisecond)).UTC()`, set `Stage.Duration = (time.Duration(DurationMillis)*time.Millisecond).String()`, and call `DeriveStatusBlock(&result)` at the end to populate the nested Status block.
    - `internal/rollouts/client.go` replaces the Plan 01 stub bodies of `List` and `Get` with the full retryablehttp request flow shown in the interfaces section above. Both methods set the same three headers (Authorization, Content-Type, User-Agent). `Get` constructs `GET /internal/projects/{p}/environments/{envKey}/automated-releases/{rolloutID}` (path verified against captured fixture during Task 2). `List` annotates the query construction with `// PAPERCUT: PC-002` above the filter-set line, `// PAPERCUT: PC-003` above the All-branch, `// PAPERCUT: PC-011` above the path construction.
    - `internal/rollouts/errors.go` REPLACES the Plan 01 skeleton `mapAPIError`/`mapTransportError` with the full implementation per RESEARCH.md §"Mapping function" (lines ~765-819). The function attempts a best-effort unmarshal of `{"code":string, "message":string}` from the body; switches on `statusCode` to assign `Code`, `Message`, `NextAction`; uses `errors.SuggestionForStatus(statusCode, baseURI)` (from `internal/errors/suggestions.go`) when applicable for the nextAction hint to keep parity with existing CLI error envelopes. `RawBody` is stored on the struct but the struct's JSON marshaling MUST NOT expose it — either mark `RawBody` with `json:"-"` or do not marshal `RolloutError` directly (the envelope's `EnvelopeError` is what gets marshaled, not `RolloutError`).
    - Three small JSON fixtures land under `internal/rollouts/testdata/` — hand-crafted (not captured live; the API is unstable) but matching the shape RESEARCH.md documents in §"Field Mapping API → CLI". `list_progressive_in_progress.json` has at least one item with `kind:"progressive"`, `status:"in_progress"`, no metricConfigurations, one stage. `list_guarded_regressed.json` has one item with `kind:"guarded"`, `status:"monitoring_regressed"`, two stages, one metricConfiguration with `Status:"regressed"`, one event with `Kind:"regression_detected"`. `get_guarded_completed.json` has a single rollout (not wrapped in `items`) with `kind:"guarded"`, `status:"completed"`.
  </behavior>

  <action>
    Write `client_test.go` first with all 15 sub-tests; run; they will all fail (most fail to compile since the stub doesn't make HTTP calls). Hand-craft the three JSON fixtures under `testdata/`. Then implement: (a) raw types + converters in `models.go`; (b) full `mapAPIError`/`mapTransportError` in `errors.go` per the RESEARCH.md mapping table; (c) real HTTP bodies in `client.go` for `List` and `Get` per the request-flow code block. Re-run tests until green.

    Honor the `Logger = nil` constraint on the retryablehttp client (already set in Plan 01's `newRetryableClient`) — verifies no log spam during retry tests.

    Use `errors.SuggestionForStatus` from `internal/errors/suggestions.go` for the `NextAction` field when it returns a non-empty string, falling back to the RESEARCH.md-specified hints when not.

    Annotate the three Phase 1 papercut sites with `// PAPERCUT: PC-NNN` comments per RESEARCH.md §"Phase 1 papercut annotations in code": PC-002 (filter-element-zero), PC-003 (no-pagination), PC-005 (status enum mixing — already annotated in Task 1), PC-011 (`/internal/` URL prefix), PC-013 (legacy `controlVariationId` if encountered in converter), PC-014 (stage durations only as int64 millis — annotate the `Duration` derivation line). The actual entries get seeded into `.planning/API-PAPERCUTS.md` in Plan 03; Task 2 just plants the source-code anchors.

    Per CLAUDE.md global rule: do NOT use `$()` command substitution in any shell commands.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli &amp;&amp; go test ./internal/rollouts/... -count=1 -v -run '(TestStatusMapping|TestRolloutsClient)'</automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - `grep -c "rawRolloutList" internal/rollouts/models.go` returns ≥ 3 (struct decl + return type on converter + JSON unmarshal target).
    - `grep -c "func (raw rawRolloutList) toRolloutList()" internal/rollouts/models.go` returns 1.
    - `grep -c "func (raw rawRollout) toRollout()" internal/rollouts/models.go` returns 1.
    - `grep -c "func (raw rawStage) toStage()" internal/rollouts/models.go` returns 1.
    - `grep -c "DeriveStatusBlock(" internal/rollouts/models.go` returns ≥ 1 (converter wires status).
    - `grep -c "time.Unix(0,\s*.*\\*int64(time.Millisecond))\\.UTC()" internal/rollouts/models.go` returns ≥ 1.
    - `grep -c "retryablehttp.NewRequestWithContext" internal/rollouts/client.go` returns ≥ 1 (real HTTP path, not stub).
    - `grep -c "c\.httpClient\.Do(req)" internal/rollouts/client.go` returns ≥ 1.
    - `grep -c "mapAPIError(body, resp.StatusCode)" internal/rollouts/client.go` returns ≥ 1.
    - `grep -c "PAPERCUT: PC-002" internal/rollouts/client.go` returns ≥ 1.
    - `grep -c "PAPERCUT: PC-003" internal/rollouts/client.go` returns ≥ 1.
    - `grep -c "PAPERCUT: PC-011" internal/rollouts/client.go` returns ≥ 1.
    - `grep -c "PAPERCUT: PC-014" internal/rollouts/models.go` returns ≥ 1.
    - All ErrCode* constants are referenced in mapAPIError: `grep -oE "ErrCode(Unauthorized|Forbidden|NotFound|Conflict|BadRequest|RateLimited|UpstreamUnavailable|NetworkError|UnknownUpstream)" internal/rollouts/errors.go | sort -u | wc -l` returns 9.
    - `RawBody` is never JSON-tagged in a way that would marshal it: `grep -E "RawBody\s+\[\]byte\s+\`json:\"-\"\`" internal/rollouts/errors.go` returns 1, OR `grep "RawBody" internal/rollouts/errors.go | grep -v 'json:"-"'` returns ZERO json-tagged occurrences.
    - The Plan 01 stub `return &RolloutList{Items: []Rollout{}}, nil` is GONE: `grep -c "Items: \\[\\]Rollout{}" internal/rollouts/client.go` returns 0.

    Behavior assertions (test command):
    - `go test ./internal/rollouts/... -count=1 -v -run '(TestStatusMapping|TestRolloutsClient)'` exits 0; output contains at minimum 15 PASS lines for TestRolloutsClient sub-tests plus the Task 1 status-mapping passes.
    - `go test ./... -count=1 -short` overall regression check stays green (no impact on `cmd/`, other internal packages).
    - The retry sub-tests prove the retry envelope: 5xx-then-200 results in N+1 requests (assert via request counter); 5xx-all-the-way results in exactly 5 requests with terminal error; 4xx results in exactly 1 request.

    CLI assertion (end-to-end against an httptest fixture, executed as a final smoke):
    - With the Plan 03 flag wiring not yet present, the CLI invocation still works (calls `client.List` with default `ListOpts{}`); a separate manual smoke against a captured-fixture httptest.NewServer URL via `--base-uri http://127.0.0.1:NNNN` produces a fully-decorated envelope with real timestamps and real `status.label` strings.
  </acceptance_criteria>

  <done>Real HTTP path is live. The 13 raw API statuses each produce the correct `(kind, label)` tuple via the converter. The retry envelope is exercised by tests. The full `error.code` taxonomy maps every documented HTTP failure mode to a stable enum value. Source-level `// PAPERCUT: PC-NNN` annotations are planted at the six Phase-1-surface workaround sites. Plan 03 takes it from here to wire the user-facing flag surface and seed the papercuts doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ldcli → LaunchDarkly API | TLS-protected outbound HTTP; `Authorization` header carries the access token; reqs may be retried up to 4 times. |
| LaunchDarkly API → ldcli | Untrusted JSON response body; parsed via `encoding/json` into typed raw structs. |
| `RolloutsClient` → `Envelope` consumers | Error body content flows through `mapAPIError` and ends up in `EnvelopeError.Message` / `Details`. Sensitive fragments must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Information disclosure | Access token in retried request URLs | mitigate | `retryablehttp.Client.Logger = nil` (set in Plan 01) ensures the retry layer never logs request URLs/headers. Verify in Plan 02 the constant is preserved: `grep -c "c\\.Logger\\s*=\\s*nil" internal/rollouts/client.go` returns 1. |
| T-02-02 | Information disclosure | Raw upstream body echoed to caller | mitigate | `RolloutError.RawBody` is held on the error struct but is NOT JSON-tagged (`json:"-"` per the acceptance criterion) so it's never marshaled into `EnvelopeError`. Only the curated `Code`, `Message`, `NextAction` are user-facing. The envelope path goes through `NewErrorEnvelope` which reads only those three curated fields. |
| T-02-03 | Information disclosure | Verbose 5xx error messages | mitigate | `mapAPIError` produces a generic `"LaunchDarkly returned 5xx X"` message for 5xx (does NOT echo upstream body for 5xx). 4xx error messages can include the API's `message` field because those are operator-actionable and don't include sensitive infrastructure detail. |
| T-02-04 | Tampering | Untrusted JSON unmarshal | mitigate | `encoding/json.Unmarshal` into typed raw structs with strict field shapes; no `interface{}` or `map[string]any` for response data; unknown fields are silently ignored (Go default), preventing struct-tag-confusion attacks. |
| T-02-05 | Denial of service | Unbounded retry loop | mitigate | `RetryMax=4` (cap), `RetryWaitMax=8s` (cap), `DefaultBackoff` honors `Retry-After`. Total wall time bounded at ~16s. Test #5 and #6 above verify the cap is enforced. |
| T-02-06 | Spoofing | Authorization header construction | accept | Access token is set verbatim into the `Authorization` header (no `Bearer ` prefix — matches `internal/resources/Client` precedent). Existing CLI auth model is unchanged; user-supplied token is trusted by definition. |
| T-02-07 | Repudiation | Idempotency-Key (deferred to Phase 2) | mitigate (deferred) | Wired in Plan 01; not exercised in Phase 1 (no mutations). Plan 02 introduces no mutation. Phase 2 exercises it. |
| T-02-08 | Elevation of privilege | RBAC failures masked | mitigate | 403 maps to `ErrCodeForbidden` with `NextAction = "Verify role includes \"viewProject\" on the target project"`. PC-009 (RBAC errors don't name the missing action) is documented as a known papercut in Plan 03. CLI does not invent privilege — failures propagate. |

No `high` severity threats are unmitigated.
</threat_model>

<verification>
**Status mapping verification:**

```bash
cd /Users/alex/code/launchdarkly/ldcli
go test ./internal/rollouts/... -run TestStatusMapping -count=1 -v
# Expect: all 14+ sub-tests pass, one per documented raw status (plus sub-conditions)
```

**Round-trip HTTP verification:**

```bash
go test ./internal/rollouts/... -run TestRolloutsClient -count=1 -v
# Expect: all 15+ sub-tests pass, including retry-envelope verification and error.code mapping
```

**Regression check:**

```bash
go test ./... -count=1 -short
# Expect: no failures across the rest of the codebase
```

**Architectural check:**

```bash
# Plan 01 stub is gone from production code
grep -c "Items: \[\]Rollout{}" internal/rollouts/client.go    # Expect: 0
# Plan 02 papercut annotations are present
grep -rE "PAPERCUT: PC-(002|003|005|011|014)" internal/rollouts/   # Expect: ≥ 5 lines
```
</verification>

<success_criteria>
- `make test` (or `go test ./... -count=1`) green.
- `internal/rollouts/client_test.go` exercises happy path (200), retry-success (5xx-then-200), retry-exhaustion (all-5xx → `upstream_unavailable`), no-retry-on-4xx, and explicit mapping for 401/403/404/409/400/429 against `httptest.NewServer`.
- `internal/rollouts/status_mapping_test.go` covers all 13 documented raw statuses (with sub-condition discrimination for `in_progress` × 4 and `reverted` × 3) and produces the labels specified in CONTEXT.md `<specifics>` and RESEARCH.md §Status Mapping.
- `RolloutsClient.List` and `RolloutsClient.Get` issue real HTTP requests via the retryablehttp client; the Plan 01 stub return values are gone.
- Timestamps emitted in JSON are RFC 3339 UTC; durations are unit-bearing strings (AGENT-04).
- All Phase-1-surface papercuts have `// PAPERCUT: PC-NNN` source-code anchors (PC-002, PC-003, PC-005, PC-011, PC-014; PC-013 only if encountered in the converter — Task 2 documents whether it was). The actual entries in `.planning/API-PAPERCUTS.md` land in Plan 03.
- Requirement coverage for this plan: FOUND-04 (`error.code` taxonomy in envelope per D-01), FOUND-05 (retry layer wired + tested; idempotency helper wired), FOUND-08 (every error has stable `Code` + `NextAction` where applicable), LIST-02 (per-rollout fields ID/kind/environment/state/variations/timestamps/stage-index all present in DTO + converter), AGENT-03 (mutation idempotency-key plumbing in place for Phase 2; documented in Plan 02 SUMMARY), AGENT-04 (RFC 3339 timestamps + Go-duration strings emitted by converter).
</success_criteria>

<output>
After completion, create `.planning/phases/01-list-foundation-first-end-to-end-slice/01-02-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`.

Capture:
- The actual `Get` URL path used (verify against captured fixture: is it `/environments/{envKey}/automated-releases/{rolloutID}` or different?). Plan 03 needs this confirmed for help-text examples.
- Whether `RawBody` was given `json:"-"` or kept un-tagged (security disposition confirmation).
- Any divergence between the RESEARCH.md-specified `nextAction` strings and what `errors.SuggestionForStatus` already returns (so Plan 03's docs match reality).
- The Plan 01 A2 question (does the API actually return `environmentKey` or only `environmentId`?) — Task 2's fixtures decide what's plausible; Plan 03 records the answer in `API-PAPERCUTS.md` if missing.
</output>
