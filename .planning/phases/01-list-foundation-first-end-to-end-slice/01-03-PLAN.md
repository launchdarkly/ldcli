---
phase: 01-list-foundation-first-end-to-end-slice
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - cmd/cliflags/flags.go
  - cmd/flags/rollouts/flags.go
  - cmd/flags/rollouts/list.go
  - cmd/flags/rollouts/plaintext.go
  - cmd/flags/rollouts/list_test.go
  - cmd/flags/rollouts/rollouts_test.go
  - cmd/cmdtest.go
  - internal/rollouts/client.go
  - .planning/API-PAPERCUTS.md
autonomous: true
mvp_mode: true
requirements:
  - LIST-01
  - LIST-03
  - DOC-01
  - AGENT-01
  - AGENT-02
  - AGENT-05
  - FOUND-02
  - FOUND-07

must_haves:
  truths:
    - "`--environment <key>` filters the list by environment key (D-04 keeps env filter; --state was dropped)"
    - "`--limit N` (default 20) bounds the response; `--all` requests the full history (D-05)"
    - "`--detailed` expands the plaintext rendering but leaves JSON output unchanged (D-06 + D-07)"
    - "`--state` filter is NOT registered as a CLI flag (D-04: dropped from v1)"
    - "`--idempotency-key` is NOT registered as a CLI flag in Phase 1 (deferred to Phase 2 per the discretion call in CONTEXT.md and RESEARCH.md recommendation)"
    - "Rollouts in the response are sorted client-side by CreatedAt DESC, then ID ASC (deterministic tiebreaker — AGENT-05)"
    - "Plaintext default is a 5-column table: ID, KIND, ENVIRONMENT, STATE, STARTED (D-06)"
    - "Plaintext --detailed format expands per-rollout to multi-line records with ID, Kind, Environment, State, Label, Started, Ended, Stage, Target var, Original var, Raw status (D-06)"
    - "When the server returns exactly `limit` items, the envelope's `meta.warnings` includes a saturation hint pointing at PC-003"
    - "`.planning/API-PAPERCUTS.md` exists, follows the documented template, and contains entries PC-001 through PC-016 from the architecture research"
    - "Each Phase 1 source-code workaround site that was annotated in Plan 02 with `// PAPERCUT: PC-NNN` has a corresponding entry in API-PAPERCUTS.md (cross-reference is verified)"
    - "Help text for `list` documents the deterministic sort order (AGENT-05)"
  artifacts:
    - path: "cmd/cliflags/flags.go"
      provides: "New flag-name + description constants: AllFlag, DetailedFlag, LimitFlag"
      contains: "AllFlag\\s*=\\s*\"all\""
    - path: "cmd/flags/rollouts/flags.go"
      provides: "Extended initListFlags registering --environment, --limit (int, default 20), --all (bool), --detailed (bool) in addition to Plan 01's --flag and --project"
    - path: "cmd/flags/rollouts/list.go"
      provides: "runE now reads new flags via Viper, builds full ListOpts, applies client-side sort, surfaces saturation warning in meta.warnings, dispatches plaintext detailed vs default"
    - path: "cmd/flags/rollouts/plaintext.go"
      provides: "Full 5-column table renderer + --detailed multi-record renderer using internal/output/table.go helpers; replaces Plan 01's placeholder"
    - path: "cmd/flags/rollouts/list_test.go"
      provides: "Integration tests via cmd.CallCmd + rollouts.MockClient: plaintext default, JSON, --json shorthand, --detailed, --environment, --limit, --all, error envelope shape, sort order, saturation warning"
    - path: "cmd/flags/rollouts/rollouts_test.go"
      provides: "Banner-suppression tests covering JSON mode suppression and non-TTY suppression"
    - path: "cmd/cmdtest.go"
      provides: "New helper CallCmdWithStderr (or extended CallCmd signature) returning stderr alongside stdout — required because banner writes to stderr"
    - path: "internal/rollouts/client.go"
      provides: "List adds client-side sort (sort.Slice by CreatedAt DESC, ID ASC) and saturation-warning population on meta.warnings when len(items) == requested limit"
    - path: ".planning/API-PAPERCUTS.md"
      provides: "Living papercuts document seeded with PC-001..PC-016 per template in RESEARCH.md §API-PAPERCUTS.md"
      contains: "schemaVersion|Active Index|PC-001|PC-016"
  key_links:
    - from: "cmd/flags/rollouts/list.go (runE)"
      to: "rollouts.ListOpts construction"
      via: "viper.GetString/GetInt/GetBool of the four new flags"
      pattern: "viper\\.GetInt\\(cliflags\\.LimitFlag\\)"
    - from: "cmd/flags/rollouts/list.go"
      to: "RenderRolloutListPlaintext"
      via: "function call with detailed boolean derived from --detailed flag"
      pattern: "RenderRolloutListPlaintext\\(.*detailed.*\\)"
    - from: "internal/rollouts/client.go (List)"
      to: "sort.Slice on RolloutList.Items"
      via: "after successful unmarshal + converter; before return"
      pattern: "sort\\.Slice\\("
    - from: "internal/rollouts/client.go (List)"
      to: "EnvelopeMeta.Warnings"
      via: "saturation check: len(items) >= requestedLimit ⇒ append to meta.warnings"
      pattern: "List returned exactly"
    - from: ".planning/API-PAPERCUTS.md"
      to: "internal/rollouts/client.go + status_mapping.go + models.go (workaround sites)"
      via: "PC-NNN anchor referenced by `// PAPERCUT: PC-NNN` comments in source"
      pattern: "PC-002|PC-003|PC-005|PC-011|PC-014"
---

<objective>
Wire the full user-facing flag surface, plaintext rendering, end-to-end integration
tests, and the seeded `.planning/API-PAPERCUTS.md` deliverable on top of Plan 02's
real HTTP path. After Plan 03, `list` is feature-complete for Phase 1's requirements:
operators can filter by environment, bound the result, request the full history,
expand the plaintext rendering, and get a documented sort order; the JSON envelope is
agent-friendly with `error.code`, warnings, RFC 3339 timestamps; and the papercuts
doc exists with all 16 cataloged entries plus source-code cross-references.

Purpose: Finish Phase 1 against ROADMAP's success criteria. Hand off a working
`ldcli flags rollouts-beta list` that satisfies LIST-01/02/03, AGENT-01/02/05,
FOUND-02/07, DOC-01.

Output: `make test && make build && ./ldcli flags rollouts-beta list --flag X
--project Y` (against any LD API endpoint) returns a deterministically sorted,
fully decorated envelope; help text documents the contract; the papercuts doc
is in place and seeded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-PATTERNS.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-01-SUMMARY.md
@.planning/phases/01-list-foundation-first-end-to-end-slice/01-02-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md

<interfaces>
<!-- From Plans 01 + 02 (already on disk): -->

cmd/cliflags/flags.go existing constants (DO NOT modify the order; only APPEND inside the const block):
```go
const (
    BaseURIDefault      = "https://app.launchdarkly.com"
    // ...
    AccessTokenFlag  = "access-token"
    AnalyticsOptOut  = "analytics-opt-out"
    BaseURIFlag      = "base-uri"
    // ...
    EnvironmentFlag  = "environment"
    FieldsFlag       = "fields"
    FlagFlag         = "flag"
    // Plan 03 appends: AllFlag, DetailedFlag, LimitFlag (alphabetized within block)
    // ... existing descriptions
)
```

cmd/flags/toggle.go initFlags pattern (analog for initListFlags):
```go
func initFlags(cmd *cobra.Command) {
    cmd.Flags().String(cliflags.EnvironmentFlag, "", "The environment key")
    _ = cmd.MarkFlagRequired(cliflags.EnvironmentFlag)
    _ = cmd.Flags().SetAnnotation(cliflags.EnvironmentFlag, "required", []string{"true"})
    _ = viper.BindPFlag(cliflags.EnvironmentFlag, cmd.Flags().Lookup(cliflags.EnvironmentFlag))
    // ... three more required flags
    cmd.Flags().Bool(cliflags.DryRunFlag, false, cliflags.DryRunFlagDescription)
}
```

cmd/cmdtest.go existing CallCmd signature:
```go
func CallCmd(t *testing.T, clients APIClients, trackerFn analytics.TrackerFn, args []string) ([]byte, error)
```
- Returns ONLY stdout (no stderr). Plan 03 adds `CallCmdWithStderr` returning `(stdout, stderr []byte, err error)` so banner tests can assert on stderr without touching the existing 30+ usages of `CallCmd`.

Plan 02-shipped internal/rollouts/client.go:
- Has List + Get with real HTTP; Plan 03 enhances List in two surgical ways: (1) `sort.Slice` over the items returned from `raw.toRolloutList()` by `(CreatedAt DESC, ID ASC)`; (2) detect saturation (`len(items) >= requestedLimit`) and populate `meta.warnings` via the returned envelope construction path. Note: the meta-warnings field lives on `Envelope.Meta`, not on `RolloutList`. Plan 03 introduces a `RolloutList.Warnings []string` carrier on the typed struct OR populates `meta.warnings` only at the envelope-construction site in `cmd/flags/rollouts/list.go`. Choose the envelope-site approach (cleaner; keeps `internal/rollouts/` free of envelope concerns; matches RESEARCH.md §"meta.warnings").

cmd/flags/rollouts/list.go (Plan 01 — Plan 03 enhances runE):
```go
func runE(client rollouts.Client) func(*cobra.Command, []string) error {
    return func(cmd *cobra.Command, args []string) error {
        // Plan 01: reads only --access-token, --base-uri, --project, --flag; builds empty ListOpts.
        // Plan 03: ADDS reads of --environment, --limit, --all, --detailed; builds full ListOpts;
        //          applies saturation warning logic; dispatches plaintext rendering with detailed bool.
    }
}
```

Per AGENT-05, list help text MUST include:
```
Rollouts are returned in reverse-chronological order by createdAt timestamp,
with rollout ID as the deterministic tiebreaker.

By default, the 20 most recent rollouts are returned. Use --all to fetch the
full history (subject to upstream API limits — see API-PAPERCUTS.md PC-003).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the full list-flag surface (`--environment`, `--limit`, `--all`, `--detailed`), client-side sort, saturation warning, and full plaintext rendering — with integration tests via cmd.CallCmd + MockClient</name>
  <files>
    cmd/cliflags/flags.go,
    cmd/flags/rollouts/flags.go,
    cmd/flags/rollouts/list.go,
    cmd/flags/rollouts/plaintext.go,
    cmd/flags/rollouts/list_test.go,
    cmd/flags/rollouts/rollouts_test.go,
    cmd/cmdtest.go,
    internal/rollouts/client.go
  </files>

  <read_first>
    - cmd/cliflags/flags.go (existing const block to append to; existing description-constant convention; existing AllFlagsHelp map — Plan 03 may extend if appropriate)
    - cmd/flags/toggle.go (initFlags pattern: required vs optional flag registration; MarkFlagRequired + SetAnnotation + BindPFlag triple for required; plain `cmd.Flags().X` for optional)
    - cmd/flags/toggle_test.go (analog for cmd.CallCmd-based integration test: assert on stdout content, set up MockClient.On(...).Return(...))
    - cmd/cmdtest.go (existing CallCmd signature; Plan 03 ADDS a sibling `CallCmdWithStderr` — do not modify CallCmd's existing signature because the rest of the codebase depends on it)
    - internal/output/table.go (existing table-rendering helpers; reuse for the 5-col table)
    - internal/output/plaintext_fns.go (per-resource renderer convention)
    - cmd/flags/rollouts/list.go (Plan 01 runE — Plan 03 enhances the same function)
    - cmd/flags/rollouts/plaintext.go (Plan 01 placeholder — Plan 03 replaces with the real renderer)
    - internal/rollouts/client.go (Plan 02 List method — Plan 03 adds sort + saturation detection in the same body, just before `return raw.toRolloutList(), nil`)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md sections "list Command Shape" (flag spec ~922; help text ~937), "Sorting" (~948), "Pagination Strategy" (~963), "Plaintext default" (~972), "Plaintext --detailed" (~984), "Test cases" (~1182)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-PATTERNS.md sections "cmd/flags/rollouts/flags.go", "cmd/flags/rollouts/plaintext.go", "cmd/flags/rollouts/list_test.go", "cmd/flags/rollouts/rollouts_test.go", "cmd/cliflags/flags.go (MODIFY)"
  </read_first>

  <behavior>
    Tests (write first):
    - `cmd/flags/rollouts/list_test.go` exercises the full Phase 1 surface via `cmd.CallCmd(t, APIClients{RolloutsClient: mockClient}, analytics.NoopClientFn{}.Tracker(), args)` (analog: `cmd/flags/toggle_test.go`). Required sub-tests:
      1. `succeeds with plaintext output` — TTY mode (`FORCE_TTY=1` or harness `isTerminal()=true`); mockClient.On(...).Return(non-empty *RolloutList, nil); assert stdout contains the 5 column headers `ID`, `KIND`, `ENVIRONMENT`, `STATE`, `STARTED` (case-insensitive); assert each rollout ID appears on its own row.
      2. `succeeds with JSON output` — `--output json`; assert stdout JSON-decodes to an Envelope with `schemaVersion = "rollouts.v1beta1"`, `kind = "RolloutList"`, `data.items` is a non-empty array, each item has `kind`, nested `status` object with three string fields `status`/`kind`/`label`, RFC-3339 `createdAt`.
      3. `succeeds with --json shorthand` — `--json`; assert same envelope as #2; assert stdout starts with `{`.
      4. `succeeds with --detailed plaintext` — TTY mode + `--detailed`; assert stdout contains the per-record multi-line fields including `Target var:`, `Original var:`, `Raw status:` (per D-06 detailed layout).
      5. `--detailed has no effect on JSON output` — `--output json --detailed`; assert stdout envelope contains the SAME fields as #2 (D-07: full field set always).
      6. `--environment flag passes to ListOpts.Environment` — args include `--environment prod`; assert via mockClient that the most recent call received `ListOpts{Environment: "prod", Limit: 20, All: false}`.
      7. `--limit flag passes to ListOpts.Limit` — args include `--limit 5`; assert `ListOpts{Limit: 5, All: false}`.
      8. `default --limit is 20` — args omit `--limit`; assert `ListOpts{Limit: 20, All: false}`.
      9. `--all overrides --limit` — args include `--all --limit 5`; assert `ListOpts{All: true, Limit: 5}` (Limit field is preserved on opts but the client implementation ignores it when All=true; verify the client's actual upstream limit via Plan 02's test — Plan 03 only verifies the opts shape passed to the mock).
      10. `error from client emits Error envelope and exits non-zero` — mockClient returns `*rollouts.RolloutError{Code: "not_found", Message: "Feature flag not found", NextAction: "Verify --flag value"}` with non-nil err; CallCmd returns non-nil err; if `--output json`, the err's `.Error()` string is parseable JSON with `kind = "Error"` and `error.code = "not_found"`.
      11. `--state flag is NOT recognized (D-04)` — args include `--state running`; CallCmd returns error mentioning `unknown flag --state` or `unrecognized flag`.
      12. `--idempotency-key flag is NOT recognized (deferred to Phase 2)` — args include `--idempotency-key something`; CallCmd returns error mentioning `unknown flag --idempotency-key` or `unrecognized flag`.
      13. `saturation warning emitted when len(items) == limit` — mockClient returns a list with exactly 20 items; assert JSON envelope's `meta.warnings` array contains a string referencing `PC-003` or `truncated`.
      14. `sort order is CreatedAt DESC, ID ASC` — Plan 03 Task 1 adds `sort.Slice` to `internal/rollouts/client.go`'s `List` method, so the **mock returns deliberately unsorted items** and the integration test asserts the **command's emitted envelope** has them in sorted order — proving the sort is applied somewhere in the production path (it lives in the real client; the mock returns the list as-is). Concretely: mockClient returns three items in input order [(id="z", createdAt=T2), (id="b", createdAt=T1), (id="a", createdAt=T1)] where T2 > T1. Assert the JSON envelope's `data.items[]` is [(id="z", T2), (id="a", T1), (id="b", T1)] — i.e., reverse-chronological by createdAt then ascending by id. Because the assertion targets the **command output** (post-sort), this test does NOT need to extend `internal/rollouts/client_test.go`; the sort is verified end-to-end via the integration test alone. (The mock is intentionally a dumb pass-through; if production accidentally bypassed the sort, this test would fail.)
    - `cmd/flags/rollouts/rollouts_test.go` (NEW — banner suppression):
      1. `prints beta banner on TTY when --output is not json` — invoke via `CallCmdWithStderr` with `FORCE_TTY=1` and `--output plaintext`; assert stderr contains the substring `"rollouts-beta is unstable"`.
      2. `suppresses beta banner with --output json` — same invocation but `--output json`; assert stderr does NOT contain that substring.
      3. `suppresses beta banner when stderr is not a TTY` — invoke without FORCE_TTY in test environment (stderr is not a TTY in `go test`); assert stderr does NOT contain that substring even though `--output plaintext`.
    - `cmd/cmdtest.go` adds `func CallCmdWithStderr(t *testing.T, clients APIClients, trackerFn analytics.TrackerFn, args []string) (stdoutBytes, stderrBytes []byte, err error)` — same construction as the existing `CallCmd` (NewRootCommand + isTerminal injection per env-var) but additionally calls `cmd.SetErr(&stderrBuf)` and returns both buffers. Existing `CallCmd` signature is UNCHANGED; the new helper is additive.

    Implementation (after tests fail):
    - `cmd/cliflags/flags.go` appends inside the existing const block (alphabetized): `AllFlag = "all"`, `DetailedFlag = "detailed"`, `LimitFlag = "limit"`. After the existing description constants: `AllFlagDescription = "Return all rollouts (ignores --limit; subject to upstream API limits per PC-003)"`, `DetailedFlagDescription = "Plaintext only: include variations, ended-at, current stage index, raw API status. Ignored in JSON output (always full)."`, `LimitFlagDescription = "Maximum number of rollouts to return (default 20; ignored when --all is set)"`. Extend `AllFlagsHelp` map to include the three new flags (per existing convention).
    - `cmd/flags/rollouts/flags.go` extends `initListFlags`. The four new registrations: (a) `cmd.Flags().String(cliflags.EnvironmentFlag, "", "Filter rollouts by environment key (optional)")` + `viper.BindPFlag` (NOT MarkFlagRequired — optional per D-04). (b) `cmd.Flags().Int(cliflags.LimitFlag, 20, cliflags.LimitFlagDescription)` + `viper.BindPFlag`. (c) `cmd.Flags().Bool(cliflags.AllFlag, false, cliflags.AllFlagDescription)` + `viper.BindPFlag`. (d) `cmd.Flags().Bool(cliflags.DetailedFlag, false, cliflags.DetailedFlagDescription)` + `viper.BindPFlag`.
    - `cmd/flags/rollouts/list.go` updates `runE`: build full `rollouts.ListOpts{Environment: viper.GetString(cliflags.EnvironmentFlag), Limit: viper.GetInt(cliflags.LimitFlag), All: viper.GetBool(cliflags.AllFlag)}`. After `client.List(...)` returns the `*RolloutList`, construct the envelope via `rollouts.NewListEnvelope(list)`, then check saturation: if `!opts.All && opts.Limit > 0 && len(list.Items) >= opts.Limit`, append `fmt.Sprintf("List returned exactly %d items; results may be truncated upstream (see API-PAPERCUTS.md PC-003)", opts.Limit)` to `env.Meta.Warnings`. For dispatch: `detailed := viper.GetBool(cliflags.DetailedFlag)`; if `cliflags.GetOutputKind(cmd) == "json"`, marshal envelope; else call `RenderRolloutListPlaintext(list, detailed)` and print. Also update the cmd's `Long` description to include the AGENT-05 sort-order help text quoted in the `<interfaces>` block above.
    - `cmd/flags/rollouts/plaintext.go` REPLACES Plan 01's placeholder. The default 5-column table uses `internal/output/table.go` helpers (read the file to find the exact function name; if no suitable helper exists, format with `text/tabwriter`). Columns: `ID`, `KIND`, `ENVIRONMENT`, `STATE`, `STARTED`. STATE column shows `r.Status.Kind` (the 5-bucket lifecycle). STARTED shows `r.StartedAt.Format(time.RFC3339)` if non-nil else `r.CreatedAt.Format(time.RFC3339)` (defensive against missing start time). The `--detailed` form is multi-line per record per D-06:
        ```
        ID:           <id>
        Kind:         <kind>
        Environment:  <env>
        State:        <status.kind>
        Label:        <status.label>
        Started:      <startedAt RFC3339 or —>
        Ended:        <endedAt RFC3339 or —>
        Stage:        <latestStageIndex+1> of <len(stages)> (<alloc>%)
        Target var:   <targetVariationId>
        Original var: <originalVariationId>
        Raw status:   <status.status>
        ---
        ```
      Where `<alloc>%` is `Stages[LatestStageIndex].Allocation/1000`.
    - `internal/rollouts/client.go` enhances `List` after the converter call: (1) sort items via `sort.Slice(items, func(i,j int) bool { ... CreatedAt.After(...) || (Equal && ID < ID) ... })`. The sort comparator handles ties via `ID < ID` (deterministic). Place AFTER `raw.toRolloutList()` returns the list, before returning to caller. (2) DO NOT populate `meta.warnings` inside `internal/rollouts/` — that field lives on `Envelope.Meta` which is constructed at the command-handler layer; the typed `RolloutList` stays free of UI concerns. The command handler (cmd/flags/rollouts/list.go) detects saturation by comparing `len(list.Items)` against the limit it requested. Document this split in a code comment on `RolloutsClient.List` to make the layering explicit.
    - `cmd/cmdtest.go` adds `CallCmdWithStderr` as described above (additive; CallCmd untouched).
  </behavior>

  <action>
    Write `cmd/flags/rollouts/list_test.go` first with all 14 sub-tests; write `cmd/flags/rollouts/rollouts_test.go` with the 3 banner-suppression tests; run; they all fail (mostly compile errors because the new flags don't exist yet). Add `CallCmdWithStderr` to `cmd/cmdtest.go`. Append the three new constants to `cmd/cliflags/flags.go` inside the existing const block (alphabetize within it). Extend `cmd/flags/rollouts/flags.go` initListFlags to register the four new flags. Update `cmd/flags/rollouts/list.go` runE to read the new flags via Viper, build full ListOpts, populate saturation warning at envelope-construction time, dispatch plaintext detailed vs default. Replace `cmd/flags/rollouts/plaintext.go` with the real 5-col + detailed renderer (use `internal/output/table.go` helpers if they exist; otherwise use `text/tabwriter`). Add the `sort.Slice` call to `internal/rollouts/client.go`'s `List` method (this is the **first time** the sort appears in the production path — Plan 02 ships the unsorted List; Plan 03 Task 1 introduces deterministic ordering). Re-run tests until all pass.

    Per CLAUDE.md global rule: avoid `$()` command substitution. The verify command below uses straight commands.

    A note on the "—" character in detailed plaintext output: use the literal em-dash `—` (U+2014). The CLI's terminal output already uses U+26A0 in the banner; em-dash is similarly fine. If a test asserts on the exact string, use the actual Unicode character in the assertion.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli &amp;&amp; go build ./... &amp;&amp; go test ./... -count=1 -short -v -run '(TestList|TestRollouts|TestStatusMapping)' &amp;&amp; ./ldcli flags rollouts-beta list --help 2&gt;&amp;1 | grep -q 'reverse-chronological order' &amp;&amp; ./ldcli flags rollouts-beta list --help 2&gt;&amp;1 | grep -q -E '\-\-(limit|all|detailed|environment)' &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - `grep -c 'AllFlag\s*=\s*"all"' cmd/cliflags/flags.go` returns 1.
    - `grep -c 'DetailedFlag\s*=\s*"detailed"' cmd/cliflags/flags.go` returns 1.
    - `grep -c 'LimitFlag\s*=\s*"limit"' cmd/cliflags/flags.go` returns 1.
    - `grep -c 'cliflags\.AllFlag' cmd/flags/rollouts/flags.go` returns ≥ 1.
    - `grep -c 'cliflags\.LimitFlag' cmd/flags/rollouts/flags.go` returns ≥ 1.
    - `grep -c 'cliflags\.DetailedFlag' cmd/flags/rollouts/flags.go` returns ≥ 1.
    - `grep -c 'cliflags\.EnvironmentFlag' cmd/flags/rollouts/flags.go` returns ≥ 1.
    - The `--state` flag is NOT registered (D-04): `grep -rcE 'cliflags\.StateFlag|"\\-\\-state"' cmd/flags/rollouts/` returns 0.
    - The `--idempotency-key` flag is NOT registered (deferred): `grep -rcE 'idempotency-key' cmd/flags/rollouts/` returns 0.
    - `grep -c 'sort\.Slice' internal/rollouts/client.go` returns ≥ 1 (sort in List).
    - `grep -c 'reverse-chronological order' cmd/flags/rollouts/list.go` returns ≥ 1 (sort doc in help text).
    - `grep -c 'PC-003' cmd/flags/rollouts/list.go` returns ≥ 1 (saturation warning references the papercut anchor).
    - `grep -c 'func CallCmdWithStderr' cmd/cmdtest.go` returns 1 (new helper added; CallCmd signature unchanged: `git diff cmd/cmdtest.go | grep -c '^-func CallCmd('` returns 0).

    Behavior assertions (test commands):
    - `go test ./cmd/flags/rollouts/... -count=1 -v` exits 0 with ALL listed sub-tests PASS.
    - `go test ./... -count=1 -short` is green (no regression elsewhere).
    - `./ldcli flags rollouts-beta list --help` exit 0; output contains `--environment`, `--limit`, `--all`, `--detailed`, AND the substring `reverse-chronological order`.
    - `./ldcli flags rollouts-beta list --state foo --flag x --project y --access-token t` exits non-zero with stderr (or error envelope) mentioning `unknown flag` or `unrecognized flag` for `--state`.
    - `./ldcli flags rollouts-beta list --idempotency-key abc --flag x --project y --access-token t` exits non-zero similarly.

    CLI assertion:
    - `./ldcli flags rollouts-beta list --flag x --project y --access-token t --output json --base-uri https://invalid.example` exits non-zero, stdout JSON contains `"kind":"Error"` and `"error":{"code":"<network_error|upstream_unavailable|not_found|...>"`. (The exact code depends on what the invalid host returns; the assertion is that a structured Error envelope is emitted on stdout, not a generic "request failed".)
  </acceptance_criteria>

  <done>The list command is feature-complete for Phase 1: `--environment`/`--limit`/`--all`/`--detailed` are wired with Viper, the 5-col table + detailed multi-record plaintext renderer work, sort is deterministic, saturation warning emits via `meta.warnings`, help text documents the sort order, banner suppression behaves correctly across all four (TTY×output-kind) combinations, and the integration test suite proves all 14 surface behaviors. `--state` and `--idempotency-key` remain off-surface per D-04 and the deferred-feature decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create `.planning/API-PAPERCUTS.md` seeded with PC-001..PC-016 from architecture research; verify all source-code `// PAPERCUT: PC-NNN` annotations cross-reference real entries</name>
  <files>
    .planning/API-PAPERCUTS.md
  </files>

  <read_first>
    - .planning/research/ARCHITECTURE.md (the 16 papercuts P1..P16 with full text per RESEARCH.md citation; this is the source-of-truth for the seeded content)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md section "API-PAPERCUTS.md: Seeded Content" (~1016: the full template structure with the Active Index table and per-entry format)
    - .planning/phases/01-list-foundation-first-end-to-end-slice/01-RESEARCH.md "Phase 1 papercut annotations in code" (~1079: which papercuts are annotated in Phase 1 source code — the cross-reference must hold)
    - internal/rollouts/client.go, status_mapping.go, models.go (verify which PC-NNN comments Plan 02 actually planted — Task 2 must cross-reference real comments to real entries)
  </read_first>

  <behavior>
    - Create `.planning/API-PAPERCUTS.md` at the repo root's `.planning/` directory (not in the phase subdirectory — it's a milestone-level deliverable per ROADMAP.md cross-cutting constraint DOC-02 and CONTEXT.md).
    - Use the template from RESEARCH.md §"API-PAPERCUTS.md: Seeded Content" verbatim. The doc starts with a brief preface, then has these sections in order:
      1. Title `# API Papercuts: gonfalon automated-releases`
      2. Living-doc disclaimer (1-2 lines)
      3. Metadata block: `Last updated`, `Active count: 16`, `Resolved count: 0`
      4. `## Active Index` — a table with columns `Anchor | One-line | Discovered | Affected commands` and exactly 16 rows (PC-001..PC-016) pulled from RESEARCH.md's table on lines ~1037-1052.
      5. `## Entries` — sixteen `### PC-NNN — <title>` sections, each with the structured fields `Title`, `Discovered`, `API behavior`, `CLI workaround`, `What we'd prefer`, `Status`, `Removal criteria`. Full text per ARCHITECTURE.md's papercut catalog; copy the existing detail for PC-001 from RESEARCH.md §"API-PAPERCUTS.md: Seeded Content" (lines ~1056-1064 in the RESEARCH.md file). Each entry is roughly 8-12 lines.
      6. `## Resolved` section — empty (literally `*(empty)*`).
    - If Plan 02's SUMMARY recorded that `environmentKey` is missing from the API response (the A2 question), append a `PC-017 — environmentKey absent from list response` entry to both the Active Index table and the Entries section; the corresponding source-code annotation (added in Plan 02 or Task 1 of Plan 03 at the converter or env-derivation site) must reference PC-017. If A2 came back as "present", skip this.
    - Cross-reference integrity: every `// PAPERCUT: PC-NNN` comment in the Phase 1 source tree must point to a real entry in the doc.
  </behavior>

  <action>
    Create `.planning/API-PAPERCUTS.md` using the Write tool (not heredoc). Copy the template structure from RESEARCH.md §"API-PAPERCUTS.md: Seeded Content". For each PC-NNN entry, populate the body from `.planning/research/ARCHITECTURE.md`'s papercut catalog. Use the one-line summaries verbatim from the index table on lines ~1037-1052 of RESEARCH.md.

    The doc is checked in as a first-class deliverable per ROADMAP DOC-02 cross-cutting constraint. Per CLAUDE.md, no emojis in the doc unless explicitly required (the U+26A0 banner character lives in source code, not in this markdown). Use ASCII only in the doc body.

    After writing, run the cross-reference integrity check via grep (see acceptance criteria). If any `PC-NNN` is referenced in source but missing from the doc, add the missing entry; if any doc entry has no source-code reference (PC-001, PC-004, PC-007, PC-008, PC-009, PC-010, PC-012, PC-015, PC-016 are expected to have NO Phase 1 source-code annotation per RESEARCH.md — they're for Phase 2-4), that's fine; the doc entry stands as a forward-looking placeholder.
  </action>

  <verify>
    <automated>cd /Users/alex/code/launchdarkly/ldcli &amp;&amp; test -f .planning/API-PAPERCUTS.md &amp;&amp; grep -c "^### PC-" .planning/API-PAPERCUTS.md &amp;&amp; for PC in PC-001 PC-002 PC-003 PC-004 PC-005 PC-006 PC-007 PC-008 PC-009 PC-010 PC-011 PC-012 PC-013 PC-014 PC-015 PC-016; do grep -q "$PC" .planning/API-PAPERCUTS.md || { echo "missing: $PC"; exit 1; }; done &amp;&amp; for PC in $(grep -rohE 'PC-[0-9]+' internal/rollouts/ cmd/flags/rollouts/ | sort -u); do grep -q "^### $PC" .planning/API-PAPERCUTS.md || { echo "source references $PC but doc has no entry"; exit 1; }; done &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - `test -f .planning/API-PAPERCUTS.md` (the file exists at the planning root).
    - `grep -cE "^### PC-(0[0-9]{2}|0[1-9][0-9])" .planning/API-PAPERCUTS.md` returns ≥ 16 (one section per papercut; A2 case may add PC-017).
    - The Active Index table has at least 16 rows: `grep -cE "^\| PC-(00[1-9]|01[0-6])" .planning/API-PAPERCUTS.md` returns 16.
    - `grep -c "Active count: 16" .planning/API-PAPERCUTS.md` returns 1 (or "17" if A2 surfaced a new papercut).
    - `grep -c "Resolved count: 0" .planning/API-PAPERCUTS.md` returns 1.
    - Each section has the required structured fields: `grep -cE "(Title|Discovered|API behavior|CLI workaround|What we'd prefer|Status|Removal criteria)" .planning/API-PAPERCUTS.md` returns ≥ 96 (16 × 6 fields, allowing for minor variation).

    Cross-reference assertions:
    - For every `// PAPERCUT: PC-NNN` source-code anchor planted in Plan 02, the doc has a matching `### PC-NNN` entry. Verified by the bash loop in `<verify>`.
    - The doc references this milestone explicitly: `grep -c "Phase 1\|milestone" .planning/API-PAPERCUTS.md` returns ≥ 1.

    Behavior assertions:
    - No CI failure introduced by adding a new file in `.planning/`: `go test ./... -count=1 -short` stays green.
    - The doc is checked into git: `git status --short .planning/API-PAPERCUTS.md` shows the file as either modified or new (no "untracked" warning ahead of commit; Plan 03's commit picks it up).
  </acceptance_criteria>

  <done>`.planning/API-PAPERCUTS.md` is in place, follows the documented template, contains entries for all 16 cataloged papercuts (plus optional PC-017 if A2 surfaced one), and every `// PAPERCUT: PC-NNN` annotation Plan 02 planted in source cross-references a real entry. DOC-01 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user CLI args → Viper → rollouts.ListOpts | Untrusted strings; flow into URL query construction via `url.Values.Set` (already URL-encoded). |
| `meta.warnings` content → stdout consumer | CLI-generated string; no user input echoed. |
| `.planning/API-PAPERCUTS.md` → git history | Internal doc; no secrets. |
| plaintext renderer → stdout | Rollout data fields formatted into table rows; data is already trusted (from upstream API). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Injection | URL query construction in `RolloutsClient.List` (env filter) | mitigate | `url.Values.Set` and `q.Encode()` URL-encode automatically; no manual concatenation. Plan 02 already does this; Plan 03 doesn't change it. Verify (regression check): `grep -E 'path \+ "\?.*\+ opts\.Environment' internal/rollouts/client.go` returns ZERO (no string concat of user-supplied env directly into URL). |
| T-03-02 | Injection | Plaintext renderer fields | mitigate | All rendered fields are either typed primitives (int allocations, time.Time timestamps) or strings the API itself produced. The CLI does not echo user-supplied `--flag`/`--project`/`--environment` into output rows (those control the API call, not the output content). |
| T-03-03 | Information disclosure | `meta.warnings` strings | mitigate | Plan 03 introduces exactly one warning string format: `"List returned exactly %d items; results may be truncated upstream (see API-PAPERCUTS.md PC-003)"`. The `%d` is `opts.Limit` (an int) — no user-supplied string interpolated. |
| T-03-04 | Tampering | `.planning/API-PAPERCUTS.md` content | accept | Doc is internal to the repo, not loaded at runtime. Tampering risk = git history risk only. |
| T-03-05 | Denial of service | `--all` with huge `limit=1000` upstream | mitigate | `--all` requests `limit=1000` per RESEARCH.md; transparent multi-call pagination is deferred (Papercut P3). Saturation warning informs operators when the cap was hit. Retry envelope still caps total wall time at ~16s. |
| T-03-06 | Repudiation | New `analytics` event names introduced | accept | Plan 03 does NOT add new analytics events beyond what Plan 01 wired (`flags-rollouts-beta` with `action=cmd.Name()`); existing infrastructure honors `analytics-opt-out`. |
| T-03-07 | Spoofing | Test harness `CallCmdWithStderr` | accept | Test-only helper; not built into production binary. Additive; existing `CallCmd` signature unchanged. |

No `high` severity threats are unmitigated.
</threat_model>

<verification>
**End-to-end:**

```bash
cd /Users/alex/code/launchdarkly/ldcli
make test
make build

# Help text documents sort order (AGENT-05)
./ldcli flags rollouts-beta list --help | grep -E 'reverse-chronological|deterministic tiebreaker'

# All four new flags are registered
./ldcli flags rollouts-beta list --help | grep -E '\-\-(environment|limit|all|detailed)'

# Dropped flags are NOT registered (D-04 + Phase 2 deferral)
./ldcli flags rollouts-beta list --help | grep -E '\-\-(state|idempotency-key)' && echo "FAIL: forbidden flag present" || echo "OK: forbidden flags absent"
```

**Papercuts doc integrity:**

```bash
# Doc exists and is seeded
test -f .planning/API-PAPERCUTS.md
grep -cE '^### PC-' .planning/API-PAPERCUTS.md      # Expect: ≥ 16
grep -c 'Active count: 1[67]' .planning/API-PAPERCUTS.md    # Expect: 1 (16 or 17 if A2 added PC-017)

# Every source-code annotation cross-references a doc entry
for PC in $(grep -rohE 'PC-[0-9]+' internal/rollouts/ cmd/flags/rollouts/ | sort -u); do
  grep -q "^### ${PC}" .planning/API-PAPERCUTS.md && echo "${PC} OK" || echo "${PC} MISSING"
done
```

**Regression:**

```bash
go test ./... -count=1 -short
```
</verification>

<success_criteria>
- `make test && make build` succeed; the full ldcli test suite is green; no regressions in `cmd/flags/`, `cmd/dev_server/`, `internal/*/` outside the rollouts subtree.
- `./ldcli flags rollouts-beta list --flag X --project Y --access-token T` invokes successfully (against any reachable LD API endpoint or httptest stub) and produces an envelope with all the LIST-02 fields, sorted deterministically, with saturation warning emitted when applicable.
- `--environment`, `--limit`, `--all`, `--detailed` are all wired (LIST-03); `--state` is NOT wired (D-04 dropped from v1); `--idempotency-key` is NOT wired (deferred to Phase 2 per discretion).
- Help text documents the deterministic sort order (AGENT-05) and the saturation/pagination behavior (LIST-03 + PC-003).
- Beta banner emits correctly on TTY non-JSON, suppresses correctly on JSON or non-TTY stderr (FOUND-02 + FOUND-07; verified by `cmd/flags/rollouts/rollouts_test.go`).
- `.planning/API-PAPERCUTS.md` exists, is seeded with PC-001..PC-016 (plus optional PC-017 from A2 outcome), follows the documented template, and every `// PAPERCUT: PC-NNN` source-code annotation Plan 02 planted is cross-referenced (DOC-01).
- Cross-cutting requirements re-validated for Phase 1: AGENT-01 (every command supports `--output json` — inherited from root persistent flag), AGENT-02 (exit 1 for any error per D-01, `error.code` taxonomy in envelope per FOUND-04 reframed), AGENT-05 (deterministic sort documented in help).
- Phase 1 acceptance from ROADMAP §"Success Criteria" 1-5 all hold: list returns enumeration with stable ordering; JSON envelope is well-formed and ANSI-free; `--environment` filter works; agent can branch on `error.code`; API-PAPERCUTS.md is seeded.
</success_criteria>

<output>
After completion, create `.planning/phases/01-list-foundation-first-end-to-end-slice/01-03-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`.

Capture:
- Outcome of the A2 investigation (was `environmentKey` present in the API response? did PC-017 get added?).
- Whether `internal/output/table.go` had a reusable helper or `text/tabwriter` was the fallback (so subsequent phases know what pattern to mirror for `status` / `watch` plaintext).
- Any decision deferred to Phase 2 that surfaced during ergonomics work (e.g., should `--limit 0` mean "unbounded" or be rejected? what did the test resolve?).
- Confirmation that all 18 requirement IDs in Phase 1 (FOUND-01..08, DOC-01, LIST-01..03, AGENT-01..05) are reflected in source or tests, ready for the phase-verification step.
- A short list of new papercuts (if any) discovered while implementing Plans 01/02/03 — DOC-02 cross-cutting constraint says these get appended to API-PAPERCUTS.md, not deferred.
</output>
