id = "ldcli__scheduled-changes__get"
argv = ["scheduled-changes", "get", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__scheduled-changes__get.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
