id = "ldcli__data-export-destinations__create-generate-trust-policy"
argv = ["data-export-destinations", "create-generate-trust-policy", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__data-export-destinations__create-generate-trust-policy.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
