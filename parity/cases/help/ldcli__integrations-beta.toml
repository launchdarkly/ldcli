id = "ldcli__integrations-beta"
argv = ["integrations-beta", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integrations-beta.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
