id = "ldcli__experiments"
argv = ["experiments", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__experiments.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
