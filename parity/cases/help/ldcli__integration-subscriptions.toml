id = "ldcli__integration-subscriptions"
argv = ["integration-subscriptions", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-subscriptions.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
