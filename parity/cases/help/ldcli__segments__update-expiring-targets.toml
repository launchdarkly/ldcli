id = "ldcli__segments__update-expiring-targets"
argv = ["segments", "update-expiring-targets", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__segments__update-expiring-targets.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
