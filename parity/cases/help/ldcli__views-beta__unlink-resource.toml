id = "ldcli__views-beta__unlink-resource"
argv = ["views-beta", "unlink-resource", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__views-beta__unlink-resource.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
