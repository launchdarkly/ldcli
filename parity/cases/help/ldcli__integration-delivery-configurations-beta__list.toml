id = "ldcli__integration-delivery-configurations-beta__list"
argv = ["integration-delivery-configurations-beta", "list", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-delivery-configurations-beta__list.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
