id = "ldcli__access-tokens"
argv = ["access-tokens", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__access-tokens.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
