id = "ldcli__teams__update"
argv = ["teams", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__teams__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
