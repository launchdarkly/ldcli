id = "ldcli__releases-beta__update-phase-status"
argv = ["releases-beta", "update-phase-status", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__releases-beta__update-phase-status.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
