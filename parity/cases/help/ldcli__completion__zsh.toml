id = "ldcli__completion__zsh"
argv = ["completion", "zsh", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__completion__zsh.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
