id = "ldcli__integrations-beta__get-configuration"
argv = ["integrations-beta", "get-configuration", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integrations-beta__get-configuration.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
