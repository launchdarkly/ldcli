id = "ldcli__release-pipelines-beta__list"
argv = ["release-pipelines-beta", "list", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__release-pipelines-beta__list.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
