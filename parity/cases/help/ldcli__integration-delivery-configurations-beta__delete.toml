id = "ldcli__integration-delivery-configurations-beta__delete"
argv = ["integration-delivery-configurations-beta", "delete", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-delivery-configurations-beta__delete.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
