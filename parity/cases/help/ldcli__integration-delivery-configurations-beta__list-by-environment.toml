id = "ldcli__integration-delivery-configurations-beta__list-by-environment"
argv = ["integration-delivery-configurations-beta", "list-by-environment", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-delivery-configurations-beta__list-by-environment.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
