id = "ldcli__layers__update"
argv = ["layers", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__layers__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
