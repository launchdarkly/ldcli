id = "ldcli__layers__create"
argv = ["layers", "create", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__layers__create.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
