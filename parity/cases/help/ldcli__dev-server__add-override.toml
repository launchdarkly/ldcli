id = "ldcli__dev-server__add-override"
argv = ["dev-server", "add-override", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__add-override.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
