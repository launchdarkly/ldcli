id = "ldcli__environments__update"
argv = ["environments", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__environments__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
