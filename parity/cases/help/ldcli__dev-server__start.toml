id = "ldcli__dev-server__start"
argv = ["dev-server", "start", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__start.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
