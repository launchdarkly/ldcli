id = "ldcli__metric-groups-beta"
argv = ["metric-groups-beta", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__metric-groups-beta.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
