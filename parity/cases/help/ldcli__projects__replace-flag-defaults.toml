id = "ldcli__projects__replace-flag-defaults"
argv = ["projects", "replace-flag-defaults", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__projects__replace-flag-defaults.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
