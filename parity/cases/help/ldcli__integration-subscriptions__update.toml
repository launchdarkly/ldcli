id = "ldcli__integration-subscriptions__update"
argv = ["integration-subscriptions", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-subscriptions__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
