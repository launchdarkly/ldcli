id = "ldcli__dev-server__list-projects"
argv = ["dev-server", "list-projects", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__list-projects.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
