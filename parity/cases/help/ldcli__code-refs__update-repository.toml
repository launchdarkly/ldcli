id = "ldcli__code-refs__update-repository"
argv = ["code-refs", "update-repository", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__code-refs__update-repository.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
