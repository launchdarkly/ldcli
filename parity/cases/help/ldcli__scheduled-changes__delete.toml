id = "ldcli__scheduled-changes__delete"
argv = ["scheduled-changes", "delete", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__scheduled-changes__delete.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
