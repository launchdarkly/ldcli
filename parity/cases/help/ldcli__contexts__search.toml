id = "ldcli__contexts__search"
argv = ["contexts", "search", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__contexts__search.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
