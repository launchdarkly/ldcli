id = "ldcli__follow-flags"
argv = ["follow-flags", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__follow-flags.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
