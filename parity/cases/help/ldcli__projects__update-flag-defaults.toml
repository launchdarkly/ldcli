id = "ldcli__projects__update-flag-defaults"
argv = ["projects", "update-flag-defaults", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__projects__update-flag-defaults.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
