id = "ldcli__follow-flags__list"
argv = ["follow-flags", "list", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__follow-flags__list.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
