id = "ldcli__dev-server"
argv = ["dev-server", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
