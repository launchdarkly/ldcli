id = "ldcli__scheduled-changes__update"
argv = ["scheduled-changes", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__scheduled-changes__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
