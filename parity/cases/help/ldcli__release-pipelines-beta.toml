id = "ldcli__release-pipelines-beta"
argv = ["release-pipelines-beta", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__release-pipelines-beta.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
