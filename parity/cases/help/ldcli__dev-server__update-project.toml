id = "ldcli__dev-server__update-project"
argv = ["dev-server", "update-project", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__update-project.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
