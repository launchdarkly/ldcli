id = "ldcli__integration-delivery-configurations-beta__validate"
argv = ["integration-delivery-configurations-beta", "validate", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-delivery-configurations-beta__validate.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
