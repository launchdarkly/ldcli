id = "ldcli__symbols__generate"
argv = ["symbols", "generate", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__symbols__generate.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
