id = "ldcli__applications-beta__delete-version"
argv = ["applications-beta", "delete-version", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__applications-beta__delete-version.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
