id = "ldcli__webhooks"
argv = ["webhooks", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__webhooks.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
