id = "ldcli__insights-deployments-beta__create-deployment-event"
argv = ["insights-deployments-beta", "create-deployment-event", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__insights-deployments-beta__create-deployment-event.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
