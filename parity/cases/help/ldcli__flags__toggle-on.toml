id = "ldcli__flags__toggle-on"
argv = ["flags", "toggle-on", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__flags__toggle-on.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
