id = "ldcli__contexts__replace-kind"
argv = ["contexts", "replace-kind", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__contexts__replace-kind.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
