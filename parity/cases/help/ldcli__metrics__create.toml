id = "ldcli__metrics__create"
argv = ["metrics", "create", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__metrics__create.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
