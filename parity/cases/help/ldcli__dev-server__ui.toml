id = "ldcli__dev-server__ui"
argv = ["dev-server", "ui", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__ui.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
