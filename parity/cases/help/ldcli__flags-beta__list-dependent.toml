id = "ldcli__flags-beta__list-dependent"
argv = ["flags-beta", "list-dependent", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__flags-beta__list-dependent.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
