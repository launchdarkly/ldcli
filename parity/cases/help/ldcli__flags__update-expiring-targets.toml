id = "ldcli__flags__update-expiring-targets"
argv = ["flags", "update-expiring-targets", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__flags__update-expiring-targets.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
