id = "ldcli__experiments__replace-settings"
argv = ["experiments", "replace-settings", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__experiments__replace-settings.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
