id = "ldcli__dev-server__remove-project"
argv = ["dev-server", "remove-project", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__remove-project.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
