id = "ldcli__contexts__delete-instances"
argv = ["contexts", "delete-instances", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__contexts__delete-instances.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
