id = "ldcli__releases-beta"
argv = ["releases-beta", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__releases-beta.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
