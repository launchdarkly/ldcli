id = "ldcli__releases-beta__update"
argv = ["releases-beta", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__releases-beta__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
