id = "ldcli__integration-delivery-configurations-beta"
argv = ["integration-delivery-configurations-beta", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-delivery-configurations-beta.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
