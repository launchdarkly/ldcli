id = "ldcli__release-pipelines-beta__replace"
argv = ["release-pipelines-beta", "replace", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__release-pipelines-beta__replace.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
