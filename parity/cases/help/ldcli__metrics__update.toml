id = "ldcli__metrics__update"
argv = ["metrics", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__metrics__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
