id = "ldcli__integration-subscriptions__get"
argv = ["integration-subscriptions", "get", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-subscriptions__get.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
