id = "ldcli__integration-delivery-configurations-beta__get"
argv = ["integration-delivery-configurations-beta", "get", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__integration-delivery-configurations-beta__get.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
