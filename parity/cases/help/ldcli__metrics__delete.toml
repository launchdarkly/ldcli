id = "ldcli__metrics__delete"
argv = ["metrics", "delete", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__metrics__delete.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
