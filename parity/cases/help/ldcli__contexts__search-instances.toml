id = "ldcli__contexts__search-instances"
argv = ["contexts", "search-instances", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__contexts__search-instances.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
