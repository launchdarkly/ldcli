id = "ldcli__scheduled-changes"
argv = ["scheduled-changes", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__scheduled-changes.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
