id = "ldcli__dev-server__add-project"
argv = ["dev-server", "add-project", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__add-project.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
