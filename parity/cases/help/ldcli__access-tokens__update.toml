id = "ldcli__access-tokens__update"
argv = ["access-tokens", "update", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__access-tokens__update.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
