id = "ldcli__layers__list"
argv = ["layers", "list", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__layers__list.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
