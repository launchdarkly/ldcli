id = "ldcli__access-tokens__list"
argv = ["access-tokens", "list", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__access-tokens__list.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
