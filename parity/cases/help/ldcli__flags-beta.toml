id = "ldcli__flags-beta"
argv = ["flags-beta", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__flags-beta.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
