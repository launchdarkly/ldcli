id = "ldcli__audit-log"
argv = ["audit-log", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__audit-log.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
