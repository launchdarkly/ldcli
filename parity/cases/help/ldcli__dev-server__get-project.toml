id = "ldcli__dev-server__get-project"
argv = ["dev-server", "get-project", "--help"]
declare = ["config:ldcli/config.yml"]

[expect]
status = 0
stdout_file = "ldcli__dev-server__get-project.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
