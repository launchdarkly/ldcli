id = "config-yaml-quoting-null"
argv = ["config", "--set", "project", "null", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-null.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \"null\"\n"
