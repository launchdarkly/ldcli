id = "config-set-two-keys-keeps-others"
argv = ["config", "--set", "project", "p2", "environment", "prod", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[seed]
"config:ldcli/config.yml" = "output: markdown\naccess-token: api-not-a-real-token\nproject: proj\n"

[expect]
status = 0
stdout_file = "config-set-two-keys-keeps-others.stdout"

[expect.files]
"config:ldcli/config.yml" = "access-token: api-not-a-real-token\nenvironment: prod\noutput: markdown\nproject: p2\n"
