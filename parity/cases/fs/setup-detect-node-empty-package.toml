id = "setup-detect-node-empty-package"
argv = ["setup", "detect"]
declare = ["config:ldcli/config.yml", "work:package.json"]
rust = true

[seed]
"work:package.json" = "{}\n"

[expect]
status = 0
stdout_file = "setup-detect-node-empty-package.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
"work:package.json" = "{}\n"
