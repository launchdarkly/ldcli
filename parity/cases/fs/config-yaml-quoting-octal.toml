id = "config-yaml-quoting-octal"
argv = ["config", "--set", "project", "0o17", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-octal.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \"0o17\"\n"
