id = "config-list-empty"
argv = ["config", "--list"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-list-empty.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
