id = "config-help-flag"
argv = ["config", "--help"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-help-flag.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
