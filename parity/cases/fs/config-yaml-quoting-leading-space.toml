id = "config-yaml-quoting-leading-space"
argv = ["config", "--set", "project", " lead", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-leading-space.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: ' lead'\n"
