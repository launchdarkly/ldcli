id = "setup-install-manual-sdk-plaintext"
argv = ["setup", "install", "--sdk-id", "swift-client-sdk", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "setup-install-manual-sdk-plaintext.stdout"
stderr_file = "setup-install-manual-sdk-plaintext.stderr"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
