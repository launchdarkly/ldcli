id = "config-yaml-quoting-hex"
argv = ["config", "--set", "project", "0x10", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-hex.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \"0x10\"\n"
