id = "setup-install-pip-warns-when-requirements-is-a-directory"
argv = ["setup", "install", "--sdk-id", "python-server-sdk", "--package-manager", "pip"]
declare = ["config:ldcli/config.yml", "work:requirements.txt/keep"]
rust = true

[seed]
"bin:pip3" = "#!/bin/sh\necho ok\n"
"work:requirements.txt/keep" = ""

[expect]
status = 0
stdout_file = "setup-install-pip-warns-when-requirements-is-a-directory.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
"work:requirements.txt/keep" = ""
