id = "config-precedence-flag-before-subcommand"
argv = ["-o", "json", "config", "--list"]
declare = ["config:ldcli/config.yml"]
rust = true

[seed]
"config:ldcli/config.yml" = "output: plaintext\n"

[expect]
status = 0
stdout_file = "config-precedence-flag-before-subcommand.stdout"

[expect.files]
"config:ldcli/config.yml" = "output: plaintext\n"
