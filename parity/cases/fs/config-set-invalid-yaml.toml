id = "config-set-invalid-yaml"
argv = ["config", "--set", "output", "json"]
declare = ["config:ldcli/config.yml"]
rust = true

[seed]
"config:ldcli/config.yml" = "output: [unclosed\n"

[expect]
status = 1
stdout_file = "config-set-invalid-yaml.stdout"
stderr_file = "config-set-invalid-yaml.stderr"

[expect.files]
"config:ldcli/config.yml" = "output: [unclosed\n"
