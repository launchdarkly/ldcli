id = "config-yaml-quoting-trailing-space"
argv = ["config", "--set", "project", "trail ", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-trailing-space.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: 'trail '\n"
