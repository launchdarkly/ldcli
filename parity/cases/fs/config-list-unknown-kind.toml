id = "config-list-unknown-kind"
argv = ["config", "--list", "--output", "yaml"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 1
stdout_file = "config-list-unknown-kind.stdout"
stderr_file = "config-list-unknown-kind.stderr"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
