id = "config-yaml-quoting-colon-space"
argv = ["config", "--set", "project", "a: b", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-colon-space.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: 'a: b'\n"
