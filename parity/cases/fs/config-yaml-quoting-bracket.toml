id = "config-yaml-quoting-bracket"
argv = ["config", "--set", "project", "[x", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-bracket.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: '[x'\n"
