id = "setup-install-already-in-requirements"
argv = ["setup", "install", "--sdk-id", "python-server-sdk", "--package-manager", "pip"]
declare = ["config:ldcli/config.yml", "work:requirements.txt"]
rust = true

[seed]
"bin:pip" = "#!/bin/sh\necho ok\n"
"work:requirements.txt" = "launchdarkly-server-sdk==9\n"

[expect]
status = 0
stdout_file = "setup-install-already-in-requirements.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
"work:requirements.txt" = "launchdarkly-server-sdk==9\n"
