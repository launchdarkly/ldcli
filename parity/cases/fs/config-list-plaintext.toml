id = "config-list-plaintext"
argv = ["config", "--list", "--output", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[seed]
"config:ldcli/config.yml" = "output: markdown\naccess-token: api-not-a-real-token\nproject: proj\n"

[expect]
status = 0
stdout_file = "config-list-plaintext.stdout"

[expect.files]
"config:ldcli/config.yml" = "output: markdown\naccess-token: api-not-a-real-token\nproject: proj\n"
