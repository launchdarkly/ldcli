id = "config-yaml-quoting-url"
argv = ["config", "--set", "project", "https://app.launchdarkly.com", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-url.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: https://app.launchdarkly.com\n"
