id = "config-yaml-quoting-yes"
argv = ["config", "--set", "project", "yes", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-yes.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \"yes\"\n"
