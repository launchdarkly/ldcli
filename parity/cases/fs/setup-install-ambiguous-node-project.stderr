cannot tell which package manager to use: this project doesn't say which package manager it uses
pass --package-manager with one of: npm (not installed), yarn (not installed), pnpm (not installed), bun (not installed)
