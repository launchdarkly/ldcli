id = "config-yaml-quoting-float"
argv = ["config", "--set", "project", "1.5", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-float.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \"1.5\"\n"
