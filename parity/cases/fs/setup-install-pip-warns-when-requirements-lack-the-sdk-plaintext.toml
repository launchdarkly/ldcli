id = "setup-install-pip-warns-when-requirements-lack-the-sdk-plaintext"
argv = ["setup", "install", "--sdk-id", "python-server-sdk", "--package-manager", "pip", "-o", "plaintext"]
declare = ["config:ldcli/config.yml", "work:requirements.txt"]
rust = true

[seed]
"bin:pip" = "#!/bin/sh\necho ok\n"
"work:requirements.txt" = "flask\n"

[expect]
status = 0
stdout_file = "setup-install-pip-warns-when-requirements-lack-the-sdk-plaintext.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
"work:requirements.txt" = "flask\n"
