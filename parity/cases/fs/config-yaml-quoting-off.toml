id = "config-yaml-quoting-off"
argv = ["config", "--set", "project", "off", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-off.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \"off\"\n"
