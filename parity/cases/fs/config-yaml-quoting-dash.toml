id = "config-yaml-quoting-dash"
argv = ["config", "--set", "project", "-", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-dash.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: '-'\n"
