id = "config-yaml-quoting-star"
argv = ["config", "--set", "project", "*x", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-star.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: '*x'\n"
