id = "config-yaml-quoting-nan"
argv = ["config", "--set", "project", ".nan", "-o", "plaintext"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "config-yaml-quoting-nan.stdout"

[expect.files]
"config:ldcli/config.yml" = "project: \".nan\"\n"
