id = "setup-detect-python-pyproject-hatch"
argv = ["setup", "detect"]
declare = ["config:ldcli/config.yml", "work:pyproject.toml"]
rust = true

[seed]
"work:pyproject.toml" = "[tool.hatch.build]\npackages = [\"app\"]\n"

[expect]
status = 0
stdout_file = "setup-detect-python-pyproject-hatch.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
"work:pyproject.toml" = "[tool.hatch.build]\npackages = [\"app\"]\n"
