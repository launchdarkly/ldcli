id = "setup-init-help-topic"
argv = ["help", "setup", "init"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "setup-init-help-topic.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
