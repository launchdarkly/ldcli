id = "setup-install-help-after-flags"
argv = ["setup", "install", "--sdk-id", "node-server", "--help"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "setup-install-help-after-flags.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
