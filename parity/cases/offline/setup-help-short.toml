id = "setup-help-short"
argv = ["setup", "-h"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "setup-help-short.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
