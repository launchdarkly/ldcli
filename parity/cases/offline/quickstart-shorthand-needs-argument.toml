id = "quickstart-shorthand-needs-argument"
argv = ["quickstart", "-o"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 1
stdout_file = "quickstart-shorthand-needs-argument.stdout"
stderr_file = "quickstart-shorthand-needs-argument.stderr"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
