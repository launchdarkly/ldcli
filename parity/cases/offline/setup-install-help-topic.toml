id = "setup-install-help-topic"
argv = ["help", "setup", "install"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "setup-install-help-topic.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
