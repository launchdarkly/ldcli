id = "root-help-wins-over-version"
argv = ["--help", "--version"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "root-help-wins-over-version.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
