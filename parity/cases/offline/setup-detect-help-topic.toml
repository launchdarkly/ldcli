id = "setup-detect-help-topic"
argv = ["help", "setup", "detect"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "setup-detect-help-topic.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
