id = "setup-init-flag-needs-argument"
argv = ["setup", "init", "--sdk-id"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 1
stdout_file = "setup-init-flag-needs-argument.stdout"
stderr_file = "setup-init-flag-needs-argument.stderr"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
