id = "root-help-subcommand"
argv = ["help"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "root-help-subcommand.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
