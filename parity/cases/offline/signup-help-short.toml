id = "signup-help-short"
argv = ["signup", "-h"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "signup-help-short.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
