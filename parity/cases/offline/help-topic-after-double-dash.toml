id = "help-topic-after-double-dash"
argv = ["help", "--", "config"]
declare = ["config:ldcli/config.yml"]
rust = true

[expect]
status = 0
stdout_file = "help-topic-after-double-dash.stdout"

[expect.files]
"config:ldcli/config.yml" = "{}\n"
